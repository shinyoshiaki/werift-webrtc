werift GCC 実装を固定 libwebrtc pin と体系的に突合し、自律的に問題を探索・修正して完成させる

対象リポジトリ:

- "shinyoshiaki/werift-webrtc"

対象ブランチ:

- "ticket/25088816-7ffa-4f6d-86bc-34d0a3100d01"

対象チケット:

- "TICKET-ticket-25088816-7ffa-4f6d-86bc-34d0a3100d01.md"
- 親要件: "TICKET-ticket-c724daf9-5f79-4e30-bd50-7b27c8951844.md"

libwebrtc の比較基準:

- commit: "0fda16159e33adf59c71a7ad1173dcbe5a632102"
- ローカル snapshot: "third_party/libwebrtc-ref/"

目的

現在の GCC send-side BWE 実装について、既知のレビュー指摘だけを修正して終了してはいけない。

固定 pin の libwebrtc と werift の実装を subsystem / state transition / lifetime / ordering / clock domain 単位で体系的に比較し、

1. 既知の問題を修正する
2. 修正によって生まれた新しい意味論差を探す
3. まだ指摘されていない同種の問題を自律的に探索する
4. 発見した問題ごとに regression test を追加する
5. 修正後に再度固定 pin と比較する
6. Blocker / Major がゼロになるまで繰り返す

こと。

「既知指摘を全部直した」だけでは完了ではない。

最終的に、

- Blocker: 0
- Major: 0
- GCC の主要 state transition に説明不能な pin 差がない
- lifetime が有界
- clock-domain 混在がない
- Known Differences が重大な意味論差を隠していない
- 親チケットの acceptance criteria を回帰させていない
- unit test / type check / simulation が成功

となった時点だけを実装完了とする。

---

1. Source of truth

互換性判断には次の2つだけを使うこと。

1. 親・子チケットの要件
2. "third_party/libwebrtc-ref/" に固定された libwebrtc "0fda16159..."

禁止:

- Web 上の最新 "main" を互換性判断に混ぜる
- 記憶している libwebrtc の挙動を根拠にする
- 差異を調べず "Known Differences" に追加して済ませる
- 単体テストが通ることだけを根拠に仕様一致と判断する

必要な libwebrtc source が snapshot に不足している場合は、

1. 固定 commit "0fda16159..." の該当ファイルを特定
2. "third_party/libwebrtc-ref/fetch_ref.py" の curated list に追加
3. 同じ固定 commit から取得
4. "PIN.json" / "MANIFEST.md" を整合させる

こと。

別 revision を取得して比較してはいけない。

---

2. 最初に修正・再確認する既知領域

以下は現在までにレビューで問題が見つかった領域である。

ただし、これらを直した時点で作業を終了してはいけない。

2.1 RTT semantics

現在までの調査では、werift が raw な

feedback arrival - earliest send

を high RTT 判定に使っている可能性がある。

固定 pin では少なくとも

feedback_rtt
pending_time
propagation_rtt

を区別している。

以下を固定 pin の実際のデータフローから追跡すること。

OnTransportPacketsFeedback
  ↓
UpdatePropagationRtt
  ↓
SendSideBandwidthEstimation
  ↓
IsRttAboveLimit
  ↓
GetBandwidthLimitedCause
  ↓
ProbeController

"send_side_bandwidth_estimation.{h,cc}" が snapshot に無ければ固定 pin から追加する。

特に、

長い feedback batch
≠
高 propagation RTT

であることを考慮する。

raw feedback RTT と propagation RTT を混同して probe を禁止していないか検証する。

2.2 feedback 内の state transition

feedback 開始時と終了時だけを比較して state transition を判定してはいけない。

固定 pin のように packet ごとの transition を追う。

例:

normal
  ↓
underuse
  ↓
normal

が1つの TWCC feedback 内で起きた場合でも、

recovered_from_overuse = true

相当を検出できること。

特に以下を探す。

- underuse → normal
- normal → overuse
- overuse → normal
- loss state transition
- HOLD enter/exit

一つの batch の最終 state だけを見て途中 transition を失っていないか。

2.3 probe / delay / loss の update ordering

werift と pin の処理順序を明示的な表にすること。

特に、

probe estimate
delay-based BWE
loss-based BWE
final target
ProbeController::SetEstimatedBitrate
GetBandwidthLimitedCause

の順序を追う。

固定 pin の production path と意味論が一致するようにする。

以下のような構造差を放置してはいけない。

loss update
↓
probe result で loss state を上書き

vs

probe result を delay path に反映
↓
その結果を使って loss update
↓
post-loss state から final target / cause

同じ TWCC batch 内に

- valid probe result
- packet loss
- delay state change

が同時に存在するケースを regression test 化する。

---

3. 既知問題以外を自律的に探索する

以下の観点を 最低1回ずつ subsystem 横断でレビューすること。

3.1 Clock domain

全ての timestamp 比較について、

sender wall clock
feedback arrival clock
TWCC receiver timeline
RTP send timestamp
controller elapsed time

のどの clock domain かを特定する。

異なる domain を

a - b > timeout

のように直接比較していないか探す。

特に:

- probe pacing timeout
- probe result timeout
- estimator history cleanup
- RTT
- inter-arrival
- loss observation
- recovery cooldown
- sentInfos cleanup

を確認する。

3.2 Lifetime coupling

以下の lifetime を別々に追う。

RTP sentInfos
ProbeController session
BitrateProber pacing cluster
ProbeController result wait
ProbeBitrateEstimator measurement history
TWCC soft-loss state
receiver reorder history

一つの timeout や controller completion によって、本来独立している state が一緒に消えていないか確認する。

逆に lifetime が無期限になっていないか確認する。

特に:

- ACK が一度も来ない
- 一部しか送れない
- 一部しか ACK されない
- feedback が永遠に来ない
- wrap が発生する
- recovery probe を長時間繰り返す

ケースを考える。

3.3 Premature cleanup

以下を重点的に探す。

- controller timeout で estimator history を消す
- 80% ACK 時点で残り ACK を捨てる
- partial feedback で active probe を abort
- soft loss を definitive loss として確定
- delayed feedback を早く削除
- sequence wrap 前に必要情報を削除

3.4 Missing cleanup / unbounded growth

Map / Set / queue / history の全てを列挙し、

最大件数
最大保持時間
cleanup trigger

を確認する。

特に:

- "sentInfos"
- "seqToCluster"
- "seqToSendInfo"
- "estimatorHistory"
- "awaitingResults"
- soft lost sequences
- finalized sequences
- TWCC reorder state
- acknowledged bitrate windows
- loss observations

長時間接続で有界か確認する。

3.5 Ordering

各 subsystem が期待する ordering を固定 pin から確認する。

候補:

transport-wide seq order
send-time order
receive-time order
feedback order

特に:

- loss bookkeeping
- acknowledged bitrate
- delay detector
- probe estimator
- soft-loss correction

で同じ sort を流用していないか確認する。

3.6 State collapsing

複数状態を単一 boolean に潰していないか探す。

悪い例:

const congested = ...

だけで、

- overuse
- underuse
- RTT backoff
- loss decreasing
- loss increasing
- loss HOLD
- delay-based

をまとめて制御すること。

固定 pin の enum / cause / state machine が複数ある場合は、その意味を保持する。

3.7 Setter / reset semantics

以下のような操作について、

SetEstimate
SetBandwidthEstimate
Reset
OnRouteChange
SetBitrates

が内部 history をどこまで維持するか固定 pin と比較する。

setter で不必要に、

- RTT
- max-bitrate statistics
- inherent loss
- observations
- HOLD timer
- ALR state
- probe state

を消していないか。

逆に reset すべき状態が残っていないか。

3.8 Same-feedback interactions

単独 subsystem の unit test だけでなく、同じ feedback に複数イベントが含まれるケースを作る。

例:

probe result + loss
probe result + overuse
late ACK + controller timeout
underuse→normal + loss decreasing
high RTT + loss increasing
sequence reorder + soft-loss correction

ここで update order による意味論差を探す。

---

4. 固定 pin との state-transition table を作る

最低限以下を表にして比較する。

Delay

normal
underuse
overuse

Loss

delay_based
decreasing
increasing
IncreaseUsingPadding 相当
HOLD

RTT

normal
high

Probe controller

init
waiting_for_result
complete

Probe lifetime

queued
pacing
send-filled
awaiting result
estimator history
expired

各組み合わせについて最低限、

new probe allowed?
further probe allowed?
recovery probe allowed?
probe result upward apply?
probe result downward apply?
max probe bitrate?
final target source?

を pin / werift で比較する。

説明できない違いがあれば実装を修正する。

---

5. Regression test 方針

問題を見つけたら、原則として

修正前に失敗する最小 regression
↓
実装修正
↓
regression pass

の順で進める。

可能な限り "private" state の直接書き換えではなく public/full-path を使う。

private state を使う必要がある場合も、最終的には少なくとも1本は

rtpPacketSent
↓
TWCC feedback
↓
observable output / probe event / bitrate

を通る full-path regression を作る。

境界値を必ず含める。

例:

- "timeout - 1"
- "timeout"
- "timeout + 1"
- exactly 80%
- exactly max bitrate
- 16-bit sequence wrap
- 24-bit reference-time wrap

---

6. テストが実装を追認していないか self-review する

テストコードを見て、

実装がこうなっているから expected もそうした

だけになっていないか確認する。

expected value / transition は、

- pinned libwebrtc
- ticket requirement

から説明可能であること。

特にコメントに

libwebrtc:
pin:

と書いてあるテストは、本当に固定 pin source と一致しているか再確認する。

過去レビューでは「pin と一致」と書いたテスト自体が誤っていたため、コメントを仕様根拠として信用しないこと。

---

7. Known Differences の扱い

"GCC_KNOWN_DIFFERENCES" を一項目ずつ再監査する。

残してよい差異は以下をすべて満たすものだけ。

1. チケット上許容されている
2. pure TypeScript / architecture 制約上合理的
3. GCC の主要 control response を変えない
4. state transition を変えない、または影響が十分限定的
5. regression test がある

次のような差異を Known Difference にしてはいけない。

- probe の発火可否が変わる
- bandwidth が上がる/下がる条件が変わる
- cleanup lifetime が変わって leak / late feedback loss が起きる
- state transition が変わる
- update ordering が変わって final target が変わる
- pin に存在する重要 input を無視する

Known Difference に追加する前に「実装で合わせられないか」を検討する。

---

8. 親チケット acceptance の回帰確認

GCC 修正だけでなく、以下を壊していないことを確認する。

- 共通 "BandwidthEstimator" interface が薄い
- legacy estimator が default
- estimator 選択 API が維持
- "onAvailableBitrate" は bitrate が変化した時のみ通知
- unit は bps
- legacy 固有 congestion/score event を common interface に漏らさない
- estimator 再利用時 reset
- sender disposal / cleanup
- RTP media / padding sequence ownership
- SR packet/octet counter
- TWCC receiver が GCC 専用になっていない

---

9. develop を必ず評価する

最終段階で、

git fetch
git rev-list --left-right --count develop...HEAD

相当を確認する。

behind がある場合は、その commit 内容を確認する。

GCC と無関係でも、最終 merge 前提で問題がないか評価する。

必要なら develop を取り込んだ上で全 validation を再実行する。

「以前 develop を確認した」は不可。

最終 HEAD に対して確認すること。

---

10. Validation

修正が完了したら最低限次を実行する。

cd packages/webrtc
npx vitest run tests/media/bandwidthEstimator.test.ts

近傍:

npx vitest run \
  tests/media/bandwidthEstimator.test.ts \
  tests/media/receiverTwcc.test.ts \
  tests/media/rtpSender.test.ts

type:

npm run type

package tests:

npm test

peer simulation:

npm run test:sim

Chrome simulation:

cd ../../../e2e
npm run test:sim

さらに変更範囲に応じて repository root で:

npm run test:small

または

npm run ci

を実行する。

失敗した場合は、

原因特定
→ 修正
→ 同じコマンドを再実行

を成功するまで繰り返す。

sim を環境要因で実行できない場合は、成功扱いにせず

- 実行不能理由
- どこまで実行したか
- 未確認範囲

を最終報告に明記する。

---

11. Ticket / docs cleanup

実装と docs を一致させる。

特に親チケット先頭に残っている、

The parent ticket read was rejected...

のようなエージェント生成時のメタ文は削除する。

チケット・README・JSDoc・generated docs・Known Differences が実装と一致しているか確認する。

---

12. 必須 self-review pass

全修正とテスト成功後、実装者としてではなく reviewer としてもう一度見る。

この pass では「既知問題が直ったか」を確認するのではなく、新しい問題を探すことだけを目的にする。

最低限次をもう一度探す。

clock mismatch
ordering mismatch
state collapse
update-order mismatch
premature cleanup
missing cleanup
lifetime coupling
same-feedback interaction
wrap boundary
late feedback
partial feedback
setter/reset asymmetry
Known Differences hiding semantics

最低1回は

「既知指摘とは無関係な Major がないか」

だけを探す pass を行う。

問題を見つけた場合は修正・regression・validation に戻る。

---

13. 完了条件

以下を全部満たすまで「完成」「完了」と報告しない。

- [ ] Blocker = 0
- [ ] Major = 0
- [ ] 固定 pin と主要 state transition を体系的に比較済み
- [ ] RTT semantics が pin と整合
- [ ] feedback 内 state transition を取りこぼさない
- [ ] probe / delay / loss update ordering が pin と整合
- [ ] controller / pacing / estimator history の lifetime が分離されている
- [ ] 全 Map / Set / history が長時間で有界
- [ ] clock-domain 混在がない
- [ ] ordering の誤りがない
- [ ] partial / late / reordered feedback が安全
- [ ] sequence / TWCC reference wrap が安全
- [ ] state-preserving setter が history を不必要に消さない
- [ ] Known Differences が重大な意味論差を隠していない
- [ ] 親 acceptance を回帰していない
- [ ] targeted regression tests 成功
- [ ] package type check 成功
- [ ] package tests 成功
- [ ] peer simulation 成功
- [ ] Chrome simulation 成功、または実行不能理由を明記
- [ ] 必要な広範囲 CI 成功
- [ ] develop 差分を最終 HEAD で評価済み
- [ ] ticket / docs が clean
- [ ] 最終 self-review でも Blocker / Major を発見できない

---

14. 最終報告

最後に次の形式で報告すること。

発見した問題

各項目について:

- Severity
- 原因
- werift の挙動
- pinned libwebrtc の挙動
- 実際に起きる影響
- 修正方法

既知問題と、今回の自律探索で新しく発見した問題を分ける。

実施した修正

ファイル単位ではなく、意味論単位で説明する。

Regression tests

各 test が何の再発を防ぐかを書く。

State-transition comparison

最低限:

状態/遷移| pinned libwebrtc| werift| 最終判定

Validation

実際に実行したコマンドと結果を列挙する。

未実行を成功扱いしない。

Known Differences

残したものだけ列挙し、それぞれなぜ major semantics を変えないか説明する。

develop

最終 HEAD が develop に対して ahead / behind いくつか、behind commit をどう評価したかを書く。

最終 self-review

Blocker: 0
Major: 0
Minor: N

Major が1件でもある場合は、

実装未完了

と明記し、修正サイクルを継続する。

---

最重要ルール

レビューで指摘された項目を全部消すことがゴールではない。

ゴールは、

«固定 pin とチケットを source of truth として自分自身で追加問題を探索し、修正・regression・再比較・self-review を反復した結果、これ以上 Blocker / Major を発見できない状態にすること。»

である。

既知問題を直したあとに、必ずもう一度

「今回の修正によって別の state transition を壊していないか」

を調査すること。