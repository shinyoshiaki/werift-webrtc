# タスク詳細化

## 1. タスクの目的と背景

- このリポジトリには、`packages/webrtc` 向けの **upstream WPT 実行基盤** がすでにあります。  
  - 実行: `npm run wpt --workspace packages/webrtc`
  - カバレッジ: `npm run wpt:coverage --workspace packages/webrtc`
  - 実装: `packages/webrtc/tools/wpt-runner/*`
- runner は `coverage/webrtc-wpt/results.json` / `results.md` を出力し、`results.md` にはすでに **「1件以上 PASS した test file+variant の一覧」** を出せる実装があります。
- したがってこのタスクの本体は、**WPT 実行基盤の新規作成ではなく、既に PASS が出始めている upstream WebRTC WPT を起点に互換性を詰め、対象 file の成功率を 100% まで引き上げること**です。

### baseline.json から確認できる既知の「PASS を含む target」
`packages/webrtc/wpt/baseline.json` ベースでは、少なくとも次の file+variant に PASS 実績があります。  
※これは「既知の seed」であり、**実際の対象決定は最新の `results.json` から再計測**するのが前提です。

| File | Variant | 既知 PASS 数 |
| --- | --- | ---: |
| `webrtc/RTCPeerConnection-removeTrack.https.html` | `(default)` | 12 |
| `webrtc/RTCPeerConnection-onnegotiationneeded.html` | `?rest` | 10 |
| `webrtc/RTCPeerConnection-addIceCandidate.html` | `?rest` | 7 |
| `webrtc/RTCPeerConnection-addTrack.https.html` | `(default)` | 7 |
| `webrtc/RTCPeerConnection-canTrickleIceCandidates.html` | `(default)` | 3 |
| `webrtc/RTCPeerConnection-ontrack.https.html` | `(default)` | 3 |
| `webrtc/RTCPeerConnection-setRemoteDescription-rollback.html` | `?rest` | 3 |
| `webrtc/RTCPeerConnection-setLocalDescription-parameterless.https.html` | `?rest` | 1 |
| `webrtc/RTCPeerConnection-setLocalDescription-rollback.html` | `(default)` | 1 |

---

## 2. 実装すべき具体的な機能や変更内容

### 1. 対象 file の抽出と進捗管理
- 最新の WPT 実行結果から、**PASS が 1 件以上ある file+variant** を抽出する。
- その一覧と進捗を、永続的に追える Markdown に記録する。配置は `packages/webrtc/wpt/progress.md` が自然です。
- `progress.md` には最低限、以下を持たせる。
  - file
  - variant
  - total / pass / fail / timeout
  - success rate
  - status（`active` / `done` / `excluded`）
  - 対象 subtest 一覧
  - subtest ごとの failed attempt count
  - 除外理由

### 2. 失敗試行回数の記録と除外ルール
- 特定 subtest に対して「実装改善 → 再実行」を 1 試行として記録する。
- 改善しても FAIL/TIMEOUT のままなら `failed_attempts += 1`。
- **3回を超えた subtest は task 完了条件から除外**し、`progress.md` に
  - 対象 subtest
  - 試行回数
  - 最終エラー概要
  - 除外理由
  を残す。

### 3. WPT runner / レポート補強
- 既存 `formatMarkdownReport()` は「PASS を含む file+variant 一覧」を出せるので再利用する。
- ただし task 用の進捗管理には、
  - 最新 `results.json` から対象抽出
  - file+variant 単位の成功率計算
  - subtest 単位の attempt 集計
  - 除外対象を除いた completion 判定
  が必要なので、`tools/wpt-runner` 配下へ progress 用ロジックを追加するのが妥当です。

### 4. 互換性向上の実装改善
改善の主戦場は `packages/webrtc/src` で、特に以下です。

| 領域 | 主なファイル | 既知の対象 |
| --- | --- | --- |
| RTCPeerConnection API 互換 | `src/peerConnection.ts` | `addTrack` / `removeTrack` / `addIceCandidate` / `setLocalDescription` / `setRemoteDescription` / `negotiationneeded` / `ontrack` / `canTrickleIceCandidates` |
| transceiver 挙動 | `src/transceiverManager.ts` | sender 再利用、direction 更新、removeTrack |
| SDP 状態管理 | `src/sdpManager.ts` | pending/current description、rollback |
| ICE candidate 適用 | `src/secureTransportManager.ts` | `sdpMid` / `sdpMLineIndex` / `usernameFragment` 整合 |
| WPT 用 media 環境 | `src/nonstandard/navigator.ts` | dummy media 前提の `getUserMedia` |

### 5. テスト補強
- runner/progress 変更: `packages/webrtc/tests/tools/*`
- 互換性変更: `packages/webrtc/tests/integrate/peerConnection.test.ts` 周辺
- この repo では test の Arrange/Act/Assert と日本語コメント規約があるため、それに合わせる。

---

## 3. 技術的な実装アプローチを調査し結果を簡潔にまとめる

### 現行実装の活用ポイント
- `packages/webrtc/tools/wpt-runner/runner.ts`
  - WPT HTML を実行
  - file+variant ごとの進捗整形
  - Markdown レポート生成
- `packages/webrtc/tools/wpt-runner/run.ts`
  - `results.md` を生成
- `packages/webrtc/tests/tools/wptProgress.test.ts`
  - 「PASS を含む file+variant 一覧」の Markdown 出力をすでにテスト済み

### 実装方針
1. まず `npm run wpt --workspace packages/webrtc` の最新結果から、PASS を 1 件以上含む file+variant を抽出する。
2. その結果を `packages/webrtc/wpt/progress.md` に正規化して保存する。
3. file+variant ごとに FAIL/TIMEOUT subtest を潰す。
4. 失敗した subtest だけを `WPT_TARGET_FILTER` で絞って反復する。
5. 3回超えても改善できない subtest は除外し、分母から外して file 成功率を再計算する。

### 実装時に使える既存運用
- 対象絞り込みは `WPT_TARGET_FILTER` が使えます。
- submodule checkout は runner 側で補助されますが、前提として `git submodule update --init --recursive` が必要です。
- WPT 実行環境は dummy media 付き Node VM で、browser 完全再現ではありません。

### 重要な調査結果
- 現在の `discoverWptTargets()` は **`third_party/wpt/webrtc` 配下の HTML を自動発見して実行**しており、現 checkout では **214 targets** あります。
- 一方で `packages/webrtc/wpt/allowlist.json` の `enabled` / `subtests` は、現状 **実行対象の絞り込みには使われていません**。  
  そのため、このタスクを効率よく回すには、**progress 管理対象だけを明示的に回せる仕組み**を先に入れる価値があります。

---

## 4. 考慮すべき制約や注意点

- **「テストファイル」は実質 `file + variant` 単位で扱うべき**  
  runner が variant 付きで別 target として扱っているため。
- **baseline.json は完了判定の元データには不十分**  
  既知 PASS は分かるが、総 subtest 数は持たないため、成功率は最新 `results.json` から計算する必要があります。
- **synthetic failure も失敗として扱うべき**  
  `[timeout]`, `[exception]`, `[worker-timeout]`, `[worker-exit]`, `[harness]` などは infrastructure failure ですが、100% 完了判定では未解決扱いが妥当です。
- **進捗 Markdown は `coverage/` 配下ではなく commit 可能な場所に置くべき**  
  `coverage/webrtc-wpt/results.md` は実行生成物なので、task 管理の正本には向きません。
- **互換性改善は public API に波及しやすい**  
  `RTCPeerConnection` 変更は WPT だけでなく既存 integration test への影響も確認が必要です。
- **repo 規約上、WPT や互換性変更時は docs/test 更新が必要になりやすい**  
  `packages/webrtc/README.md` と関連 test の更新を伴う可能性があります。

---

## 5. 完了条件

1. 最新 WPT 実行結果から、**PASS を 1 件以上含む file+variant の一覧**を抽出できる。  
2. `packages/webrtc/wpt/progress.md` が作成され、file+variant ごとの成功率と subtest 状態を追跡できる。  
3. 各対象 file+variant について、**除外対象を除く全 subtest が PASS** になっている。  
4. 改善に失敗した subtest は failed attempt count が記録され、**3回超過で明示的に除外**されている。  
5. 除外された subtest には、最終状態と除外理由が `progress.md` に残っている。  
6. 互換性改善は `packages/webrtc/src` の実装に反映され、対応する test も更新されている。  
7. WPT runner / progress 仕様を変更した場合、`packages/webrtc/tests/tools/*` のテストが追従している。  
8. 最終的な task 完了判定は、**progress.md 上で active target がすべて 100%**、または **除外ルール適用後に 100%** であること。  

このタスクは、実質的には **「WPT 部分成功 target の洗い出し」→「進捗管理基盤の追加」→「RTCPeerConnection 互換性改善の反復」** の3段階タスクとして扱うのが最も安全です。