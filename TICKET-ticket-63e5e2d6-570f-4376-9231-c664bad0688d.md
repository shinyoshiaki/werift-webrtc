# 実装修正を行う

- Issue: [shinyoshiaki/werift-webrtc#677](https://github.com/shinyoshiaki/werift-webrtc/issues/677)
- 関連: [werift-webrtc#678](https://github.com/shinyoshiaki/werift-webrtc/issues/678)（再利用可能な RTP continuity rewriter。本チケットの後続）
- 背景事例: [dgreif/ring#1816](https://github.com/dgreif/ring/issues/1816)（Ring `answered_timeout` 後も下流 RTP/SRTP タイムラインを維持したまま上流 WebRTC を差し替える）

## 1. タスクの目的と背景

`RTCRtpSender` は track / ソース切替時に RTP の sequence number と timestamp を書き換えて、下流から見たストリームを連続させる。しかし現行の `replaceRTP()` は境界で **直前パケットと同じ出力 seq / timestamp に写像** しうる。加えて discontinuity 分岐で 32bit timestamp に 16bit 演算を使っている。

これが問題になる典型は `MediaStreamTrack.onSourceChanged` 経路である。`getUserMedia` の loop / 再 start は、送出する先頭パケットと同一の header で `onSourceChanged` を発火する。Ring のようなリレーでは、上流コネクションを差し替えても HomeKit 側の長寿命セッションを壊せない。

本チケットの目的は、ソース切替後も **単調かつ wrap 安全な RTP タイムライン** を保ちつつ、新ソース内部のパケット間隔・同一フレーム timestamp・欠落/順序乱れを歪めないこと。公開 API としての再利用 rewriter 導入（#678）は範囲外とする。

## 2. 実装すべき具体的な機能や変更内容

対象は主に `packages/webrtc/src/media/rtpSender.ts`。テストは `packages/webrtc/tests/media/rtpSender.test.ts` を拡張し、Arrange 共通化が必要なら `packages/webrtc/tests/fixture.ts` に寄せる。

### 2.1 境界オフセットの修正（必須）

現行:

```ts
this.seqOffset = uint16Add(this.sequenceNumber, -sequenceNumber)
this.timestampOffset = uint32Add(this.timestamp, -timestamp)
```

`sequenceNumber` / `timestamp` は直近の **出力** 値。新ソース先頭が `seq=100, ts=5000`、直前出力が `seq=40001, ts=93000` のとき、先頭パケットの出力は再び `40001 / 93000` になる（重複）。

修正方針（Issue の suggested direction）:

```text
seqOffset = nextOutputSeq - firstInputSeq
timestampOffset = nextOutputTimestamp - firstInputTimestamp
```

- `nextOutputSeq = uint16Add(lastOutputSeq, 1)`
- `nextOutputTimestamp = uint32Add(lastOutputTimestamp, timestampStep)`
- 以降のパケットにはこの **固定オフセット** だけを適用する（パケット毎に再計算しない）

これで先頭パケットは seq がちょうど 1 進み、新ソースの相対 seq/ts 構造は保存される。

### 2.2 timestamp の 32bit 演算（必須）

現行 discontinuity 分岐:

```ts
this.timestampOffset = uint16Add(this.timestampOffset, 1)
```

RTP timestamp は 32bit。`packages/common/src/number.ts` の `uint32Add` を使う。seq 側の `uint16Add(..., 2)` も、修正後の「先頭は last+1」と矛盾するため見直し対象。

### 2.3 切替経路の統一（必須）

呼び出し箇所:

| 経路 | 場所 | 現行の問題 |
| --- | --- | --- |
| `onSourceChanged` | `registerTrack()` | 送出する先頭パケットと同一 header で `replaceRTP`。重複バグが顕在化 |
| `replaceTrack()` | 先頭受信待ちのあと `replaceRTP` | `asPromise()` で見たパケットは `registerTrack` 前のため **送出されない**。`track.header` 利用時は「最後に受信した header」基準 |
| 公開 `replaceRTP()` | `doc/classes/RTCRtpSender.md` | 上記と同じ計算 |

受け入れ条件「最初の置換パケットの seq がちょうど 1 進む」を両経路で満たすため、**実際に送出される先頭パケット** で固定オフセットを確定する（pending フラグ + 次の `sendRtp`、または probe パケットも送出する）。`replaceRTP({ sequenceNumber, timestamp })` の意味は「これから送る新ソース先頭の入力 header」と文書化する。

### 2.4 `onSourceChanged` 購読の破棄（推奨・同一修正に含める）

`disposeTrack` は `onReceiveRtp` のみ解除し、`onSourceChanged` は残る。`replaceTrack` 繰り返しでハンドラが積み上がり、切替後の旧 track 発火がオフセットを壊しうる。`disposeTrack` に含める。

### 2.5 テスト（必須）

既存の `replaceTrack` テストは `sendRtp` の呼び出し回数のみで、書き換え後の seq/ts を見ていない。`dtlsTransport.state = "connected"` と `sender.codec` をセットし、`dtlsTransport.sendRtp` を spy して **書き換え後 header** を検証する。

`sendRtp` は header を in-place で書き換える。同一 `RtpPacket` を再利用しない（`clone()`）。

カバーすべきケース（Issue Acceptance criteria）:

- `replaceTrack()` と `onSourceChanged` の両方
- 置換後先頭パケットの seq が直前出力からちょうど 1
- timestamp が 32bit 演算（`uint16Add` では壊れる値、例: offset > 0xffff）
- seq 16bit wrap（例: last=65535 → 0）と timestamp 32bit wrap（例: last=0xfffffffe）
- 境界前後の重複・再順序・欠落。固定オフセットなので新ソースのギャップ/乱れは出力にも同じ相対関係で残る
- 同一 timestamp の複数 RTP（ビデオフレーム）。切替後も同一フレームは同一出力 timestamp
- 連続複数回のソース切替
- 既存の RTX（`rtpCache` クリア後の NACK）、RTCP SR（`rtpTimestamp` は書き換え後値）、TWCC 拡張が壊れないこと

テスト規約: Arrange / Act / Assert。Arrange は `tests/fixture.ts` 等へ集約。Act / Assert には日本語コメント。

### 2.6 範囲外

- #678 の公開 `RtpContinuityRewriter`
- シグナリング再接続、Ring / HomeKit 固有のリトライ
- SSRC の世代管理や RTX シーケンスのリセット（sender SSRC は既に `sendRtp` で上書き）
- WPT `RTCRtpSender-replaceTrack.https.html` のカバレッジ拡張は必須ではない（本バグは werift 独自の continuity 拡張）

## 3. 技術的な実装アプローチ（調査結果）

### 現行フロー

1. `sendRtp` が入力 header に `seqOffset` / `timestampOffset` を足し、結果を `this.sequenceNumber` / `this.timestamp` に保存する。
2. `replaceRTP` は「最後の出力」と「新ソースの与えられた入力」の差をオフセットにする。切替後は固定加算であり、パケット毎の再計算はしていない。欠陥は **境界値の求め方** と **uint16 誤用**。
3. `onSourceChanged` は `userMedia` の loop / 再 start で、`packetizer.packetize` 後・`writeRtp` 前に先頭 header で発火する（`packages/webrtc/src/nonstandard/userMedia.ts`）。
4. `replaceRTP` は `rtpCache` を空にする。NACK/RTX は切替前 seq を再送しない（下流タイムライン付け替えと整合）。

### 推奨実装

`replaceRTP` 内（または同ファイルの内部関数）で wrap 安全に:

```text
seqOffset      = uint16Add(uint16Add(lastSeq, 1), -firstInputSeq)
timestampOffset = uint32Add(uint32Add(lastTs, timestampStep), -firstInputTs)
```

未送信時（`this.sequenceNumber == undefined`）はオフセットを触らない（現行通り）。

**timestampStep:** 既定は `1`（衝突回避の最小増分）。ビデオの同一フレームは入力 ts が同じなので、固定オフセットなら出力 ts も揃う。直前パケット間 delta は同一フレームで 0 になりうるため使わない。codec の clockRate（映像 90000 / Opus 48000）に基づくフレーム間隔は、より自然なギャップが必要なときだけ `discontinuity` または明示引数で足す。

**replaceTrack と onSourceChanged の差:** ヘッダ基準の即時計算だと、probe を捨てる `replaceTrack` では先頭送出が last+2 になりうる。受け入れ条件を満たすなら、切替時は pending にし **最初に `sendRtp` するパケットの書き換え前 seq/ts** でオフセットを確定するのが安全。公開 `replaceRTP(header)` は「この header が次に送るパケット」とみなし即時計算してよい。

#678 に備え、計算は小さな純粋関数に分離してよいが、今は `packages/rtp` の公開 API には出さない。

### 検証コマンド

パッケージローカルを優先:

```bash
cd packages/webrtc && npm test -- tests/media/rtpSender.test.ts
cd packages/webrtc && npm run type
```

sender 周辺や track 購読まで触る場合は `cd packages/webrtc && npm test`。

## 4. 考慮すべき制約や注意点

- **公開 API:** `replaceRTP` は typedoc 公開。オフセット意味が変わるのは意図した修正。呼び出し側は「新ソース先頭の入力 header」を渡す前提にする。
- **in-place 変異:** `sendRtp` は `header.sequenceNumber` / `timestamp` / `ssrc` / `payloadType` / `extensions` を書き換える。offset 計算は書き換え前の入力値で行う。
- **uint 演算:** seq は必ず `uint16Add`、timestamp は必ず `uint32Add`。JS の素の加減算や `uint16Add` の timestamp 利用は禁止。
- **固定オフセット:** 切替後に欠落・再順序・重複があっても offset を更新しない。更新すると新ソースの RTP トポロジが壊れる。
- **RTX / RTCP / TWCC:** `rtpCache` クリアは維持。RTX 用 `rtxSequenceNumber` は独立でリセット不要。RTCP SR の `rtpTimestamp` は書き換え後値のまま。TWCC は transport 側カウンタで、メディア seq とは独立。
- **WPT 厳格化の漏れ込み禁止:** 独自 continuity を `packages/webrtc/src` のブラウザ互換デフォルトから切り離して壊さない（本修正は既存の独自拡張の正しさ）。
- **Windows 非対応:** 実行・テストは Unix 系。
- **後続 #678:** 公開 rewriter・リレー example は別チケット。こちらは sender の正しさとテストに留める。

## 5. 完了条件

- [ ] ソース切替後の先頭送出パケットが、直前出力 seq を再利用せず **ちょうど 1** 進む（16bit wrap 込み）
- [ ] timestamp 連続性が **32bit** 演算のみで、discontinuity 経路の `uint16Add` 誤用が無い
- [ ] 切替後オフセットが固定で、新ソースの欠落・再順序・重複・同一フレーム同一 timestamp が相対的に保存される
- [ ] `replaceTrack()` と `onSourceChanged` の両方で上記が成り立つ
- [ ] 連続した複数回切替でもオフセットが安定する
- [ ] `onSourceChanged` 購読が track 差し替え / `stop` で解除される
- [ ] `packages/webrtc/tests/media/rtpSender.test.ts`（必要なら fixture）が 2.5 のケースをカバーし、Arrange / Act / Assert と日本語コメントがある
- [ ] `cd packages/webrtc && npm test -- tests/media/rtpSender.test.ts` と `npm run type` が通る
- [ ] 既存の sender RTX / RTCP 経路を意図せず壊していない（関連テストまたは明示的な回帰ケース）
- [ ] 公開 `replaceRTP` の「先頭入力 header を渡す」意味がコードと一致している。typedoc を変える場合は `doc/classes/RTCRtpSender.md` を再生成または追記する
- [ ] #678 の公開 rewriter をこのチケットでは追加しない