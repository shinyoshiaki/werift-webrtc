# 主要なユースケースを想定してヒープスナップショットを定期的に取得して結果をレポート化してメモリリークを発見できるようにする自動試験を実装する

## 1. タスクの目的と背景

### 目的

werift-webrtc の主要なユースケース（DataChannel 通信、メディア通信、PeerConnection の接続/切断の繰り返し）を長時間実行する自動試験を実装し、以下 3 点を実現する。

- 実行中にヒープスナップショットとメモリ使用量のサンプリングを**定期的に取得**する
- 取得結果を**レポート化**し、ヒープ使用量の増加傾向（メモリリーク）を自動検出できるようにする
- 実時間で長時間待つのではなく、**実時間より高速に多くのパケットを送信**することで「長時間相当」の試験を短時間で実行する（時間加速）

### 背景（コードベース調査の結果）

- `werift-webrtc` は Node.js 用の純 TypeScript WebRTC 実装。テスト基盤は **vitest 3.0.5**（`vitest.workspace.mjs` が `packages/*/vitest.config.mts` を読み込む。`packages/webrtc/vitest.config.mts` は `minWorkers: 3, maxWorkers: 3, retry: 1`）。
- リポジトリ全体（`packages/` `e2e/` `loadtest/` `benchmark/` `examples/`）を検索した結果、**ヒープスナップショットやメモリリーク検出の既存実装は存在しない**。
- 既存のリーク関連チェックは `packages/sctp/tests/open-handles-check.ts` の「タイマーハンドル数の baseline / after 比較」のみ（リソースリーク検出の先例パターンとして参照可能）。
- 類似の実行系リポジトリ: `benchmark/`（ts-node で単体スクリプト実行、`issue540-repro.ts` は既知問題の再現スクリプト）、`loadtest/`（外部サーバ向けロードテスト）。
- CI は `.github/workflows/nodejs.yml` が Node 18.x で `npm run ci` を実行するのみで、メモリ系試験は含まれない。本チケットでは **memleak 試験を GitHub Actions などの CI の対象にしない**方針を確定済み。
- 主要なユースケースの実装・テストの先例:
  - DataChannel: `packages/webrtc/tests/integrate/datachannel.test.ts`、`tests/utils.ts` の `createDataChannelPair()`
  - メディア通信: `packages/webrtc/tests/integrate/mediaChannel.test.ts`（`MediaStreamTrack` + `transceiver.sender.onReady` + `track.writeRtp()`）
  - 接続ライフサイクル: `tests/issue/141.test.ts`、`e2e/tests/datachannel/close.test.ts`
- 実行環境は **Node 24 以上を下限**とする（`node:v8` の `writeHeapSnapshot()` / `getHeapStatistics()` / `process.memoryUsage()` が Node 24 で利用可能。スナップショット 1 枚あたり約 5MB と実機検証済み）。

## 2. 実装すべき具体的な機能や変更内容

### 2.1 メモリリーク試験（本体）

新規 `packages/webrtc/tests/memleak/` 配下に以下を作成する（AGENTS.md のテスト規約に従い、Arrange 用ユーティリティは単一ファイルに集約し、Act / Assert には適切な粒度で日本語コメントを付ける）。

| ファイル | 役割 |
|---|---|
| `memleak.test.ts` | 各シナリオを N サイクル（既定 50、環境変数で変更可）繰り返すテスト本体。ヒープ増加トレンドが閾値を超えたら失敗させる |
| `scenarios.ts` | 主要ユースケースのシナリオ定義（Arrange / 実行本体） |
| `heapUtils.ts` | ヒープ計測・スナップショット取得・GC 強制・サンプリングの共通ユーティリティ（単一ファイルに集約） |
| `report.ts` | レポート（JSON / CSV / Markdown）生成 |
| `memleak.vitest.config.mts` | メモリ試験専用 vitest 設定（後述の制約に対応。`--expose-gc`、タイムアウト延長、並列 worker 無効化） |

### 2.2 対象シナリオ（主要なユースケース）

1. **DataChannel ループ**: ピア 2 台（`RTCPeerConnection`）を offer/answer + ICE 候補交換で接続 → DataChannel を open → メッセージ送受信 → 両 `close()` → 繰り返し。毎サイクル新規に接続を張ることで、トランスポート・SCTP アソシエーション・タイマー等のリーク源を網羅する。
2. **メディア通信ループ**: ピア 2 台を接続し、`MediaStreamTrack` + `addTransceiver(track, { direction: "sendonly" })` 相当でビデオ RTP（`track.writeRtp()`）を送受信 → 両 `close()` → 繰り返し。
3. **接続ライフサイクルのみ（コントロール）**: メディア/データなしで接続確立（ICE → DTLS → SCTP）と `close()` だけを繰り返す。上位層の影響を分離するためのベースライン。

各シナリオは環境変数（例: `MEMLEAK_ITERATIONS`、`MEMLEAK_SNAPSHOT_INTERVAL`）で回数・スナップショット間隔を調整可能にする。

### 2.3 時間加速（長時間相当を短時間で実行する工夫）

「長時間実行」を実時間で待たずに、**実時間より高速にパケットを送る**ことで同等の処理量（長時間分のパケット数・接続数）を短時間で処理する。

- **メディア送信（RTP）**: 実時間の 30fps ペーシングを再現せず、`track.writeRtp()` をタイトループで連続送信する。調査の結果、werift の送信経路（`writeRtp` → `rtpSender.sendRtp()` → `dtlsTransport.sendRtp()`）には**ペーシングが無く即時送信**されるため、1 サイクルあたりのフレーム数を指定（例: `MEMLEAK_MEDIA_FRAMES`、既定 1000 枚/サイクル）して大量に流す。30fps で 1 時間＝約 108,000 枚相当も、タイトループなら数分で送出できる。
- **DataChannel 送信**: 1 サイクルあたりのメッセージ数を指定（例: `MEMLEAK_DC_MESSAGES`）し、RTT 待ちせず一括送信する。送信速度は SCTP のフロー制御（輻輳/ウィンドウ）が自然にバックプレッシャーとして効くため、`send()` の await を尊重すれば安全に高速化できる。
- **接続ライフサイクル**: 毎サイクル新規接続（offer/answer + ICE → DTLS → SCTP 確立）と `close()` を**ギャップなしに連続実行**する。確立・切断のオーバーヘッドを実時間で待たず、次のサイクルへ即座に進む。
- **実行時間の短縮検証**: レポートに「実時間（wall clock）」と「処理量（送信パケット数 / サイクル数）」の両方を記録し、時間加速の効果（例: 実時間 1 時間相当を数分で消化）を確認できるようにする。

> 注: 実時間に依存する挙動（ICE consent freshness、SCTP heartbeat / RTO、DTLS 再送など）はタイマー処理のためそのまま実時間で動作する。これらを加速する場合の選択肢は §3.5 を参照。

### 2.4 定期的なヒープスナップショットとサンプリング

- 各サイクル終了後に `global.gc()` で GC を強制してから `process.memoryUsage()`（rss / heapTotal / heapUsed / external / arrayBuffers）と `v8.getHeapStatistics()`（heap_size_limit 等）をサンプリングする。
- 定期的に `v8.writeHeapSnapshot()` でスナップショットファイルを保存する（既定は数サイクルに 1 枚、失敗時は必ず保存）。

### 2.5 レポート生成とリーク判定

- **判定**: ウォームアップ数サイクル後の heapUsed の推移を線形回帰し、傾き（bytes/cycle）が閾値を超えた場合、または最終サンプルが「初期ベースライン + 許容マージン」を超えた場合に失敗と判定する。GC のゆらぎに強くするため単一サンプルではなく複数サンプルの中央値/移動平均を使う。
- **レポート**: 実行結果を `artifacts/memleak/` 配下に出力する。
  - `report.json`: シナリオ毎のサイクル別メトリクス（heapUsed 等の時系列）+ 判定結果 + スナップショットファイル一覧
  - `report.csv`: 時系列データ（トレンド可視化用）
  - `summary.md`: 人間向け要約（シナリオ毎の結果、判定、スナップショットパス）
- スナップショットは Chrome DevTools での手動解析にも使えるよう、保存パスをレポートに記録する。

### 2.6 npm スクリプトとドキュメント

- `packages/webrtc/package.json` に `memleak` スクリプトを追加する（例: `vitest run ./tests/memleak --config vitest.memleak.config.mts`）。
- **実行環境**: memleak 試験は **Node 24 以上**で実行することを前提とし、Node 24 での動作を検証する（Node 18 等の旧バージョンでの動作保証はしない）。
- パッケージガイド（`packages/webrtc/AGENTS.md` 等）の Commands にスクリプトと実行方法を追記する（AGENTS.md の「scripts 変更時はガイド更新」ルールに従う）。
- `.gitignore` にスナップショット保存先（`artifacts/`）を追加する。
- **CI 対象外を確定**: memleak 試験は GitHub Actions などの CI（`npm run ci` / `.github/workflows/*`）の対象にしない。ローカル / 手動でのみ実行する開発者向けツールとして位置づけ、workflow の追加は行わない。

## 3. 技術的な実装アプローチ（調査結果）

### 3.1 計測 API（Node v24 で実機検証済み）

```ts
import { writeHeapSnapshot, getHeapStatistics } from "node:v8";
import { memoryUsage } from "node:process";
// - writeHeapSnapshot(path) -> 約 5MB/枚 の .heapsnapshot を生成
// - getHeapStatistics() -> heap_size_limit 等
// - memoryUsage() -> rss / heapTotal / heapUsed / external / arrayBuffers
```

- GC 強制のため `--expose-gc` で Node を起動し `global.gc()` を呼ぶ。Node 24 環境で vitest の `poolOptions.*.execArgv` による worker への `--expose-gc` 伝搬を実機検証して有効化する（`NODE_OPTIONS=--expose-gc` はフォールバックとして併記可）。

### 3.2 実行方式の選択

- **推奨**: `packages/webrtc/tests/memleak/` を既定の `vitest run ./tests` から**除外**し、専用 vitest config（`memleak.vitest.config.mts`）と専用 npm スクリプトでのみ実行する。理由:
  - 既定設定は `minWorkers: 3` で並列実行されるため、メモリ計測が他テストと混ざって不正確になる。
  - `retry: 1` の自動リトライはリーク検出の失敗を隠すため不適切。
  - 実行時間が長く（50 サイクルで数分）、既定の `test:small` / `ci` を圧迫する。
- 除外方法: 既定 config の `exclude` に `**/tests/memleak/**` を追加、または memleak 側を他ディレクトリ構成にする。
- 代替案: `benchmark/` と同様に ts-node / tsx の単体スクリプトとして実装する手もあるが、vitest の expect ベースの判定・レポート規約と合わせるなら専用 config 方式が一貫する。

### 3.3 リーク判定の実装方針

- 計測: 各サイクルで `global.gc()` → `await` 一巡（未処理の非同期クリーンアップを消化）→ サンプル取得。
- 判定: ウォームアップ（例: 最初の 10 サイクル）を除いた heapUsed の線形回帰の傾きを計算。閾値（例: 傾き > 一定値、または最終サンプルが初期中央値 + 許容割合を超える）で失敗。単発のばらつきを吸収するため複数サンプルの中央値/移動平均を使用。
- デバッグ用: 検出されたリークの原因特定は `writeHeapSnapshot` の `.heapsnapshot` を Chrome DevTools で開いて行う（自動比較は行わない）。

### 3.4 既存テスト資産の再利用

- `packages/webrtc/tests/utils.ts` の `createDataChannelPair()` / `exchangeOfferAnswer` / `exchangeIceCandidates` は Arrange ユーティリティとして再利用可能（ただし実装の都合で必要な場合は memleak 用に軽量版を `scenarios.ts` 側に集約してもよい）。
- `packages/sctp/tests/open-handles-check.ts` のタイマーハンドル比較パターンを補助メトリクスとして併用すると、ヒープ以外のリソースリーク（タイマー等）も捕捉できる。

### 3.5 時間加速の実現方法（調査結果）

- **RTP 即時送信の確認**: `packages/webrtc/src/media/` の送信経路は `MediaStreamTrack.writeRtp()` → `onReceiveRtp` → `rtpSender.sendRtp()` → `dtlsTransport.sendRtp()` で**ペーシング・遅延なし**。既存の `tests/integrate/mediaChannel.test.ts` もペースなしで RTP を連続書き込みしており、時間加速の根拠として利用できる。
- **タイマー系は定数のため実時間動作**: 
  - ICE consent freshness: `packages/ice/src/iceBase.ts` の `CONSENT_INTERVAL = 5s` / `CONSENT_TIMEOUT = 30s` が**モジュール定数**で、インスタンス毎の変更は不可。長時間接続相当の consent 蓄積を加速するにはソース側の定数化・環境変数化が必要なため、本チケットでは「時間加速の対象外（実時間で動作）」とし、必要になった場合は別タスクで定数の環境変数化を検討する。
  - SCTP: `sctp.setHeartbeatInterval(interval)` はインスタンス単位で設定変更可能（`packages/sctp/src/sctp.ts`）。heartbeat 加速が必要ならテスト側で縮小設定できる。RTO は実測 RTT から計算されるためそのまま利用。
  - DTLS: 再送間隔は 1 秒固定（`packages/dtls/src/flight/flight.ts`）で、接続確立が短時間なため加速不要。
- **fake timer（`vi.useFakeTimers()`）は不採用**: 実ネットワーク I/O（dgram 等）とトランスポート内部タイマーが混在するため、仮想クロックの制御は不安定。**パケット送信レートの高速化**を主軸とし、必要に応じて SCTP heartbeat のみ縮小設定する方式を採る。
- **代替案（並列化）**: ピアペアを複数同時接続して単位時間あたりの処理量を増やす手もあるが、ヒープ計測は同時接続分のオブジェクトが混在してノイズになるため、本試験では「逐次ループ × 高速送信」を主とし、並列化は行わない（計測の確実性を優先）。

## 4. 考慮すべき制約や注意点

- **GC の非決定性**: スナップショット/サンプリング値は GC タイミングに左右されるため、必ず強制 GC + 複数サンプル + ウォームアップを入れ、閾値は十分な余裕を持つ。過敏すぎる判定は誤検知の原因になる（CI フレークは対象外だが、手動実行の信頼性を損ねる）。
- **実行時間とリソース**: 時間加速により 50 サイクル × 3 シナリオで数分規模に収まる想定（実時間長時間相当を短時間化）。スナップショットは約 5MB/枚で、全サイクル保存するとディスクを圧迫するため間引き保存（失敗時は必ず保存）とする。
- **時間加速の対象範囲**: リークは実時間ではなく処理量（パケット数・サイクル数）に比例するため高速送信が有効。ただし実時間に依存する挙動（ICE consent freshness、DTLS 再送など）はそのまま実時間で動作する点に注意し、レポートの時間軸は「実時間」と「処理量」を分けて記録する。
- **高速化と実挙動の乖離**: 送信レートを極端に上げると SCTP の輻輳制御・再送・順序制御のタイミングが実時間運用と乖離する可能性がある。判定は「パケット処理量ベースのリーク検出」に限定し、タイミング挙動そのものの検証は対象外とする。
- **バックプレッシャー**: 高速一括送信時は SCTP フロー制御やイベントループ負荷で実効速度が頭打ちになる。`send()` の戻り値 / await を尊重し、イベントループを塞がない範囲で送信する。
- **既定テストスイートへの影響**: `npm run test:small` / `npm run ci`（GitHub Actions 含む）で実行されないこと、それらの所要時間・成功率に影響しないことを保証する。
- **Node バージョン**: memleak 試験の実行は **Node 24 以上を下限**とし、Node 24 で `--expose-gc` の vitest worker への伝搬を実機検証して確定する。Node 18 等の旧バージョンでの動作保証は行わない（CI 対象外のため、リポジトリ全体の `engines` 変更は本チケットの範囲外）。
- **vitest 並列性**: 専用 config で `fileParallelism: false` 相当にし、シナリオ間でヒープ状態が混ざらないようにする。
- **AGENTS.md 規約**: Arrange ユーティリティの集約、Act / Assert の日本語コメント付与、ルート原因の修正（失敗を黙らせない）、パッケージガイドの Commands 更新に従う。
- **既知のリーク踏み台にしない**: 実装中にリークが検出された場合は、そのサイクル失敗を握りつぶさず、必要な修正（root cause 修正）を別タスクとして切り出し報告する。

## 5. 完了条件

1. `packages/webrtc/tests/memleak/` に上記シナリオ 3 種 × N サイクルの自動試験が実装され、`packages/webrtc/package.json` の npm スクリプトで実行できる。
2. 実行中に定期的なヒープスナップショット（`v8.writeHeapSnapshot`）とメモリメトリクス（`process.memoryUsage` 等）のサンプリングが行われていることをログ/レポートで確認できる。
3. 実行後に `report.json` / `report.csv` / `summary.md` が生成され、シナリオ毎の時系列メトリクス・判定結果・スナップショットパスが確認できる。
4. リーク判定ロジックが機能していることを、正常ケース（pass）と合成リーク入りケース（fail）の両方で手動検証済みである。
5. **時間加速**が実装され、実時間より高速なパケット送信（RTP 連続書き込み / DataChannel 一括送信）で長時間相当を短時間に消化できることを、レポートに記録される「実時間」と「処理量（送信パケット数・サイクル数）」の両方で確認できる。
6. 既定の `npm run test:small` / `npm run type` / `npm run ci` に悪影響がない（memleak が既定スイートから除外され、型チェックが通る）。また GitHub Actions などの CI の対象に含まれていないことを確認する。
7. **Node 24 以上**の環境で実行し、`--expose-gc` が vitest worker に伝搬して正しく計測できることを実機で確認済みである。
8. パッケージガイドの Commands と `.gitignore` が更新され、実行手順がドキュメント化されている。

