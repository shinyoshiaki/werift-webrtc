│ "class RTCDtlsTransport|start\(" in **/dtls.ts (packages)

  │ "fingerprint" in **/*.{ts,js} (packages)

該当箇所は絞れたので、DTLS開始処理・証明書取得経路・既存テストの3点をまとめて確認します。

  │ packages/webrtc/src/transport/dtls.ts

  │ packages/webrtc/src/transport/dtls.ts

  │ packages/webrtc/src/utils.ts

  │ packages/webrtc/tests/integrate/getStats.test.ts

  │ packages/webrtc/tests/issue/141.test.ts

  │ packages/webrtc/tests/issue/142.test.ts

  │ packages/webrtc/src/sdp.ts

  │ "remoteCertificate|peer certificate|getPeer|certificate" in **/*.{ts,js}
  │ (packages)

DTLS実装側に相手証明書の生バイトは保持されていそうなので、WebRTC層から検証可能かをもう一段だけ見ます。

  │ packages/dtls/src/context/cipher.ts

  │ packages/dtls/src/socket.ts

  │ packages/dtls/src/flight/client/flight5.ts

  │ packages/dtls/src/client.ts

  │ packages/dtls/src/server.ts

  │ "onConnect|emitConnect|connected|cipher\.remoteCertificate|remoteCertificate
  │ " in **/*.ts (packages/dtls/src)

残りは「どこで remoteParameters が入るか」と「失敗時に接続全体へどう伝播するか」です。そこが実装タスクの粒度に直結します。

  │ "setRemoteParams\(|\.start\(\)" in **/*.ts (packages/webrtc/src)

  │ "setRemoteDescription\(|createOffer\(|createAnswer\(|ondatachannel|ontrack|c
  │ lose\(" in **/*.{test.ts,ts} (packages/webrtc/tests)

  │ "dtls.*failed|setState\("failed"\)|throw new Error\("remote fingerprint not
  │ exist"\)|onError" in **/*.ts (packages/webrtc/src)

  │ packages/webrtc/src/secureTransportManager.ts

  │ packages/webrtc/src/secureTransportManager.ts

  │ packages/webrtc/src/peerConnection.ts

  │ packages/webrtc/tests/utils.ts

  │ "getFingerprints\(|RTCDtlsFingerprint\(|remoteParameters\.fingerprints|base6
  │ 4Certificate" in **/*.ts (packages/webrtc/src)

  │ "iceconnectionstatechange|connectionstatechange|dtlsTransport\.state|failed"
  │ in **/*.test.ts (packages/webrtc/tests)

テスト観点を正確に書くため、関連パッケージの既存テスト入口だけ確認します。必要なら最小限の既存テストも見ます。

  │ packages/webrtc/package.json

  │ "RTCDtlsTransport|dtls" in **/*.test.ts (packages/webrtc/tests)

  │ "describe\(|test\(" in **/*transport*.test.ts (packages/webrtc/tests)

  │ packages/webrtc/tests/transport/dtls.test.ts

  │ packages/webrtc/tests/integrate/peerConnection.test.ts

  │ packages/webrtc/tests/fixture.ts

実装時の落とし穴が見えたので、アルゴリズム名の扱いと既存ユーティリティ再利用の有無を確認します。

  │ "new RTCDtlsParameters\(|dtlsFingerprints|fingerprints\.push" in **/*.ts
  │ (packages/webrtc/src)

  │ "sha-256|sha256|createHash\(|fingerprint\(" in **/*.ts (packages/webrtc/src,
  │ packages/dtls/src)

  │ "Promise\.allSettled|setConnectionState\("failed"\)|connectionStateChange.ex
  │ ecute" in **/*.ts (packages/webrtc/src)

  │ packages/webrtc/src/peerConnection.ts

  │ packages/webrtc/src/peerConnection.ts

関連テストが既にどこまで守れているかだけ軽く確認します。ここで負のケースが欠けていれば、そのままタスク化の根拠になります。

● Run relevant webrtc tests (shell)
  │ cd /home/shin/code/werift.worktree/42578493-79cc-4fd6-b6ef-75747f800783/pack
  │ ages/webrtc && npm test -- tests/transport/dtls.test.ts
  │ tests/issue/141.test.ts tests/issue/142.test.ts

既存の負ケーステストの書き方も合わせるため、接続失敗をどう待つかだけ探します。

  │ "connectionStateChange|iceConnectionStateChange|onconnectionstatechange|toBe
  │ \("failed"\)|toBe\('failed'\)|rejects" in **/*.test.ts
  │ (packages/webrtc/tests)

  │ "asPromise\(|once\(|subscribe\(\(.*failed" in **/*.test.ts
  │ (packages/webrtc/tests)

  │ packages/webrtc/tests/integrate/peerConnection.test.ts

  │ packages/webrtc/tests/integrate/mediaChannel.test.ts

片方向しか検証できない実装になっていないか確認します。ここがタスクのスコープに影響します。

  │ packages/dtls/src/flight/client/flight5.ts

  │ packages/dtls/src/flight/server/flight6.ts

  │ "certificateRequest|sendCertificate\(|new Certificate\(|certificate_11" in
  │ **/*.ts (packages/dtls/src/flight)

コード面の調査は十分できたので、仕様の期待値だけ最小限確認します。修正タスクの境界を切る材料にします。

● Web Search (MCP: github-mcp-server) · In DTLS-SRTP for WebRTC/SDP, what does the specification re…

✗ Memory stored testing practices
    ./te…

# タスク詳細化: DTLS peer certificate と SDP `a=fingerprint` の照合欠落を修正する

## 1. タスクの目的と背景

本タスクの目的は、**DTLS ハンドシェイクで受信した相手証明書のハッシュが、シグナリングで受け取った SDP の `a=fingerprint` と一致することを必ず検証し、不一致なら接続を失敗させる**ようにすることです。

現状のコード調査では、以下の状態です。

- `packages/webrtc/src/sdp.ts` で SDP の `a=fingerprint` は正しくパースされ、`RTCDtlsParameters.fingerprints` に保持されている
- `packages/webrtc/src/peerConnection.ts` で、その `dtlsParams` は `RTCDtlsTransport.setRemoteParams()` に渡されている
- しかし `packages/webrtc/src/transport/dtls.ts` の `RTCDtlsTransport.start()` は、**fingerprint が存在するか**しか見ておらず、**実際にハンドシェイクで受信した相手証明書との照合をしていない**
- そのまま `connected` 状態に遷移し、SRTP/SCTP 利用に進むため、**MITM によるなりすましと、その後の SRTP 鍵悪用リスク**につながる

仕様上も、DTLS-SRTP では **受信した証明書の fingerprint と SDP の fingerprint を比較し、一致しなければ接続を終了する**ことが期待されます。したがって、これは単なる改善ではなく**認証バインディング不足によるセキュリティ修正**です。

---

## 2. 実装すべき具体的な機能や変更内容

1. **相手証明書 fingerprint の照合処理を追加する**
   - DTLS ハンドシェイク完了後、相手証明書の DER バイト列から fingerprint を計算する
   - SDP から受け取った `RTCDtlsParameters.fingerprints` と比較する
   - **1件でも一致しなければ失敗**とする

2. **照合失敗時の失敗処理を明確化する**
   - `RTCDtlsTransport` を `failed` にする
   - `connected` に遷移させない
   - `startSrtp()` / `updateSrtpSession()` / SCTP 接続開始を行わせない
   - `RTCPeerConnection.connectionState` が最終的に `failed` になるように既存の失敗伝播に乗せる

3. **複数 fingerprint・アルゴリズム差異に対応する**
   - `fingerprints[0]` 固定ではなく、**配列内のいずれかに一致すれば成功**
   - SDP 表記 (`sha-256`) と Node.js の `createHash` 名 (`sha256`) の差を吸収する
   - 未対応アルゴリズムは黙殺せず、**明示的に失敗**させる

4. **完全修正に必要なら DTLS パッケージ側も修正する**
   - 現状 `packages/dtls/src/flight/client/flight5.ts` では相手証明書を `cipher.remoteCertificate` に保存しているため、**DTLS client 側は照合に必要な情報を持てる**
   - 一方で `packages/dtls/src/flight/server/flight6.ts` は `certificate_11` / `certificate_verify_15` が未処理で、`RTCDtlsTransport.start()` でも `DtlsServer` に `certificateRequest: true` を渡していない
   - **双方向に peer certificate を検証する完全修正**を目指すなら、DTLS server 側でも相手証明書を取得・保持できるようにする必要がある

5. **回帰テスト・再現テストを追加する**
   - transport レベル: 改ざん fingerprint を与えたとき `RTCDtlsTransport.start()` が失敗する
   - PeerConnection レベル: SDP の fingerprint を改ざんした offer/answer を設定すると接続が `failed` になり、DataChannel/メディアが開かない
   - 正常系: 既存の DTLS/DataChannel/SDP 系テストは維持する

---

## 3. 技術的な実装アプローチを調査し結果を簡潔にまとめる

### 調査結果
- fingerprint の入力元はすでにある  
  - `packages/webrtc/src/sdp.ts`
- fingerprint の受け渡し経路もある  
  - `packages/webrtc/src/peerConnection.ts` → `RTCDtlsTransport.setRemoteParams()`
- 欠けているのは**ハンドシェイク後の照合処理**
  - `packages/webrtc/src/transport/dtls.ts`
- DTLS client 側では相手証明書の生データを保持している
  - `packages/dtls/src/flight/client/flight5.ts`
- ただし DTLS server 側は相手証明書取得が未完成
  - `packages/dtls/src/flight/server/flight6.ts`

### 推奨アプローチ
- `packages/webrtc/src/transport/dtls.ts` に、たとえば `verifyRemoteCertificateFingerprint()` のような内部ヘルパーを追加する
- 既存の `packages/webrtc/src/utils.ts` の `fingerprint()` を再利用しつつ、**SDP アルゴリズム名 → Node ハッシュ名**の変換を追加する
- 検証タイミングは、**DTLS 接続確立直後かつ SRTP/SCTP 利用開始前**
- ただし**本来の完了ラインは双方向検証**なので、必要なら `packages/dtls` 側にも修正を入れる

### テスト観点の補足
- 既存の `packages/webrtc/tests/transport/dtls.test.ts` や `packages/webrtc/tests/integrate/peerConnection.test.ts` は正常系中心
- `packages/webrtc/tests/issue/141.test.ts` / `142.test.ts` は SDP 互換系で、**fingerprint 不一致の拒否は未検証**
- 現在の既存テスト群は通るが、**今回の脆弱性を直接防ぐ負ケースが不足**している

---

## 4. 考慮すべき制約や注意点

- **BUNDLE 対応**
  - 同一 `RTCDtlsTransport` が複数 media で共有されるため、media ごとに上書きされる `remoteParameters` の扱いに注意する
  - `fingerprints` は session-level と media-level が混在・重複し得るので、**先頭1件前提にしない**

- **アルゴリズム名の正規化**
  - `sha-256` / `sha256` の差異を吸収する
  - 大文字小文字・コロン区切り表記も比較前に正規化する

- **失敗を握り潰さない**
  - 不一致・未対応アルゴリズム・相手証明書未取得は、曖昧に通さず明示的に失敗にする

- **部分修正で終わらせない**
  - `RTCDtlsTransport.start()` だけ直しても、**DTLS server 側が相手証明書を取得できないままなら片方向検証止まり**になる
  - セキュリティ修正としては、**どの DTLS role でも peer certificate を検証できること**をゴールに置くべき

- **関連しない改善は混ぜない**
  - たとえば remote certificate stats の拡張は今回の本筋ではないため、必要最小限に留める

---

## 5. 完了条件

以下をすべて満たしたら完了です。

1. **相手証明書 fingerprint と SDP `a=fingerprint` の照合が実装されている**
2. **不一致時に DTLS/PeerConnection が失敗し、接続成功扱いにならない**
3. **不一致時に SRTP/SCTP/DataChannel/メディア送受信へ進まない**
4. **複数 fingerprint とアルゴリズム正規化を考慮している**
5. **正常系の既存接続は維持される**
6. **fingerprint 改ざん時の負ケーステストが追加されている**
7. **可能なら DTLS client / server の両 role で peer certificate 検証が成立している**
   - もし段階対応にするなら、少なくとも「現時点でどちらの role まで保護できるか」をコードとテストで明確にすること

以上を踏まえると、このタスクは単なる `dtls.ts` の条件追加ではなく、**WebRTC 層と DTLS 層をまたぐ認証バインディング修正タスク**として切るのが適切です。