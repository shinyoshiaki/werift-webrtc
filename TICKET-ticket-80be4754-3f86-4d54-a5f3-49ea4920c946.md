# polyfill機能を追加する

ブラウザ向け WebRTC ライブラリが Node.js 上でドロップイン動作するよう、インストーラー関数で WebRTC 関連グローバルを werift 実装へ差し替える。`mediaRegister` を必須引数とし、ビルトイン / ユーザ定義のメディアソースを `getUserMedia` から使えるようにする。

## 1. タスクの目的と背景

werift は Node.js 向けの TypeScript WebRTC 実装で、公開 API は `RTCPeerConnection` / `MediaStreamTrack` / `RTCDataChannel` などブラウザと同名のクラスを持つ（`packages/webrtc/src`）。一方で次のギャップがあり、ブラウザ向けライブラリをそのまま Node で動かない。

- グローバルにコンストラクタが載っていない。利用側は必ず `import { RTCPeerConnection } from "werift"` が必要。多くのブラウザ向けライブラリは `new RTCPeerConnection()` / `navigator.mediaDevices.getUserMedia()` / `window.RTCPeerConnection` を前提にする。
- 既存のグローバル注入は WPT ランナー専用（`packages/webrtc/tools/wpt-runner/runner.ts` の `vm` sandbox）。公開インストーラーと実装が分かれており、コンストラクタ集合や `getUserMedia` が二重管理になっている。
- `getUserMedia` が二系統に分裂している。
  - `werift/nonstandard` の `getUserMedia({ path | buffer | stream })` は MP4/WebM 再生用の非標準 API。`MediaStream` ではなく `audio` / `video` トラックを持つプレイヤーを返す。
  - `Navigator.mediaDevices.getUserMedia(constraints)` はブラウザ風シグネチャだが、事前設定トラックか dummy しか使えず、register 機構がない。例外も素の `Error` で、W3C の `TypeError` / `NotFoundError` / `OverconstrainedError` と一致しない。
  - 本チケットではこの二系統を **polyfill の `navigator.mediaDevices.getUserMedia` に一本化**し、非標準 `getUserMedia({ path })` は **廃止する（破壊的変更）**。
- TypeScript 設定は `"lib": ["esnext"]` のみ（DOM なし）。polyfill 導入後も werift の拡張 API（`track.writeRtp` など）へ型安全に到達できる必要がある。
- README / サイトドキュメントは「getUserMedia は使えない」と明記している。本機能追加後は polyfill 経路を追記する。

目的は、ブラウザ向け WebRTC 利用ライブラリを **import してインストーラーを一度実行するだけ** で動かすこと。カメラ実デバイスのエミュレーションではなく、登録したメディアソース（ファイル / RTP / エンコード済みバイナリ / ユーザ定義）を `getUserMedia` の供給源にする。

## 2. 実装すべき具体的な機能や変更内容

### 2.1 公開エントリ `werift/polyfill`

`packages/webrtc/package.json` の `exports` に `./polyfill` を追加する（既存の `./nonstandard` と同じパターン）。

- types: `./lib/webrtc/src/polyfill/index.d.ts`
- require: `./lib/webrtc/src/polyfill/index.js`
- import: `./lib/polyfill/index.mjs`

`compile:esm` の esbuild エントリに `src/polyfill/index.ts` を追加する。コア `werift` 本体はグローバルを変更しない（opt-in）。

### 2.2 インストーラー関数

```ts
import {
  installPolyfill,
  createMp4WebmRegister,
  createRtpRtcpRegister,
  createEncodedBinaryRegister,
  createCallbackRegister,
  type MediaRegister,
} from "werift/polyfill";

const uninstall = installPolyfill({
  mediaRegister: [
    createMp4WebmRegister({
      path: "./clip.mp4",
      deviceId: "clip", // 任意。省略時はインストーラーが一意な値を採番
    }),
    createRtpRtcpRegister({
      mimeType: "video/VP8",
      udp: { port: 5004 },
      deviceId: "rtp-vp8",
    }),
    createEncodedBinaryRegister({
      mimeType: "audio/opus",
      stream: encodedOpusStream,
    }),
    createCallbackRegister({
      mimeType: "video/custom",
      kinds: ["video"],
      deviceId: "synthetic",
      createTracks: async ({ constraints }) => {
        // width / height / frameRate / facingMode 等はここに入る
        return [await generateVideoTrack(constraints)];
      },
    }),
  ],
  existingMediaDevices: "overwrite", // 省略時の既定値
  // target: sandbox, // 省略時は globalThis。WPT の vm sandbox に同じ面を載せるときに渡す
});
```

必須契約:

- 引数オブジェクトは必須。`mediaRegister` は **必須プロパティ**（省略不可）。
- `mediaRegister` は配列。 **1 件以上、または空配列**。`undefined` は型・実行時ともに拒否する。
- 空配列は合法。PeerConnection / DataChannel は使えるが、`getUserMedia({ video: true })` はデバイスなしとして `NotFoundError` になる。
- 戻り値はアンインストール関数。再インストール前に外せるようにする（テストとライブラリ共存のため）。

`existingMediaDevices`（任意、既定 `"overwrite"`）:

| 値 | 既存 `navigator.mediaDevices.getUserMedia` があるとき |
| --- | --- |
| `"overwrite"`（既定） | werift の `MediaDevices` で上書きする |
| `"throw"` | インストールを中断し `Error` を投げる |
| `"noop"` | `navigator.mediaDevices` だけ触らず、他の WebRTC グローバルは通常どおり入れる |

このオプションは **`navigator.mediaDevices` に限る**。`RTCPeerConnection` 等はドロップインのため常に差し替える。判定ヘルパは `packages/webrtc/src/polyfill/existingMediaDevices.ts` の `shouldInstallMediaDevices`。

`target`（任意、既定 `globalThis`）:

- インストール先のオブジェクト。WPT ランナーの `vm` sandbox など、`globalThis` 以外へ同じ面を載せるときに渡す。
- WPT も公開 polyfill と同じインストーラーを使う（下記 2.9）。sandbox 専用のコンストラクタ列挙を別実装のまま残さない。

インストール時に `target`（既定は `globalThis`）へ載せる最低限の WebRTC 面:

| グローバル | 実装の出どころ |
| --- | --- |
| `RTCPeerConnection` | `packages/webrtc/src/peerConnection.ts` |
| `RTCSessionDescription` | ブラウザ互換ラッパ（下記 2.6） |
| `RTCIceCandidate` | `packages/webrtc/src/transport/ice.ts` |
| `RTCDataChannel` | `packages/webrtc/src/dataChannel.ts` |
| `MediaStream` / `MediaStreamTrack` | `packages/webrtc/src/media/track.ts` |
| `RTCRtpSender` / `RTCRtpReceiver` / `RTCRtpTransceiver` | `packages/webrtc/src/media` |
| `RTCIceTransport` / `RTCDtlsTransport` | transport 実装 |
| `RTCTrackEvent` | peerConnection 既存 |
| `OverconstrainedError` | `packages/webrtc/src/errors.ts` |
| `navigator.mediaDevices` | polyfill 用 `MediaDevices`（上記ポリシーに従う） |
| `window` | `globalThis.window` が無いときだけ `globalThis` を alias。既にあれば上書きしない |

Node 21+ の既存 `navigator` オブジェクトは置換せず、`mediaDevices` を定義する。

### 2.3 `MediaRegister` 契約と getUserMedia の制約選択（確定）

ビルトインもユーザ定義も同じインタフェースにする。各 register はブラウザの **1 デバイス** に相当する。`mimeType` はソース形式の識別子であり、同一 `mimeType` の複数 register は別 `deviceId` のデバイスとして共存する。

`deviceId` は **すべての register（ビルトインファクトリ / ユーザ定義）で任意指定**。省略時はインストーラーが一意な値を採番する。指定した場合は `enumerateDevices()` と `getUserMedia({ video: { deviceId: { exact } } })` のキーになる。重複した `deviceId` を配列へ渡した場合はインストール時に拒否する（デバイス列挙が一意でなくなるため）。

```ts
type MediaKind = "audio" | "video";

interface MediaGetUserMediaRequest {
  kind: MediaKind;
  /** 指定値、またはインストーラーが採番した値 */
  deviceId: string;
  /**
   * 当該 kind の MediaTrackConstraints。
   * `video: true` / `audio: true` は `{}` に正規化する。
   * width / height / frameRate / facingMode 等も含め、
   * SelectSettings で未使用でも生成用にそのまま渡す。
   */
  constraints: MediaTrackConstraints;
}

interface MediaRegister {
  /** ソース形式。例: "video/mp4", "video/VP8", "audio/opus" */
  readonly mimeType: string;
  readonly kinds: readonly MediaKind[];
  /** 任意。省略時はインストーラーが一意な deviceId を付与する */
  readonly deviceId?: string;
  readonly groupId?: string;
  readonly label?: string;
  /**
   * この register が getUserMedia で選ばれたあとに呼ばれる生成コールバック。
   * 制約に則ったトラックをここで作る（特にユーザ定義 register）。
   */
  createTracks(request: MediaGetUserMediaRequest): Promise<MediaStreamTrack[]>;
  stop?(): void;
}
```

ユーザ定義はオブジェクトを直接渡してもよいし、コールバックを束ねるヘルパを用意する。

```ts
createCallbackRegister({
  mimeType: "video/custom",
  kinds: ["video"],
  deviceId: "synthetic", // 任意
  createTracks: async ({ kind, deviceId, constraints }) => {
    // constraints.width / height / frameRate / facingMode 等を見て生成する
    return [await generateTrack(kind, deviceId, constraints)];
  },
});
```

コールバックが必須の `exact` / `min` / `max` を満たせないときは `OverconstrainedError` を投げてよい。polyfill はそれを getUserMedia の失敗としてそのまま返す。

#### 例外マッピング（W3C Media Capture and Streams + WPT）

根拠: [Media Capture and Streams](https://www.w3.org/TR/mediacapture-streams/) の `getUserMedia` アルゴリズム、および `third_party/wpt/mediacapture-streams/`。

| 条件 | 例外 | WPT / 仕様 |
| --- | --- | --- |
| `audio` も `video` も要求しない（`{}` や未知キーのみ） | **`TypeError`**（DOMException ではない） | `GUM-empty-option-param.https.html` |
| 要求 kind の候補デバイスが 1 件も無い | **`DOMException` `NotFoundError`** | 仕様 NotFound Failure。空の `mediaRegister` で `{ video: true }` もここ |
| その kind のデバイスはあるが、必須制約（`exact` / `min` / `max`）を誰も満たせない | **`OverconstrainedError`**（`DOMException` サブクラス、`.constraint` に失敗した制約名） | `GUM-impossible-constraint.https.html`, `overconstrainederror.html` |
| 権限拒否 | **`NotAllowedError`** | `GUM-deny.https.html`。Node polyfill では権限プロンプトが無いので **出さない** |
| デバイス確保後の OS ロック等 | `NotReadableError` | 仕様。UDP bind 失敗などは実装時にここに寄せてよい |
| その他の取得失敗 | `AbortError` | 仕様 |

`NotFoundError` と `OverconstrainedError` を混同しない。前者は「その kind のソースが無い」、後者は「ソースはあるが必須制約を満たせない」。

#### デバイス選択に使う制約と fitness

`getSupportedConstraints()` が `true` を返すものだけを **デバイス選択（SelectSettings）** に載せる。生成用の width 等はここに載せない（載せると未対応のビルトインまで `exact` で OverconstrainedError になる）。

| 制約 | デバイス選択 | 生成コールバック (`createTracks`) |
| --- | --- | --- |
| `deviceId` | 使う。必須なら一致しない候補を除外。ideal なら文字列 fitness（一致 0 / 不一致 1） | `request.deviceId` として渡す |
| `groupId` | 使う。同上 | `constraints` に含めて渡す |
| `mimeType` | **werift 拡張**。register の形式選択用。`getSupportedConstraints().mimeType === true`。必須不一致は `OverconstrainedError({ constraint: "mimeType" })` | `constraints` に含めて渡す |
| `width` / `height` / `frameRate` / `facingMode` 等 | **選択には使わない**（fitness 0。TypeError / OverconstrainedError にはしない） | **必ず渡す**。ユーザ定義 register はここに則ってメディアを生成する |

`advanced` 配列はデバイス選択では仕様どおり **optional**（満たせなければその set を無視。`GUM-optional-constraint.https.html`）。生成コールバックへは `constraints.advanced` も含めて渡す。

bare 値（`{ deviceId: "abc" }`）は basic set では **ideal** として扱う（SelectSettings step 3）。

同一 fitness のときは **`mediaRegister` 配列の先勝ち**。これが「同じ mimeType を重複登録したらブラウザ同様に一意な 1 件が選ばれる」の定義。明示した `deviceId` または自動採番した `deviceId.exact` でもう一方を選べる。`enumerateDevices` では両方を別デバイスとして返す。

選択のあと、選ばれた register の `createTracks({ kind, deviceId, constraints })` を呼ぶ。`constraints` は当該 kind のオブジェクトを正規化したもの（`true` → `{}`）で、width / height / frameRate / facingMode 等を落とさない。

選択アルゴリズムの実装済み核: `packages/webrtc/src/polyfill/selectSettings.ts`（`selectRegisterForKind`）。テスト: `packages/webrtc/tests/nonstandard/polyfillSelectSettings.test.ts`。生成コールバックへ渡す制約バッグはインストーラー側で組み立てる（核の更新対象）。

### 2.4 ビルトイン register

ファクトリを `werift/polyfill` から export する。ソース指定はファクトリ引数（インストール時に束縛）。ブラウザライブラリは通常 `getUserMedia({ video: true })` しか呼ばないため、ドロップインにはインストール時束縛が必須。

共通オプション（いずれも任意）:

- `deviceId?: string` — 省略時はインストーラーが採番
- `groupId?: string`
- `label?: string`

#### MP4 / WebM（path / binary / web stream）

既存 `packages/webrtc/src/nonstandard/userMedia.ts` の再生実装を **内部実装として** 再利用する。公開 API の `getUserMedia({ path })` は廃止する（2.10）。

- 入力: `{ path: string } | { binary: Buffer | ArrayBuffer | ArrayBufferView } | { stream: Readable | ReadableStream<Uint8Array> }` の排他、加えて任意 `loop?: boolean`、共通の `deviceId` 等
- mimeType: コンテナから決定（`video/mp4` / `video/webm`）。音声のみなら `audio/mp4` / `audio/webm`
- 再生開始: トラックが PeerConnection に載るのを待たず、`getUserMedia` 後に再生を開始する（既存プレイヤーの `start()` を register 内で呼ぶ）
- `track.stop()` で再生と入力を止める

#### RTP / RTCP（UDP / web stream）

既存 `Navigator.getUdpMedia` と `MediaStreamTrackFactory.rtpSource`（`packages/webrtc/src/utils.ts`）を一本化する。

- 入力: `{ udp: { port: number; address?: string } } | { stream: Readable | ReadableStream<Uint8Array> }`、共通の `deviceId` 等
- mimeType: 必須（例 `video/VP8`, `audio/opus`）。codec clockRate 等は register オプションで渡す
- ペイロードは RTP パケットバイナリ。RTCP は RTP/RTCP mux（RFC 5761、PT 64–95 を RTCP）を既定とする
- Web Stream は長さプレフィックス付き（4 byte big-endian length + packet）を既定とする。UDP は 1 datagram = 1 RTP/RTCP パケット
- 受信 RTP は `MediaStreamTrack.writeRtp` へ。RTCP は対応する sender/track の RTCP 経路があれば接続し、無ければ読み捨ててもドロップインの送信経路は成立させる

#### Encoded binary（UDP / web stream）

既存 `packages/webrtc/src/nonstandard/userMedia/packetizer.ts` の `createPacketizer` を再利用する。

- 入力: RTP と同じ UDP / web stream。加えて `mimeType`（`video/H264` | `video/VP8` | `video/VP9` | `video/AV1` | `audio/opus`）、共通の `deviceId` 等
- 1 アクセスユニットを packetizer に渡し、出力 RTP を `writeRtp`
- UDP: 1 datagram = 1 encoded AU
- Web Stream: 4 byte BE length + AU bytes
- タイムスタンプは受信間隔と codec clockRate から生成する（コンテナ時刻が無いため）

#### ユーザ定義 register

上記 `MediaRegister` を実装したオブジェクト、または `createCallbackRegister` の戻り値を配列に混ぜられる。ビルトインと同じ選択経路を通ったあと、**選ばれた時点で** `createTracks` が呼ばれる。

ここで渡る `constraints` に `width` / `height` / `frameRate` / `facingMode` 等が入る。ユーザ定義側はこれを見て、要求に則ったメディアを生成する。ビルトイン（ファイル / RTP / encoded）はこれらの適用を必須としない（リサイズやカメラ向きを持たないため読み捨ててよい）。

dummy メディア（`dummyMedia.ts`）は製品の既定 register には入れない。WPT は 2.9 のとおり、同じ `createCallbackRegister`（または薄い `createDummyRegister` ヘルパ）で dummy ソースを `mediaRegister` に渡す。`Navigator({ dummyMedia })` によるランナー専用の getUserMedia 経路は廃止する。

### 2.5 `navigator.mediaDevices`

polyfill 用 `MediaDevices` を実装（既存 `packages/webrtc/src/nonstandard/navigator.ts` を拡張するか、polyfill 配下に専用実装を置く）。

必須:

- `getUserMedia(constraints)` → 2.3 で register を選び、その `createTracks` コールバックに正規化済み制約を渡して `Promise<MediaStream>`
- `enumerateDevices()` → 各 register を `MediaDeviceInfo` 相当（`deviceId`, `kind`, `label`, `groupId`）で返す。同一 mimeType でも別エントリ
- `getSupportedConstraints()` → `{ deviceId: true, groupId: true, mimeType: true }`（他は未サポートなので載せない）
- `addEventListener` / `ondevicechange` はスタブ可

ファイル再生は `createMp4WebmRegister` + `navigator.mediaDevices.getUserMedia` のみ。非標準 `getUserMedia({ path })` は公開しない。

### 2.6 ブラウザ互換コンストラクタと werift API 統合

ドロップイン阻害点（調査済み）:

- `RTCSessionDescription` の公開コンストラクタは `(sdp: string, type)`。ブラウザ / WPT は `new RTCSessionDescription({ type, sdp })`。polyfill がブラウザ互換コンストラクタを提供し、**WPT ランナーも同じ実装を使う**（ランナー内の独自 `createRtcSessionDescriptionClass` はこれに置き換える）。`import { RTCSessionDescription } from "werift"` の既存クラスはコア互換のため残してよい。
- `RTCIceCandidate` は `Partial<RTCIceCandidate>` を受けており、ブラウザの `RTCIceCandidateInit` と概ね互換。グローバルにはこれを載せる。
- `MediaStreamTrack` にブラウザ面が不足: `readyState`（`"live" | "ended"`）、`enabled`、`muted`、`stop()` 後の `ended`。**既存クラスに getter を足す**（ラッパを挟むと `instanceof` と `writeRtp` が壊れる）。
- `MediaStream` に `getTrackById` / `active` / `clone` が無い。最低限 `id`, `getTracks`, `getAudioTracks`, `getVideoTracks`, `addTrack`, `removeTrack` に加え、`getTrackById` と `active` を足す。
- EventTarget は Node `EventEmitter` ベースの独自実装。本チケットでは既存 EventTarget を維持し、グローバル差し替えを優先する。
- `OverconstrainedError` は WPT が `instanceof DOMException` と `.constraint` を要求する。Node では `DOMException` サブクラス化時に prototype が外れるため、コンストラクタで `Object.setPrototypeOf(this, new.target.prototype)` する（`packages/webrtc/src/errors.ts` に実装済み）。

werift API 統合:

- グローバルの `RTCPeerConnection` は werift のクラスそのもの。`import { RTCPeerConnection } from "werift"` と `instanceof` が一致する。
- `getUserMedia` が返す `MediaStreamTrack` も werift のクラス。`track.writeRtp` / `onReceiveRtp` が使える。
- TypeScript: `werift/polyfill` の型定義で `declare global` し、グローバルコンストラクタを werift クラス型にする。
- `lib: ["DOM"]` を含む TS プロジェクトでは DOM の `RTCPeerConnection` と衝突し得る。対象は Node 向けドロップインなので、ドキュメントで「Node 側 tsconfig は DOM WebRTC 型を使わない / `werift/polyfill` を types に含める」と書く。

### 2.7 ドキュメント / AGENTS / import-test

- `README.md` と `website/docs/doc1.md` / `website/i18n/ja/.../doc1.md` の「getUserMedia は使えない」を、polyfill 経由の `navigator.mediaDevices.getUserMedia` に更新する。非標準 `getUserMedia({ path })` の案内は削除し、破壊的変更として記す。CHANGELOG にも同じ破壊的変更を書く。
- `packages/webrtc/package.json` の scripts / exports 変更に合わせ `packages/webrtc/AGENTS.md` を更新する。
- `import-test` に `werift-dev/polyfill` の import スモークを追加する。
- `examples/` および `packages/webrtc/tests/nonstandard/userMedia*.ts` の `getUserMedia({ path | buffer | stream })` 呼び出しを、polyfill インストール + ブラウザ互換 `getUserMedia` に移行する。

### 2.8 テスト（Arrange / Act / Assert、Act/Assert に日本語コメント）

配置: `packages/webrtc/tests/nonstandard/`。Arrange ヘルパは共有ファイルに集約（制約選択は `polyfillSelectSettingsTestUtils.ts` 済み）。

最低限のケース:

- `mediaRegister` 省略・非配列は型または実行時エラー
- 空配列: `getUserMedia({ video: true })` が `NotFoundError`、`getUserMedia({})` が `TypeError`、`new RTCPeerConnection()` と DataChannel は動作
- ビルトイン 3 種それぞれで `getUserMedia` → トラックが RTP を出す（ファイル再生テストは廃止した `getUserMedia({ path })` ではなく polyfill + register 経由）
- 公開 `werift/nonstandard` から `getUserMedia({ path })` が消えている（import / 型）
- ビルトイン / ユーザ定義ともに任意 `deviceId` を渡せる。省略時は自動採番。重複 `deviceId` はインストール時エラー
- ユーザ定義 register が同じ経路で使われる
- ユーザ定義の `createTracks` に `width` / `height` / `frameRate` / `facingMode` 等が渡る。`video: true` のときは `{}`
- コールバックが必須制約を満たせないと宣言して投げた `OverconstrainedError` が getUserMedia から見える
- 同一 mimeType を 2 件登録: `{ video: true }` は先登録 1 件。指定 / 採番した `deviceId.exact` でもう一方を選べる
- 必須 `mimeType` 不一致は `OverconstrainedError`（`.constraint === "mimeType"`）
- ビルトインに対する `width.exact` は選択を失敗させない（生成コールバックへは渡す）
- `existingMediaDevices: "overwrite" | "throw" | "noop"` の 3 経路
- インストール後 `globalThis.RTCPeerConnection` が werift クラスと instanceof 一致
- `new RTCSessionDescription({ type: "offer", sdp: "..." })` が通る
- アンインストール後にグローバルが復元される
- `writeRtp` が返却トラックで型付けされること
- WPT ランナーが `installPolyfill({ target: sandbox, mediaRegister })` を使い、独自のコンストラクタ列挙と `Navigator({ dummyMedia })` 注入を持たない
- 許可リスト WPT が従来どおり実行できる（strict shim は runner の PeerConnection ラッパに限定）

### 2.9 WPT ランナーとの統一

`packages/webrtc/tools/wpt-runner/runner.ts` の `createContext` は、sandbox へ WebRTC グローバルを手書きで載せ、`Navigator({ dummyMedia })` で getUserMedia している。これを **公開 `installPolyfill` に寄せる**。

やること:

- `installPolyfill({ target: sandbox, mediaRegister, existingMediaDevices: "overwrite" })` で sandbox に同じ面を載せる。`window` / `self` は既存どおり sandbox 自身を指す（インストーラーは既に `window` がある target を上書きしない）。
- インストール後、runner は既存の `createPeerConnectionWrapper` で `sandbox.RTCPeerConnection` を差し替えてよい（peer 追跡と strict `addIceCandidate`）。これは polyfill 本体には入れない。
- `mediaRegister` には WPT 用 dummy ソースを `createCallbackRegister`（または `createDummyRegister`）で渡す。audio/video の決定論的 RTP は既存 `dummyMedia.ts` をコールバック内で再利用してよい。
- `RTCSessionDescription` は polyfill のブラウザ互換実装を使う。runner 内の `createRtcSessionDescriptionClass` は削除する。
- `OverconstrainedError` / `MediaStream` / `MediaStreamTrack` も polyfill と同じオブジェクトにする。
- **残してよい差**: allowlist WPT のための `RTCPeerConnection` ラッパ（peer 集合の追跡、strict `addIceCandidate` など）。これは `tools/wpt-runner` に閉じ、`packages/webrtc/src` の既定 API や polyfill 既定には漏らさない（既存 AGENTS.md の方針）。
- `setMediaPermission` や `test_driver` などハーネス専用の sandbox プロパティは runner 側に残す。
- 検証: `npm run wpt`（workspace `packages/webrtc`）が、注入経路変更後も許可リストを実行できる。

### 2.10 非標準 `getUserMedia({ path })` の廃止（破壊的変更）

`werift/nonstandard` の `getUserMedia({ path | buffer | stream })` は **公開 API から削除する**。ファイル再生は次だけを正規経路とする。

```ts
installPolyfill({
  mediaRegister: [createMp4WebmRegister({ path: "./clip.mp4" })],
});
const stream = await navigator.mediaDevices.getUserMedia({ audio: true, video: true });
```

やること:

- `packages/webrtc/src/nonstandard/index.ts` から `getUserMedia` を export しない。`userMedia.ts` の再生ロジックは mp4/webm register の内部実装へ移すか、nonstandard 非公開モジュールとして残す。
- `Navigator.mediaDevices.getUserMedia` の事前設定トラック / dummy 専用経路は、polyfill の register 選択に置き換える。WPT dummy も register 経由（2.9）。
- `packages/webrtc/tests/nonstandard/userMedia.test.ts` / `userMedia.e2e.test.ts` / `userMediaTestUtils.ts` を polyfill + `getUserMedia(MediaStreamConstraints)` に書き換える。
- `examples/` などリポジトリ内の `getUserMedia({ path })` 呼び出しを移行する。
- README / サイト / CHANGELOG に破壊的変更を書く。移行先は `createMp4WebmRegister`。

## 3. 技術的な実装アプローチ（調査結果サマリ）

- グローバル注入の既存実装は WPT sandbox の手書き列挙（`createContext`）。公開 polyfill は同じインストーラーを `target` 付きで使い、WPT もこれに揃える。
- メディア供給の実装は揃っている。新規プロトコル実装は不要。
  - ファイル: `userMedia.ts` の再生核（公開 `getUserMedia({ path })` は廃止）+ mediabunny + `packetizer.ts`
  - UDP RTP: `getUdpMedia` / `MediaStreamTrackFactory.rtpSource`
  - エンコード済み AU → RTP: `createPacketizer`
- 送信経路は既に `MediaStreamTrack.writeRtp` → `onReceiveRtp` → `RTCRtpSender` 購読。
- `getUserMedia` の制約は二段。デバイス選択は W3C SelectSettings（`deviceId` / `groupId` / 拡張 `mimeType`）。`width` / `height` / `frameRate` / `facingMode` 等は標準の `MediaTrackSupportedConstraints` にあるが、この polyfill のビルトインは能力を持たないので選択には使わず、選ばれた register の生成コールバックへ渡してユーザ定義が適用する。
- ブラウザ互換の最大差分は `RTCSessionDescription` コンストラクタ。polyfill のラッパを WPT と共有する。
- パッケージエントリ追加は `exports` + `compile:esm` の 2 点。CJS は `tsc -p tsconfig.production.json`（`include: src`）で自動出力。

先行実装（本詳細化で入れた核。インストーラー本体は未着手）:

```
packages/webrtc/src/errors.ts                         // OverconstrainedError, NotFoundError 名
packages/webrtc/src/polyfill/selectSettings.ts        // SelectSettings / 例外分岐
packages/webrtc/src/polyfill/existingMediaDevices.ts  // overwrite / throw / noop
packages/webrtc/tests/nonstandard/polyfillSelectSettings.test.ts
packages/webrtc/tests/nonstandard/polyfillSelectSettingsTestUtils.ts
```

推奨モジュール分割（残り）:

```
packages/webrtc/src/polyfill/
  index.ts
  install.ts
  mediaRegister.ts
  registers/mp4Webm.ts
  registers/rtpRtcp.ts
  registers/encodedBinary.ts
  registers/callback.ts
  rtcSessionDescription.ts
```

## 4. 考慮すべき制約や注意点

- 実行環境は Linux / macOS 等 Unix のみ。Windows ネイティブは対象外。
- コア `src/index.ts` から polyfill を re-export しない。副作用付き API を既定 import に混ぜない。
- WPT の **ハーネス専用** プロパティ（`test_driver` 等）と **strict PeerConnection ラッパ** は runner に残す。WebRTC グローバルと getUserMedia の載せ方は polyfill に統一する。
- 非標準 `getUserMedia({ path | buffer | stream })` は破壊的に廃止する。内部再生コードの再利用は可。公開シグネチャは残さない。
- `globalThis.window = globalThis` は Node 判定を壊し得る。未定義のときだけ alias する。`target` が sandbox のときはその `window` 規約に従う。
- `existingMediaDevices` の既定は上書き。誤ってブラウザ本体で読んでも上書きされる点を README に書く。`"throw"` / `"noop"` で回避できる。
- Node に権限プロンプトは無い。`NotAllowedError` を getUserMedia の通常失敗に使わない。
- `width` 等を `getSupportedConstraints` へ載せるのは禁止（載せた瞬間、ビルトインまで満たせない `exact` が OverconstrainedError になる）。値は生成コールバックへ渡し、満たせない判定はユーザ定義 register が `OverconstrainedError` を投げる。
- 任意 `deviceId` の重複はインストール時に失敗させる。自動採番は register 配列内で一意にする。
- ファイル再生の `start()` を呼び忘れると無音になる。register 内で開始・stop を紐付ける。
- 公開 API・docs・exports が変わるので、完了前に `packages/webrtc` の type と関連テスト、必要なら `import-test` を回す。

## 5. 完了条件

- [ ] `import { installPolyfill } from "werift/polyfill"` が CJS / ESM で解決する
- [ ] `installPolyfill({ mediaRegister })` の `mediaRegister` が必須で、空配列または 1 件以上のみ受け付ける
- [ ] `existingMediaDevices` が省略時 overwrite、`"throw"` / `"noop"` を選べる
- [ ] 実行後、`RTCPeerConnection` 等の WebRTC グローバルと `navigator.mediaDevices.getUserMedia` が werift 実装になる（noop 時は mediaDevices を除く）
- [ ] ビルトイン register（mp4/webm の path・binary・web stream、rtp/rtcp の udp・web stream、encoded binary の udp・web stream）とユーザ定義 register を配列で渡せる
- [ ] ビルトインファクトリ / ユーザ定義とも任意 `deviceId` を受け取れ、省略時は一意に採番される
- [ ] `getUserMedia({})` は TypeError。要求 kind の register が無いときは NotFoundError。選択時の必須制約不一致は OverconstrainedError
- [ ] ユーザ定義 `createTracks` に `width` / `height` / `frameRate` / `facingMode` 等を含む正規化済み制約が渡る
- [ ] 同一 mimeType の複数 register は getUserMedia 時に 1 件（先勝ち）。`deviceId` / 拡張 `mimeType` 制約で切り替えられる
- [ ] `getSupportedConstraints()` は `deviceId` / `groupId` / `mimeType` のみ true（width 等は選択用に載せない）
- [ ] グローバル `RTCPeerConnection` / 返却トラックは werift クラスと同一で、`writeRtp` 等の拡張 API が型・実行時ともに使える
- [ ] `new RTCSessionDescription({ type, sdp })` が polyfill グローバルで動く
- [ ] アンインストールでグローバルを復元できる
- [ ] README / 日本語ドキュメント / `packages/webrtc/AGENTS.md` が新しいエントリと getUserMedia 説明を反映している
- [ ] `packages/webrtc` の関連テストと `npm run type`（パッケージ）が通る
- [ ] 新規テストは Arrange / Act / Assert で、Act / Assert に日本語コメントがある。Arrange ヘルパは共有ファイルにまとまっている
- [ ] `installPolyfill` が任意の `target` に同じグローバル面を載せられる
- [ ] WPT ランナーが polyfill インストーラー経由で sandbox を構築し、手書きの WebRTC グローバル列挙と `Navigator({ dummyMedia })` を使わない
- [ ] `npm run wpt`（`packages/webrtc`）が許可リストを実行できる
- [ ] 公開 `werift/nonstandard` から `getUserMedia({ path | buffer | stream })` が削除されている
- [ ] ファイル再生のテスト / examples が `createMp4WebmRegister` + `navigator.mediaDevices.getUserMedia` に移行している
- [ ] README / CHANGELOG に非標準 getUserMedia 廃止の破壊的変更が書かれている