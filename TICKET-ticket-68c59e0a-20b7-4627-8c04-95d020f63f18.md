# DTLS 1.3: Pion 実装記事に基づく compliance / interop 修正

- 親チケット: `923f494d-4d8d-49e5-ac56-2591de00c87a`（Epic 3: SPED over ICE）。本チケットは SPED 搬送の前提となる **DTLS 1.3 本体** の protocol bug 修正であり、ICE/SPED/WebRTC 配線は触らない
- 仕様ベース: [DTLS 1.3 in Go: An Implementer's Perspective](https://pion.ly/blog/dtls-one-three/)（2026-08-21）/ `docs/rfc/rfc9147.txt` / RFC 8446 §4.6
- 主対象パッケージ: **`packages/dtls`**（`src/record/v1_3`、`src/engine/v1_3`、`src/handshake/message/tls13`）
- 対象ブランチ: 本 worktree（`ticket/68c59e0a-20b7-4627-8c04-95d020f63f18`）
- Epic 1 / 2 の DTLS 1.3 endpoint と Chromium interop は完了済み。本チケットはその上に残った **wire compliance** と **post-handshake** の穴を塞ぐ
- 1 PR で完結（コミット分割は §2.7）

調査時点でコード上 **再現できる欠陥は 5 件**（P1×3 + P2×2）。6 件目の DTLS 1.2 fallback は「確実に壊れている」とは断定できないが、Pion が踏んだ種類の回帰を防ぐ **wire 試験が不足**している。

| 優先度 | 問題 | 影響 | 本チケット |
| --- | --- | --- | --- |
| P1 | Unified Header の L=0 を受信できない | 合法な DTLS 1.3 peer との interop 失敗 | **必須修正** |
| P1 | NewSessionTicket が post-handshake で unexpected_message | ticket を送る実装との接続が handshake 後に死亡 | **必須修正**（受信・ACK・discard。resumption はしない） |
| P1 | out-of-order handshake queue が受信 epoch を失う | reorder で誤 epoch を dispatch。Finished 検証を入れると即破綻 | **必須修正** |
| P2 | authenticated malformed ACK を silent drop | protocol 違反を握りつぶし、state 不整合・デバッグ困難 | **必須修正** |
| P2 | ACK MTU 縮退で先頭 RecordNumber が消失 | 特殊 MTU で永久に ACK されない record | **必須修正** |
| P2/P3 | DTLS 1.2 fallback 専用 wire / 異実装試験が不足 | Pion が踏んだ extension leak / drop を検出できない | **werift dual→werift 1.2 と既存 OpenSSL の wire 試験は必須**。未リリースの Pion DTLS 1.3 双方向 CI は DoD 外 |

---

## 1. タスクの目的と背景

### 目的

RFC 9147 / RFC 8446 上は合法な peer 挙動（L=0 record、post-handshake `NewSessionTicket`、UDP reorder、認証済みだが malformed な ACK、MTU ギリギリの ACK）で werift DTLS 1.3 association が落ちないようにする。あわせて dual 1.3/1.2 fallback の ClientHello 拡張セットを wire で固定する。

完了時点で次が自動テストで成立していること。

- L=0 の Unified Header ciphertext（handshake / ACK / application_data、L=1+L=0 混在）を受信して復号できる
- client が `connected` のあと `NewSessionTicket` を strict parse → ACK → discard し、association を維持する
- handshake inbox が **message ごとの epoch** を保持し、reorder drain で Finished に epoch 2 が渡る
- epoch ≥ 2 の malformed ACK は `decode_error` で fatal。epoch 0 の不正 ACK は従来どおり silent drop
- ACK が MTU 超過した defensive path で、**未送信の RecordNumber を queue から消さない**
- dual client の CH1 / HVR 後 CH2 について、1.3 系と 1.2 系 extension の有無を wire assert する
- 既存 `packages/dtls` の unit / e2e、OpenSSL dual fallback、BoringSSL DTLS 1.3（harness がある環境）を壊さない

### 背景（Pion 記事が指摘し、werift でも当たる点）

Pion の実装記事は、規格を読んだだけでは見落とす点を interop で踏んだ記録である。werift にそのまま当てはまるものは次。

**Uses Less Data / Unified Header.** DTLS 1.3 の ciphertext は固定 13-byte ではなく、最短 2 bytes の Unified Header。L bit を落とすと length フィールド自体が無く、その record は datagram の残り全部を占める（RFC 9147 §4.1）。werift は `serializeUnifiedHeader()` に `lengthPresent` option がある一方、`decryptRecord()` が L=0 を無条件 throw している。

**Finished Is a Different Story.** DTLS 1.3 は Finished で終わりではなく、`NewSessionTicket` と `KeyUpdate` の post-handshake state machine と独立 ACK が必要（RFC 9147 §5.8.4 / Figure 10）。werift の KeyUpdate は read/write epoch 分離まで実装済み。`NewSessionTicket` のメッセージ型も `HandshakeType.new_session_ticket_4` として定数だけあるが、**codec も dispatch も無い**。

**The Spec Isn't Linear / interop testing.** epoch は方向別。UDP reorder が前提。Pion は partial retransmission / split flight / duplicated Flight 4 で壊れた。werift は `handshakeInbox` で message_seq reorder を扱うが、**inbox に epoch が無い**。

**DTLS 1.2 Fallback Isn't a Fresh Handshake.** 1.3 実装を 1.2 mode にしただけでは不十分。本物の 1.2-only 実装との試験が要る。werift は dual 経路（`dualProbeParked`、`DtlsVersionSelected`、CH-A retransmit、legacy 1.2 引き継ぎ）を持つ。e2e の接続成功は既にあるが、**CH1→HVR/SH→CH2 の extension 集合を wire で固定する試験が無い**。

### 現状（本 worktree で確認したコード）

| 箇所 | 現状 | ギャップ |
| --- | --- | --- |
| `packages/dtls/src/record/v1_3/record.ts` `decryptRecord()` | `lengthPresent === false` なら `Error("DTLS 1.3 records without length are not supported for receive")` | RFC MUST の L=0 受信が無い。throw 型も `DtlsDecodeError` ではない |
| 同 `encryptRecord()` | 常に length 付き（`serializeUnifiedHeader` の default） | 受信試験用に L=0 を生成できない。送信最適化は DoD 外 |
| `header.ts` `serializeUnifiedHeader` / `parseUnifiedHeader` | `lengthPresent` は既にある | decrypt / parseNextRecord が使っていない |
| `engine/v1_3/flight/dispatch.ts` `isExpectedHandshakeType` | `connected` で特別扱いするのは `key_update_24` のみ。それ以外は `false` | `new_session_ticket_4` は unexpected_message |
| `handshake/message/tls13/` | `keyUpdate.ts` あり。`newSessionTicket.ts` **なし** | RFC 8446 §4.6.1 の codec が無い |
| `connection-base.ts` `handshakeInbox` | `Map<number, FragmentedHandshake>` | epoch を持たない |
| `record-rx.ts` `enqueueHandshake` | `set(seq, hs)` のあと、drain は **いま処理中の record の epoch** で `dispatchHandshake(next, epoch)` | reorder で Finished に epoch 0 が渡り得る |
| `flight/client/flight5.ts` `onServerFinished` / `flight/server/flight5.ts` `onClientFinished` | 引数は `_epoch` で **未使用** | inbox 修正後に epoch===2 を検証しないと、バグが再発してもテストが赤くならない |
| `record-rx.ts` `handleAck` | 全体が `try/catch` + `log("bad ACK")` | 認証済み epoch の decode 失敗が silent |
| `flight-tx.ts` `sendAck` | MTU 超過時に numbers を戻したあと `slice(1)` して再帰 | 先頭 RecordNumber が未送信のまま消失 |
| dual fallback 試験 | `tests/e2e/self13.test.ts`（werift 1.2-only）、`tests/e2e/client_dual_openssl.test.ts`（OpenSSL `-dtls1_2` 双方向）、BoringSSL 1.3、Chromium は Epic 2 | CH1/CH2 の extension 集合の wire assert が無い。**released pion/dtls v3 は 1.2。DTLS 1.3 は main 進行中で tagging frozen** のため、Pion 1.3 双方向は本チケットの完了条件にしない |

親 Epic 3（SPED）との関係: SPED は DTLS 1.3 handshake datagram を ICE Binding に載せる。本修正は **生 DTLS と SPED 搬送の両方** に効く。SPED codec / `PeerConfig.sped` / ICE 認証境界は親の仕事であり、ここでは触らない。

---

## 2. 実装すべき具体的な機能や変更内容

エンジン規約（`packages/dtls/src/engine/v1_3/AGENTS.md`）を守る。

- `Dtls13Connection extends Dtls13ConnectionBase` 以外の class 階層を増やさない
- flight / record / post-HS は `this: Dtls13Host` の関数として `connection.ts` に assign する
- 1.3 の epoch / transcript / key を 1.2 engine と共有しない
- Public `src/index.ts` から association 内部や NST codec を新たに export しない（テストは KeyUpdate と同様 deep import）

### 2.1 P1: Unified Header L=0 を受信する

対象: `src/record/v1_3/record.ts`（`decryptRecord` / `encryptRecord` / `parseNextRecord` / `parseDatagramRecords`）。`header.ts` の codec は変更不要。

RFC 9147 §4.1:

- L=0 は length 省略。その record は下位トランスポート datagram の **残り全部** を消費する
- L=0 は datagram **最後の record にだけ** MUST
- 同一接続で L=0 と L=1 を混在してよい
- CID（C=1）は現状どおり未対応で reject

`decryptRecord()`:

```text
L=1: ciphertextLength = header の 16-bit length。total = headerLen + length
L=0: ciphertextLength = data.length - headerLen。total = data.length
     L=0 の後に別 record があるという解釈はしない（remainder が ciphertext）
```

境界:

- header 自体が切れていれば既存どおり `DtlsDecodeError`
- L=0 で remainder が AEAD tag+inner（最低 17 bytes: content type 1 + tag 16）に満たなければ `DtlsDecodeError`（truncated L=0 ciphertext）
- remainder > `DTLS13_MAX_CIPHERTEXT_LENGTH`（16640）なら既存の `DtlsRecordOverflowError`
- AEAD AAD は **length を含まない** unmasked header（RFC 9147: AAD = 平文 sequence の header）。`serializeUnifiedHeader(..., { lengthPresent: false })` と同じ並び
- C=1 は従来どおり throw
- 「未サポート」の generic `Error` は削除する

`parseNextRecord()` / `processDatagramRecords()`: `consumed === remaining` になるので、L=0 のあとにループが続かない。L=1 record のあとに L=0 final は通常どおり offset を進めてから最後を消費する。

送信（本チケットの DoD 外）:

- `encryptRecord` に optional `options?: { lengthPresent?: boolean; seq16?: boolean }` を足し、**テストが L=0 ciphertext を合法に作れる**ようにする。default は今と同じ L=1
- `flight-tx.ts` の datagram 末尾 L=0 最適化は **やらない**（MAY の節約。受信 MUST が先。pack 後に再暗号化すると sequence 消費と噛み合う）

テスト（`tests/record/v1_3/`。既存 `header.test.ts` に足すか `unified_header_l0.test.ts` を新設）:

| ケース | 期待 |
| --- | --- |
| single encrypted handshake record L=0 | 復号成功、`consumed === datagram.length` |
| single ACK record L=0 | content type ACK、consumed が末尾 |
| application_data L=0 | payload 一致 |
| L=1 record + L=0 final record（同一 datagram） | 2 件とも復号。途中の L=1 は length で切れ、最後が remainder |
| truncated L=0（header のみ / ciphertext 不足） | `DtlsDecodeError` |
| L=0 remainder > 16640 | `DtlsRecordOverflowError` |
| 既存 L=1 roundtrip / replay / CID reject | 回帰なし |

Act / Assert は日本語コメント（リポジトリ規約）。

### 2.2 P1: post-handshake `NewSessionTicket` を受けて落とさない

対象:

- 新規 `src/handshake/message/tls13/newSessionTicket.ts`（`keyUpdate.ts` と同型の Handshake codec）
- `engine/v1_3/flight/dispatch.ts` `isExpectedHandshakeType` / `dispatchHandshake`
- `engine/v1_3/flight/post-hs.ts` に `onNewSessionTicket`（新 class は作らない）
- `host.ts` / `connection.ts` に assign
- `engine/v1_3/README.md` の Post-HS 行を `KeyUpdate + NewSessionTicket + ACK` に更新

RFC 8446 §4.6.1:

```text
struct {
    uint32 ticket_lifetime;
    uint32 ticket_age_add;
    opaque ticket_nonce<0..255>;
    opaque ticket<1..2^16-1>;
    Extension extensions<0..2^16-2>;
} NewSessionTicket;
```

RFC 9147 Figure 10 / §5.8.4: サーバが NST を送り、クライアントは ACK する。複数 NST を ACK 待ちせず送ってよい。resumption / PSK / 0-RTT は **本チケット対象外**（`docs/plan/research-warp.txt` も PSK 永続化を将来扱いにしている）。

minimum implementation:

```text
HandshakeType.new_session_ticket_4
  → isExpected: role === "client" && hsPhase === "connected"
  → NewSessionTicket.deSerialize で strict parse（trailing bytes 禁止、ticket length ≥ 1）
  → 構文 OK なら ticket は保存せず discard
  → ackAfterCurrentRecord = true（KeyUpdate / Finished と同じ。RX が record を note してから ACK）
```

拒否:

| 条件 | alert |
| --- | --- |
| server が NST を受信 / handshake 中の client | 既存どおり `unexpected_message`（`isExpectedHandshakeType` が false） |
| body 切り詰め・ticket 空・length 不一致・trailing bytes | `DtlsProtocolError` + `AlertDesc.DecodeError` |
| 未知 extension | 一覧は無視してよい（TLS 1.3 の unknown は skip）。**declared extensions 長が body と不一致なら decode_error** |

`processHandshakeBytes` は handshake content type なら `accepted=true` のあと `finishHandshakeRecordAck` が走る。`ackAfterCurrentRecord` を立てれば Figure 10 の即時 ACK になる。KeyUpdate の `keyUpdateResponseAfterAck` とは独立。NST 用の再送 state machine は **送らないので不要**。

将来 resumption 用の型は実装しない（コメントで RFC フィールド名を残す程度でよい）。`interface SessionTicket` の保持・PSK derivation は明示的にやらない。

テスト:

- codec roundtrip（lifetime / ageAdd / nonce / ticket / empty extensions）
- truncated / empty ticket / trailing bytes → throw
- client `hsPhase=connected` で dispatch → 落ちない、`ackAfterCurrentRecord` または ACK 送信を観測
- server connected、または client `wait_finished` では `unexpected_message`
- 複数 NST（message_seq 連続）を連続 dispatch しても connected のまま

e2e: 合成レコードを connected 後の client engine に inject する unit で足りる。BoringSSL が ticket を送るなら既存 `npm run test:boringssl` が回帰になる（送るかは未確認 → 未確定事項）。

### 2.3 P1: handshake inbox に epoch を保存する

対象: `connection-base.ts` の `handshakeInbox`、`record-rx.ts` `enqueueHandshake`。必要なら型を `record-rx.ts` か `types.ts` に置く。

```ts
interface ReceivedHandshake {
  handshake: FragmentedHandshake;
  epoch: number;
}

handshakeInbox = new Map<number, ReceivedHandshake>();
```

enqueue:

```text
seq > nextReceiveSeq → set(seq, { handshake: hs, epoch })
seq === nextReceiveSeq → dispatchHandshake(hs, epoch)  // この record 自身の epoch
drain → dispatchHandshake(queued.handshake, queued.epoch)  // queue した当時の epoch。いまの record の epoch ではない
```

inbox overflow（32）と duplicate re-ACK、unassociated server の CH seq rewind は既存のまま。rewind 時 `handshakeInbox.clear()` も維持。

Finished の epoch 検証（inbox 修正と **同じコミットまたは直後**）:

- `onServerFinished` / `onClientFinished` の `_epoch` を使う
- handshake Finished は epoch **2**（RFC 9147: epoch 2 = handshake traffic keys。epoch 1 は reserved、app は 3+）
- `epoch !== 2` なら `DtlsProtocolError` + `unexpected_message`（または `decrypt_error`。実装は既存 Finished verify 失敗に合わせ、alert は `alertDescForHandshakeError` 経由で矛盾しないこと）

これを inbox 修正より先に入れると、SH epoch 0 のあとに queued EE/Finished を drain する合法 reorder が落ちる。**inbox 修正とセット。**

危険な現行シナリオ（テストで再現する）:

```text
ServerHello        message_seq=N     epoch 0   ← 遅延
EncryptedExtensions message_seq=N+1  epoch 2   ← 先着、inbox へ
Certificate/CV/Finished  N+2..       epoch 2   ← inbox へ
（SH 到着）drain が全部 epoch 0 で dispatch  ← バグ
```

`isAllowedHandshake` は `processHandshakeBytes` 時点の正しい epoch で既に通っている。被害は **dispatch に渡る epoch**。現状ハンドラが `_epoch` 未使用なため接続は通ることがあるが、Finished 検証を入れると即死する。Pion PR（protected-handshake epoch queue）と同種。

テスト（`tests/handshake/tls13/` に新設。`(client as any).engine13.enqueueHandshake` で足りる）:

- EE（epoch 2, seq=1）を先に enqueue し、SH（epoch 0, seq=0）相当で nextReceiveSeq を 0 から drain。**EE の dispatch に epoch 2 が渡ること**（spy、または Finished を queue して epoch!==2 で落ちないこと）
- Finished（epoch 2）を SH（epoch 0）より先に queue し、drain 後も Finished 検証が epoch 2 として通る（または dummy で epoch 記録）
- 逆順でない in-order 経路は回帰なし
- `self13_matrix.test.ts` の encrypted handshake reorder e2e も緑のまま

fragment 再組み立てが fragment ごとの epoch を持たない点は **本チケット対象外**（同一 message_seq の fragment は同一 epoch が普通）。必要なら別チケット。

### 2.4 P2: 認証済み malformed ACK は fatal

対象: `record-rx.ts` `handleAck`。`DtlsAck.deSerialize`（`handshake/message/tls13/ack.ts`）は throw を続ける。

```text
try {
  parse ACK
  …既存の empty / epoch filter / pending 更新…
} catch (error) {
  if (receivedEpoch >= 2) {
    throw new DtlsProtocolError(
      "decode_error: malformed ACK",
      AlertDesc.DecodeError,
    );
  }
  log("bad ACK", error); // unauthenticated epoch 0
}
```

`onCiphertextRecordAsync` の ACK 分岐は `handleAck` の throw をそのまま上げ、既存の `processDatagramRecords` → `DtlsProtocolError` → `failAuthenticatedHandshake` 経路に乗せる。epoch 0 plaintext ACK は `onPlaintextRecordAsync` から `handleAck(..., 0)` なので silent drop のまま。

追加の strict（推奨、同一修正内）: 認証済み ACK で `buf.length !== 2 + listLen`（trailing bytes）も decode_error。今の `deSerialize` は trailing を無視する。epoch 0 では従来どおり緩くてよい（noise）。実装が `deSerialize` を厳しくすると epoch 0 も変わるので、**strict は handleAck 側か `deSerialize(buf, { strict })`** にする。

変えないもの:

- empty ACK の意味（何も ACK しない、再送促進）
- `epoch > receivedEpoch` の RecordNumber を ignore（Erratum 8108 は追わない。既存コメントどおり verified errata のみ）
- inbound 32 件 cap で余剰を無視する挙動（allocation bound。malformed ではない）
- `tests/e2e/self13_plaintext_ack.test.ts`（epoch 0 の forged ACK が KeyUpdate を進めない）

テスト: epoch 2 の短い buffer / `listLen % 16 !== 0` で `DtlsProtocolError` + `DecodeError`。epoch 0 の同じ bytes は throw しない。

### 2.5 P2: ACK MTU 縮退で RecordNumber を消さない

対象: `flight-tx.ts` `sendAck`。

現行（バグ）:

```text
numbers = sorted.slice(0, maxN)
receivedRecordNumbers = sorted.slice(maxN)   // remainder を退避
encrypt
if (record.length > mtu) {
  receivedRecordNumbers = [...numbers, ...remainder]
  if (numbers.length > 1) {
    receivedRecordNumbers = receivedRecordNumbers.slice(1)  // 先頭 A が消失
    await sendAck(opts)  // 再帰
  }
}
```

`numbers=[A,B,C]`, remainder=`[D]` なら `[A,B,C,D]` → `slice(1)` → `[B,C,D]`。A は未送信なのに消える。

`maxAckRecordsForMtu()` が正しければ通常来ない。path MTU 縮小、header 長の見積もりずれ（将来 CID、seq 8-bit、L=0）、`ACK_ENCRYPTED_OVERHEAD` 定数ずれで踏む defensive path。

修正方針: 再帰禁止。候補数を明示的に減らす。**wire に載せた RecordNumber だけ queue から外す。** 減らすときは **末尾（新しい番号）から**（oldest-first drain を維持。`slice(1)` は oldest を捨てていて方針と逆）。

```text
sorted = oldest-first
maxN = maxAckRecordsForMtu()
for (n = min(maxN, sorted.length); n >= (allowEmpty ? 0 : 1); n--) {
  candidate = sorted.slice(0, n)
  record = encodeAck(candidate)  // encrypt は sequence を消費する。未送信でも nonce 再利用禁止なので seq 進行は許容
  if (record.length <= mtu) {
    ok = await sendWithBudget(record)
    if (!ok) {
      receivedRecordNumbers = sorted  // 何も送っていないので全部戻す
      return
    }
    receivedRecordNumbers = sorted.slice(n)  // 送った先頭 n 件だけ除去
    return
  }
}
// 1 件でも載らない: 全部戻して return（後の MTU 更新 / 再送に任せる）
receivedRecordNumbers = sorted
```

`encryptRecord` は送らなくても `writeSequence++` する。DTLS の sequence ギャップは合法。失敗した暗号化の seq を巻き戻さない。

`maxAckRecordsForMtu` の見積もりは残す（通常 path の回数を減らす）。defensive loop が最後の安全網。

テスト: `maxAckRecordsForMtu` を stub するか carrier MTU を実 ACK より小さくし、3 件 queued → 縮退後も先頭 epoch/seq が残るか、載った分だけ消えること。`slice(1)` 相当で先頭が消えないこと。既存 `ack_epoch_mtu.test.ts` の overhead 定数テストは維持。

### 2.6 P2/P3: DTLS 1.2 fallback の wire 試験を足す

コードだけから「fallback 実装が壊れている」とは断定しない。入れるのは **回帰検出**。

既存資産:

| 試験 | あるもの | 無いもの |
| --- | --- | --- |
| werift dual → werift 1.2-only | `tests/e2e/self13.test.ts` 接続成功 | CH1/CH2 extension の byte 集合 |
| werift dual ↔ OpenSSL `-dtls1_2` | `tests/e2e/client_dual_openssl.test.ts` 双方向 + SRTP | 同上 |
| HVR 後 1.3 resume | `self13_dual_hvr_resume.test.ts` | CH extension カタログ |
| supported_versions codec | `tests/version/supported_versions_wire.test.ts` | fallback 中の CH 全体 |
| pion/dtls 1.2 or 1.3 | README リンクのみ | ハーネスもスクリプトも無い |
| Chrome / BoringSSL 1.3 | Epic 2 e2e / `test:boringssl` | 本チケットで matrix を広げない（回帰維持） |

本チケットの必須:

1. **werift `[V1_3, V1_2]` client × werift `V1_2` server** で UDP を tap し、最初の ClientHello（1.3 engine の CH-A）と、HVR 後の cookie ClientHello（1.2 Flight1 dual path）を parse する
2. 次を assert する（Pion が踏んだ leak/drop のチェックリスト）

| 拡張 / フィールド | CH-A（1.3 engine `buildClientHelloExtensions`） | HVR 後 CH（`setupExtensionsForDualCookiePath` + Flight1） |
| --- | --- | --- |
| `supported_versions` (43) | 1.3 と 1.2 を preference 順 | **残す**（HVR を version 確定にしない。1.2-only は無視する） |
| `key_share` (51) | あり | **残す**（1.3 サーバが後から SH する可能性） |
| `signature_algorithms` (13) | 1.3 schemes。dual なら `rsa_pkcs1_sha256` 追加 | 同様 |
| cipher suites | `0x1301` + 1.2 ECDHE-GCM | 1.2 Flight1 の dual セット |
| cookie | 1.3 は legacy_cookie empty。TLS cookie ext は HRR 用 | DTLS 1.2 **legacy cookie フィールド** に HVR cookie |
| `use_srtp` (14) | `srtpProfiles` があるとき | 1.2 `this.extensions` 経由でも維持 |
| `extended_master_secret` (23) | **CH-A には無い**（1.3 engine は載せない） | 1.2 `setupExtensions` で **載る** |
| `renegotiation_info` (65281) | CH-A には無い | 1.2 path で **載る** |

これは「1.3 を 1.2 mode にした」のではなく、**本物の 1.2-only werift server** との経路である。OpenSSL dual e2e は既存のまま緑を維持する（接続成功。extension dump までは必須にしない。OpenSSL 出力が不安定なため）。

入れない（DoD 外）:

- released pion/dtls との 1.3 双方向（v3.1.x は 1.2。1.3 は main、tagging frozen）
- pion/dtls 1.2 専用ハーネス新設（OpenSSL と werift 1.2-only で「本物の 1.2」は既にある）
- Chrome fallback matrix の拡張（Epic 2 の既定経路を壊さないことだけ確認）
- fallback 実装の大きな書き換え（wire 試験が赤なら **そのテストが示す不足だけ** 直す。推測で CH-A に EMS を足したりしない）

試験ファイル案: `packages/dtls/tests/e2e/self13_dual_fallback_ch_wire.test.ts`。Arrange は既存 e2e の `UdpTransport` + `certPem`。Act で client transport の send を wrap して handshake plaintext を集める。

### 2.7 推奨コミット順（1 PR 内）

各コミットで `cd packages/dtls && npm run type && npm test` が通ること。

1. `fix(dtls13): accept DTLS 1.3 unified header records with L=0`
2. `fix(dtls13): keep record epoch on out-of-order handshake inbox`
3. `feat(dtls13): accept post-handshake NewSessionTicket and ACK then discard`
4. `fix(dtls13): treat authenticated malformed ACK as decode_error`
5. `fix(dtls13): do not drop unsent ACK record numbers when shrinking to MTU`
6. `test(dtls): assert dual 1.2 fallback ClientHello extension sets on the wire`
7. `docs(dtls): document post-handshake NewSessionTicket receive path`

---

## 3. 技術的な実装アプローチ（調査結果）

### L=0 と AAD

`encryptRecord` は平文 sequence の header を AAD にし、ciphertext 取得後に SN を mask する。L=0 受信は同じ手順で、header 長が `1 + seqLen`（length 2 bytes なし）。テスト用 `encryptRecord(..., { lengthPresent: false })` は `serializeUnifiedHeader` に option を渡すだけ。既存 caller は引数 3 つのまま。

`parseDatagramRecords` は `parseNextRecord` の `consumed` に従うので、decrypt が `total = data.length` を返せば自然に止まる。`processDatagramRecords` も同じ。

### NewSessionTicket と KeyUpdate の差

KeyUpdate は epoch を進め、ACK 後に write key を切り替える（`pendingKeyUpdateWrite` / `applyPendingKeyUpdateWrite`）。NST は暗号状態を変えない。dispatch に載せて ACK して捨てる。サーバからの送信・ticket 保存・PSK はしない。

`HandshakeType.new_session_ticket_4 = 4` は TLS 1.2 の NST と同じ番号だが、1.3 engine の `connected` 以外では expected にしないので 1.2 engine と混線しない。

parse 失敗の generic `Error` は KeyUpdate と同様 `alertDescForHandshakeError` に吸われる。NST は最初から `DtlsProtocolError(DecodeError)` を投げ、認証済み path を明示する。

### inbox epoch と Finished

`dispatchHandshake` は既に `onFinished(body, epoch)` に epoch を渡している。欠けるのは inbox。`onServerFinished` が epoch を無視しているため、今は reorder でも verify_data（transcript）だけで通る。epoch===2 チェックを inbox と同時に入れ、テストが「drain が epoch 0 を渡す」を検出できるようにする。

### malformed ACK と RX chain

`handleDatagram` の `rxChain.catch` は `DtlsProtocolError` を `fail()` する。ciphertext 上の ACK は AEAD 通過済みなので、decode 失敗は noise ではなく peer の protocol 違反。epoch 0 は AEAD 無しなので drop が正しい。

### sendAck の sequence 消費

MTU 超過で encrypt したあと送らない場合も `writeSequence` は進む。巻き戻すと nonce 再利用になる。ギャップ許容が正しい。loop で何度も encrypt すると seq が飛ぶので、可能なら **先に `ACK_ENCRYPTED_OVERHEAD + 16*n` で見積もり、encrypt は載る n に対して 1 回** にする。見積もりが甘く実 record がまだ大きいときだけ n-- して再 encrypt。

### dual CH の二つの生成源

- CH-A: `engine/v1_3/flight/client/flight1.ts` `buildClientHelloExtensions`（SV、groups、key_share、sigalgs、optional TLS cookie ext、optional use_srtp）。EMS / renegotiation_info なし
- HVR 後: `client.ts` `setupExtensionsForDualCookiePath` → 1.2 `setupExtensions()` + SV を unshift。`continueDualAfterHvr` が legacy cookie を付けて Flight1

両方とも `DtlsVersionSelected(V1_2)` で動く（HVR も「SH without supported_versions」も）。後者は `helloVerifyCookie` 無しで `continueDualAfterHvr(undefined)` になる。wire 試験はまず **HVR を出す werift 1.2-only server**（既存 e2e と同じ）に固定する。SH-only（Chrome が HVR を飛ばす経路）の extension カタログは試験が赤になったときだけ直す。

### Pion DTLS 1.3 を DoD にしない理由

- 記事時点でも post-handshake（NST 等）は pion/dtls#824 で追跡中だった
- GitHub 上の pion/dtls は v3 tagging frozen、1.3 は main。released v3.1.4（2026-06）は 1.2
- 親 Epic 3 の Pion 完了条件は **stun/ice codec** と released ICE agent の非 SPED fallback。pion/dtls#766 は未マージ
- 本チケットで未リリース binary を必須にすると CI が不安定になる

OpenSSL 1.2 と werift 1.2-only が「1.3 実装を 1.2 mode にしただけではない」試験になる。

---

## 4. 考慮すべき制約や注意点

- **受信 L=0 は MUST、送信 L=0 は MAY。** 送信最適化を DoD に含めない。`encryptRecord` の option はテスト用
- **CID は未対応のまま。** L=0 と CID を同時にやらない
- **resumption / PSK / 0-RTT / 送信 NST は対象外。** 合法メッセージで association を落とさないことが目的
- **エンジンに class 階層を足さない。** NST 受信は `post-hs.ts` の関数。codec は `handshake/message/tls13/` の既存 Handshake クラスパターン
- **Public API を増やさない。** `DtlsClient` / `DtlsServer` / `src/index.ts` に NST 型や L=0 フラグを出さない
- **1.2 engine と key/epoch を共有しない**
- **SPED / ICE / `PeerConfig.sped` を触らない。** 親 Epic の範囲。DTLS の ACK/record 修正は SPED 上の handshake にも自動で効く
- **authenticated 失敗を catch-and-ignore しない。** malformed ACK の修正そのものがこの規約
- **`handleAck` の epoch 0 empty ACK 抑制**（protected 後）は維持
- **Finished epoch===2 は inbox 修正と同時。** 単独で先に入れると合法 reorder が死ぬ
- **sendAck で encrypt 失敗分の seq を巻き戻さない**
- **fallback 試験が赤でも、CH-A に 1.2 専用拡張を足すのは wire 証拠が示してから。** dual は 1.3 拡張を HVR 後も残すのが意図
- **Windows 非対応**（リポジトリ全体）
- テスト規約: Arrange は共有できるなら `tests/fixture.ts`。Act / Assert に日本語コメント
- BoringSSL / OpenSSL e2e は環境依存。unit で 5 件の修正を完結させ、e2e は回帰

---

## 5. 完了条件

### Unified Header L=0

- [ ] `decryptRecord` / `parseNextRecord` が L=0 を datagram remainder として復号する
- [ ] L=1 の後続 record と、L=0 は datagram 最後、という境界がテストされている
- [ ] truncated L=0 と record_overflow が既存エラー型で失敗する
- [ ] 「records without length are not supported」を受信 path から削除している
- [ ] CID=1 は従来どおり拒否
- [ ] 送信 datagram 末尾の L=0 最適化は未実装でもよい（DoD 外）

### NewSessionTicket

- [ ] `newSessionTicket.ts` が RFC 8446 §4.6.1 を strict parse する
- [ ] client + `connected` のみ accept。それ以外は `unexpected_message`
- [ ] 受理したら ACK 対象にし、ticket は保存しない
- [ ] サーバは NST を送らない
- [ ] Public API に ticket / PSK 面を出していない

### Handshake inbox epoch

- [ ] `handshakeInbox` が `{ handshake, epoch }` を保持する
- [ ] drain が queued epoch で `dispatchHandshake` する
- [ ] Finished は epoch 2 以外を拒否する（inbox 修正と同時）
- [ ] SH 遅延 + EE/Finished 先着の reorder テストがある

### ACK

- [ ] `receivedEpoch >= 2` の malformed ACK は `DtlsProtocolError` + `decode_error` で association が fail する
- [ ] epoch 0 の不正 ACK は silent drop（既存 plaintext ACK 攻撃テストが緑）
- [ ] `sendAck` の MTU 縮退で未送信 RecordNumber が消えない
- [ ] 送信成功した番号だけ queue から外れる

### Fallback 試験

- [ ] werift dual client × werift 1.2-only server で CH-A と HVR 後 CH の extension / cookie / cipher を wire assert する
- [ ] 既存 `client_dual_openssl.test.ts` と `self13` dual fallback e2e が緑
- [ ] pion/dtls 1.3 双方向 CI は **本チケットの必須ではない**

### 回帰

- [ ] `cd packages/dtls && npm run type && npm test`
- [ ] handshake/record を触ったので、harness がある環境では `cd packages/dtls && npm run test:boringssl`
- [ ] Public / typedoc を変えた場合のみルート `npm run doc:check`（本チケットは変えない想定）
- [ ] SPED / ICE / WebRTC の既定経路を変更していない（dtls パッケージ内に閉じる）

### 検証コマンド

```bash
cd packages/dtls && npm run type && npm test
# harness があるとき
cd packages/dtls && npm run test:boringssl
```

クロスパッケージ変更が無い限りルート `npm run ci` は必須にしない。dtls の record/handshake を触るため、PR 前に可能なら `npm run test:small`。

### Definition of Done

次が自動テストで成立したときのみ完了とする。

```text
合法 L=0 ciphertext  →  werift DTLS 1.3 decrypt / dispatch
peer NewSessionTicket (client, connected)  →  ACK + discard（association 生存）
UDP reorder で epoch 0 の SH より epoch 2 の EE/Finished が先着  →  各 message が自分の epoch で dispatch
AEAD 済み malformed ACK  →  decode_error fatal
ACK MTU shrink  →  未送信 RecordNumber が残る
dual ClientHello wire  →  1.3 拡張と 1.2 拡張が CH-A / HVR 後で仕様どおり
```

送信 L=0、PSK resumption、Pion DTLS 1.3 双方向、CID、SPED 配線は完了条件に含めない。
