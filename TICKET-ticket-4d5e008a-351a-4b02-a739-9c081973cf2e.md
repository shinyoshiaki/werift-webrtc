update_todo

● Grep "onReceive"

● Glob "packages/sctp/**/*"

● Read packages/sctp/src/sctp.ts lines 70-140

● Grep "class Event|subscribe\(|once\(|asPromise\(" in **/*.{ts,tsx}
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/packages)

● Read packages/sctp/README.md

● Read packages/common/src/event.ts

● Read packages/sctp/examples/pong.ts

● Read packages/sctp/examples/ping.ts

● Read packages/sctp/tests/udp.test.ts

● Read packages/webrtc/src/media/track.ts lines 1-90

● Grep "readonly on[A-Z].*= new Event|= new Event<"
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/packages/webrtc/src)

● Read packages/sctp/src/index.ts

● Grep "SCTP\.client\(|SCTP\.server\("
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/packages/sctp)

● Grep "\.onReceive\s*=\s*\(" (/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e)

● Read packages/sctp/tests/sctp.test.ts

● Grep "onReceive\.execute"
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/packages/sctp/src/sctp.ts)

● Read packages/webrtc/src/transport/sctp.ts lines 1-120

● Grep "onReceive|sctp"
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/changelog.md)

● github-mcp-server-issue_read

● github-mcp-server-issue_read

● Read packages/sctp/package.json

● Read packages/sctp/src/transport.ts

● Grep "createUdpTransport"
(/home/shin/code/werift.worktree/4d5e008a-351a-4b02-a739-9c081973cf2e/packages/sctp/src)

● update_todo

● Create session plan file
  $ cat > /home/shin/.copilot/session-state/73571aa2-99b1-4983-8cd1-ef2a9ac93ef7/plan.md <<'EOF'
  # Plan
  - Investigate SCTP onReceive API and current examples/tests.
  - Identify mismatch between README and TypeScript API.
  - Produce detailed task specification with scope, approach, constraints, DoD....

● update_todo

## 1. タスクの目的と背景

- `packages/sctp/README.md` のサンプルが `server.onReceive = ...` / `client.onReceive = ...` になっている一方、実装側 `packages/sctp/src/sctp.ts` では `onReceive` は `readonly onReceive = new Event<[number, number, Buffer]>()` です。  
- 実際の正しい利用パターンは `onReceive.subscribe(...)` で、`packages/sctp/examples/ping.ts` / `pong.ts` / `tests/udp.test.ts` でもその形が使われています。  
- つまり「公開ドキュメントと実APIの不一致」が根本原因で、TypeScript利用者が README 通りに書くと `TS2540` で破綻します。

## 2. 実装すべき具体的な機能や変更内容

- **必須対応（本タスクの主対象）**
  - `packages/sctp/README.md` の `onReceive` 使用例を `onReceive.subscribe((streamId, ppId, data) => ...)` に修正。
  - サーバー/クライアント両方の例を同じ流儀に統一。
- **推奨対応（再発防止）**
  - README のコードを型チェック対象に含める（最小構成の doc-snippet test か、既存 `examples` 参照への置き換え）。
  - API利用方針を明記（`Event` は代入ではなく `subscribe/once/asPromise` を使う）。
- **関連不整合（別タスク化推奨）**
  - README の `import { createUdpTransport } from "werift-sctp"` は現状 `packages/sctp/src/index.ts` で未exportのため成立しないので、同issue系として別スコープ管理が妥当です。

## 3. 技術的な実装アプローチ（調査結果サマリ）

- **方針A（最小・安全）**: ドキュメント修正のみ。  
  - 既存実装・既存テスト・他パッケージ慣例（`readonly Event + subscribe`）と完全整合し、破壊的変更なし。
- **方針B（互換レイヤ追加）**: `onReceive` 代入を受ける setter を追加。  
  - 既存 `Event` 設計とズレやすく、他の `readonly Event` 群との一貫性を崩すため非推奨。
- **結論**: 本件はAPI変更ではなく「README誤記修正」が本筋で、必要なら docs/snippet の型検証を追加して品質担保するのが最適です。

## 4. 考慮すべき制約や注意点

- `werift` 全体でイベント公開は `readonly Event` が標準パターン（`webrtc` 側も同様）なので、`onReceive` を writable callback に変えるのは避けるべきです。  
- ドキュメント修正時、サンプル内の import/export 整合（`createUdpTransport` 問題）も同時確認しないと「READMEは直ったが動かない」が残ります。  
- TypeScriptユーザー向けには「代入不可・購読APIを使う」点を明示し、`streamId / ppId / data` の引数意味を併記すると誤用が減ります。

## 5. 完了条件

- [ ] `packages/sctp/README.md` の `onReceive = ...` が全て `onReceive.subscribe(...)` に置換されている。  
- [ ] READMEサンプルが TypeScript 的に `TS2540` を起こさないことを確認できる。  
- [ ] 既存 `packages/sctp/examples/*` の利用形（subscribe）とREADMEが一致している。  
- [ ] （推奨）README記載コードの型検証をCIまたはテストで担保できる状態になっている。