# タスク詳細: GitHub Pages build failure 修正

## メタ情報

| 項目 | 内容 |
|------|------|
| 対象 CI | [pages build and deployment / job build](https://github.com/shinyoshiaki/werift-webrtc/actions/runs/30065996062/job/89396981601) |
| Run ID | `30065996062` |
| Job ID | `89396981601` |
| Workflow | `pages-build-deployment`（GitHub 管理の Pages 用 workflow） |
| 失敗コミット | `3e478a12aac8e8eaa156e97a60908757be18deb1` (`develop`) |
| 失敗ステップ | `Build with Jekyll` |
| Pages 設定 | `build_type: legacy`, source: branch `develop` / path `/` |
| Pages URL | https://shinyoshiaki.github.io/werift-webrtc/ |
| 直近状況 | 同 workflow が連続 failure（少なくとも 2026-07-23 夜〜2026-07-24） |

---

## 1. タスクの目的と背景

### 目的

GitHub Pages の自動デプロイ (`pages build and deployment`) が失敗している状態を解消し、公開ドキュメント / デモ HTML が再び配信できるようにする。

### 背景

本リポジトリの公開サイトは **Jekyll サイトではなく、リポジトリ上の静的ファイル配信** を前提としている。

- README のドキュメント URL:
  - `https://shinyoshiaki.github.io/werift-webrtc/website/build/`
  - `https://shinyoshiaki.github.io/werift-webrtc/website/build/docs/api`
- デモ HTML:
  - `https://shinyoshiaki.github.io/werift-webrtc/examples/mediachannel/pubsub/answer`
  - `https://shinyoshiaki.github.io/werift-webrtc/examples/datachannel/answer`
- `website/` は Docusaurus 2（`website/build/` に静的成果物を保持、配下に `.nojekyll` あり）
- ルートには `.nojekyll` / `_config.yml` が **存在しない**
- `gh-pages` ブランチは **存在しない**
- Pages は `develop` ブランチルートを legacy Jekyll でビルドする設定

### 失敗の直接原因

Pages の checkout が `submodules: recursive` で `third_party/wpt` を取り込み、Jekyll / Liquid が WPT 配下の Markdown を処理して fatal になる。

**致命エラー:**

```text
Liquid Exception: Liquid syntax error (line 142): Unknown tag 'import'
in third_party/wpt/html/canvas/tools/README.md
```

該当箇所は WPT 側の **Jinja** 説明であり、GitHub Pages の Liquid が `{% import ... %}` を未知タグとして例外化している。

同ログには `third_party/wpt/docs/writing-tests/*.md` 由来の Liquid Warning も多数あるが、ビルドを落としているのは上記 `Unknown tag 'import'`。

---

## 2. 実装すべき具体的な機能や変更内容

### 推奨修正（最小・本リポジトリの公開モデルに一致）

1. **リポジトリルートに空の `.nojekyll` を追加する**
   - GitHub Pages の Jekyll 処理を無効化し、ブランチ内容を静的ファイルとして配信する
   - `website/build/` や `examples/**/*.html` をそのまま公開する現行 URL 構造を維持できる
   - WPT の Jinja 記法・各種 Markdown 内 `{{ }}` による Liquid 解釈を回避できる

2. **変更後の Pages 再実行を確認する**
   - `develop` へマージ後、`pages-build-deployment` が success になること
   - Pages status が `errored` から回復すること
   - 主要 URL（`website/build/`, examples HTML）が 200 で応答すること

### 代替案（必要なら検討。推奨は上記）

| 案 | 内容 | 評価 |
|----|------|------|
| A. ルート `.nojekyll` | Jekyll 無効化 | **推奨**。現行ホストモデルと一致し差分最小 |
| B. ルート `_config.yml` で `exclude` | `third_party`, `packages`, `node_modules` 等を除外 | 暫定可。ただし Jekyll 実行自体は残り、他 Markdown の Liquid 警告/将来の fatal が残る |
| C. Pages source を `website/build` 等に限定 | docs 専用配信 | examples の現行 URL が壊れるため追加対応が必要 |
| D. Actions ベース Pages（Docusaurus build → artifact deploy） | 本格的な docs パイプライン | 正攻法だがスコープ大。本チケットの一次目的（CI 修復）を超える |

### このチケットでやらないこと（スコープ外推奨）

- Docusaurus の全面刷新（現状 `2.0.0-beta.0`、`baseUrl` や organization 設定の整備）
- WPT submodule 自体の削除・内容改変
- 例題 HTML / サイトコンテンツの機能追加
- 通常の `nodejs.yml` テスト CI の変更

---

## 3. 技術的な実装アプローチ（調査結果の要約）

### 現状の Pages パイプライン

1. GitHub 管理 workflow `pages-build-deployment` が起動
2. Job `build`:
   - `actions/checkout@v4`（`ref: develop`, `submodules: recursive`）
   - `actions/jekyll-build-pages@v1`（github-pages v232 / jekyll v3.10.0）
   - 成功時のみ `upload-pages-artifact`
3. 今回は Jekyll ステップで failure、artifact upload は skipped

### なぜ WPT が混ざるか

- `.gitmodules` で `third_party/wpt` → `web-platform-tests/wpt`
- Pages checkout が recursive submodule を取る
- ルートが Jekyll source のため、WPT の `.md` がサイト生成対象になる

### なぜ `.nojekyll` が適切か

- 公開物は既に静的（`website/build/**`, `examples/**/*.html`）
- `website/build/.nojekyll` は既にあるが、**Pages source が repo root のためルート側が必要**
- Jekyll を止めるだけで、Liquid 例外経路そのものが消える

### 実装手順（実装担当向け）

```text
1. ルートに空ファイル .nojekyll を追加
2. develop 向け PR を作成・マージ（または既存作業ブランチに含める）
3. pages-build-deployment の success を確認
4. 主要 URL の疎通確認
```

必要ならローカルで類似確認:

```bash
# ルートに .nojekyll があること
test -f .nojekyll

# 致命原因ファイルが存在する（submodule init 後）
# third_party/wpt/html/canvas/tools/README.md の {% import %}
```

※ ローカルで github-pages gem を完全再現する必要は通常ない。修正は設定ファイル 1 個が中心。

### 関連ファイル

| パス | 役割 |
|------|------|
| `.nojekyll`（新規・ルート） | Jekyll 無効化（本修正の主対象） |
| `website/build/.nojekyll` | Docusaurus 成果物側（既存） |
| `website/` | Docusaurus ソース / ビルド成果 |
| `examples/**/*.html` | Pages 上のデモ入口 |
| `.gitmodules` | WPT submodule 定義（失敗の誘因） |
| `.github/workflows/nodejs.yml` | 通常 CI（本件とは別） |
| README.md | 公開 URL の期待値 |

---

## 4. 考慮すべき制約や注意点

1. **GitHub 管理 workflow はリポジトリ内 YAML では制御しにくい**
   - `pages-build-deployment` は Pages 設定駆動。修正は主にソースツリー側（`.nojekyll` 等）か、Pages 設定変更。

2. **Pages 設定変更は権限・運用影響がある**
   - `source` / `build_type` を Actions 方式へ変える場合は、デプロイ方式の再設計が必要。一次修正では触らない方が安全。

3. **URL 互換を壊さない**
   - 既存の `/website/build/` と `/examples/...` を維持すること。`baseUrl` や Pages path を不用意に変えない。

4. **submodule 公開の副作用**
   - `.nojekyll` で build は通るが、checkout が submodule を含む場合、巨大な WPT ツリーが artifact に載る可能性がある。
   - ビルド失敗の一次修正とは分離してよいが、Pages のサイズ/時間問題が出たら `exclude` や source 限定、Actions デプロイ移行を検討。

5. **WPT 本体は編集しない**
   - `third_party/wpt` 内の `{% import %}` を書き換えて回避するのは upstream submodule 汚染になるため不可。

6. **通常の npm CI とは独立**
   - 本 failure は `npm test` / `nodejs.yml` の失敗ではない。検証は Pages workflow と公開 URL が中心。

7. **Windows 非対応など本体制約は無関係**
   - 本件はドキュメント配信設定の問題。TypeScript プロトコル実装自体の変更は不要。

---

## 5. 完了条件

以下をすべて満たすこと。

1. **コード変更**
   - [ ] リポジトリルートに `.nojekyll` が存在する（空ファイルで可）
   - [ ] WPT submodule やアプリ本体ロジックを不必要に変更していない

2. **CI**
   - [ ] 対象と同等の `pages-build-deployment` が **success** になる
   - [ ] Job `build` の `Build with Jekyll`（または `.nojekyll` 後の静的 publish 相当ステップ）が failure にならない
   - [ ] 少なくとも直近 failure と同じ系統の `Liquid Exception: Unknown tag 'import' in third_party/wpt/...` が再発しない

3. **公開確認**
   - [ ] GitHub Pages status が `errored` 以外（正常配信状態）になる
   - [ ] 次の URL が到達可能（HTTP 200 相当）:
     - `https://shinyoshiaki.github.io/werift-webrtc/website/build/`
     - `https://shinyoshiaki.github.io/werift-webrtc/examples/datachannel/answer`（または既存 answer HTML のいずれか）

4. **回帰**
   - [ ] 通常の package テスト CI（`nodejs.yml` 等）を意図せず壊していない
   - [ ] README 記載のドキュメント / demo URL パスを壊していない

---

## 補足: エラーログ抜粋

```text
Liquid Exception: Liquid syntax error (line 142): Unknown tag 'import'
in third_party/wpt/html/canvas/tools/README.md

##[error] ... Rendering ... third_party/wpt ...
```

事前警告例（fatal ではないが同根）:

```text
Liquid Warning: ... "{{hosts[][www]}}" in third_party/wpt/docs/writing-tests/server-features.md
Liquid Warning: ... "{{domains[]}}" in third_party/wpt/docs/writing-tests/server-pipes.md
```

---

## 実装優先度メモ

1. **Must**: ルート `.nojekyll` 追加 → Pages 緑化
2. **Should**: 主要 URL の疎通確認
3. **Could（別チケット可）**: Pages を Actions + Docusaurus 正式デプロイへ移行 / submodule を Pages 成果物から除外
