コードベースのWPT周辺を確認し、実施対象・実装ポイント・完了条件が判断できる形まで整理します。

  │ "**/AGENTS.md"



  │ "\bwpt\b|web-platform-tests|WPT|wpt:" in **/*.{md,json,mjs,js,ts,yml,yaml}

  │ packages/webrtc/tools/wpt-runner/runner.ts

  │ packages/webrtc/package.json

  │ packages/webrtc/tools/wpt-runner/coverage.ts

  │ packages/webrtc/tools/wpt-runner/vitest.test.ts

  │ packages/webrtc/wpt/allowlist.json

  │ packages/webrtc/README.md

  │ "**/wpt*.json" in packages/webrtc

  │ "export type WptRunReport|runSelectedWpt|formatMarkdownReport|defaultReportP
  │ ath|baseline|success|passed|subtest|allowlist|compareWithBaseline" in
  │ **/*.ts (packages/webrtc/tools/wpt-runner)

  │ packages/webrtc/tools/wpt-runner/list.ts

  │ packages/webrtc/tools/wpt-runner/coverageLogic.ts

  │ packages/webrtc/tools/wpt-runner/run.ts

  │ packages/webrtc/tools/wpt-runner/runner.ts

  │ packages/webrtc/tools/wpt-runner/runner.ts

  │ packages/webrtc/tools/wpt-runner/runner.ts

  │ packages/webrtc/tools/wpt-runner/runner.ts

WPTの現在地を把握できたので、基準データから「一部成功しているファイル」を抽出して、改善対象の具体像まで詰めます。


  │ "generatedAt|"status": "PASS"|"status": "FAIL"|"status": "TIMEOUT"|"file": "
  │ in **/*.json (packages/webrtc/wpt)

● Summarize baseline WPT pass counts (shell)
  │ cd /workspace/packages/webrtc && node -e 'const fs=require("fs");const
  │ path=require("path");const
  │ base=JSON.parse(fs.readFileSync("wpt/baseline.json","utf8"));const by=new
  │ Map();for(const r of base.results){const k=`${r.file}${r.variant?`
  │ ${r.variant}`:""}`;const e=by.get(k)||{file:r.file,variant:r.variant||"",p…

  │ cd /workspace/packages/webrtc && node -e 'const fs=require("fs");const
  │ base=JSON.parse(fs.readFileSync("wpt/baseline.json","utf8"));const
  │ allow=JSON.parse(fs.readFileSync("wpt/allowlist.json","utf8"));const
  │ enabled=new
  │ Set(allow.targets.filter(t=>t.enabled).map(t=>`${t.file}${t.variant?` …

  │ packages/webrtc/tools/wpt-runner/worker.ts

  │ packages/webrtc/tests/tools/wptProgress.test.ts

  │ packages/webrtc/tools/wpt-runner/submodule.ts

  │ packages/webrtc/tests/tools/wptCoverage.test.ts

  │ "__wptSelectTest|subtests\?|subtests:|loadAllowlist\(|enabled|allowlist.targ
  │ ets|target.subtests" in **/*.{ts,tsx} (packages/webrtc/tools/wpt-runner,
  │ packages/webrtc/tests)

● Count upstream webrtc WPT html files (shell)
  │ cd /workspace/packages/webrtc && node -e 'const fs=require("fs");const
  │ path=require("path");const walk=(dir,p="")=>fs.readdirSync(dir,{withFileTy
  │ pes:true}).flatMap(e=>{if(e.name.startsWith("."))return[];const
  │ rel=p?`${p}/${e.name}`:e.name;const
  │ abs=path.join(dir,e.name);if(e.isDirectory())return walk(abs,rel);return …

  │ cd /workspace/packages/webrtc && npm run wpt:list --silent | sed -n '1,120p'

  │ coverage/webrtc-wpt

  │ "coverage/webrtc-wpt/*"

● Summarize existing WPT results (shell)
  │ cd /workspace && if [ -f coverage/webrtc-wpt/results.json ]; then node -e
  │ 'const fs=require("fs");const r=JSON.parse(fs.readFileSync("coverage/webrt
  │ c-wpt/results.json","utf8"));const by=new Map();for(const x of
  │ r.results){const k=`${x.file}${x.variant?` ${x.variant}`:""}`;const
  │ e=by.get(k)||{file:x.file,variant:x.variant||"",pass:0,fail:0,timeout:0,sk…

現在の実行結果が未生成だったため、現行実装でWPTを一度走らせて、部分的に通っているファイル群と改善余地を実測ベースで抽出します。

● Run package WPT suite and save report (shell)
  │ cd /workspace && WPT_PROGRESS=1 npm run wpt --workspace packages/webrtc
  │ --silent > /tmp/werift-wpt.stdout 2> /tmp/werift-wpt.stderr; status=$?; echo
  │ "STATUS=$status"; echo "--- STDERR (tail) ---"; tail -n 80
  │ /tmp/werift-wpt.stderr; echo "--- STDOUT (tail) ---"; tail -n 80
  │ /tmp/werift-wpt.stdout; exit $status

  │ cd /workspace && node -e 'const fs=require("fs");const r=JSON.parse(fs.rea
  │ dFileSync("coverage/webrtc-wpt/results.json","utf8"));const by=new
  │ Map();for(const x of r.results){const k=`${x.file}${x.variant?`
  │ ${x.variant}`:""}`;const e=by.get(k)||{file:x.file,variant:x.variant||"",p
  │ ass:0,fail:0,timeout:0,skip:0};if(x.status==="PASS")e.pass++;else …

● Rank mixed-result WPT files (shell)
  │ cd /workspace && node -e 'const fs=require("fs");const r=JSON.parse(fs.rea
  │ dFileSync("coverage/webrtc-wpt/results.json","utf8"));const by=new
  │ Map();for(const x of r.results){const k=`${x.file}${x.variant?`
  │ ${x.variant}`:""}`;const e=by.get(k)||{file:x.file,variant:x.variant||"",p
  │ ass:0,fail:0,timeout:0};if(x.status==="PASS")e.pass++;else …

● Show sample failures for promising WPT files (shell)
  │ cd /workspace && node -e 'const fs=require("fs");const r=JSON.parse(fs.rea
  │ dFileSync("coverage/webrtc-wpt/results.json","utf8"));const
  │ interesting=["webrtc/RTCPeerConnection-addIceCandidate.html","webrtc/RTCPe
  │ erConnection-addIceCandidate.html
  │ ?rest","webrtc/RTCPeerConnection-setRemoteDescription-rollback.html","webr…

  │ /tmp/copilot-tool-output-1778558945034-eio0aq.txt

  │ /tmp/copilot-tool-output-1778558945034-eio0aq.txt

  │ /tmp/copilot-tool-output-1778558945034-eio0aq.txt

  │ packages/webrtc/src/peerConnection.ts

  │ packages/webrtc/src/peerConnection.ts

  │ packages/webrtc/src/peerConnection.ts

  │ "addIceCandidate\(|setRemoteDescription\(|setLocalDescription\(|createDataCh
  │ annel\(|addTransceiver\(|getStats\(|setStreams\(|maxPacketLifeTime|maxRetran
  │ smits|sendEncodings|scaleResolutionDownBy|rid" in **/*.ts
  │ (packages/webrtc/src)

  │ "class RTCPeerConnection|class RTCDataChannel|class RTCRtpSender|class
  │ RTCRtpTransceiver" in **/*.ts (packages/webrtc/src)

  │ packages/webrtc/src/transceiverManager.ts

  │ packages/webrtc/src/media/rtpTransceiver.ts

  │ packages/webrtc/src/media/rtpSender.ts

  │ packages/webrtc/src/dataChannel.ts

  │ packages/webrtc/src/sctpManager.ts

  │ packages/webrtc/src/sdpManager.ts

  │ packages/webrtc/src/secureTransportManager.ts

  │ packages/webrtc/src/secureTransportManager.ts

  │ packages/webrtc/src/media/track.ts

  │ packages/webrtc/src/transceiverManager.ts

  │ "onTrack\.execute|new MediaStream\(|streamId|setStreams|streams" in **/*.ts
  │ (packages/webrtc/src/transceiverManager.ts,
  │ packages/webrtc/src/media/rtpReceiver.ts,
  │ packages/webrtc/src/media/rtpSender.ts)

  │ packages/webrtc/src/media/rtpSender.ts

  │ packages/webrtc/src/media/rtpSender.ts

  │ "setStreams|getParameters\(|setParameters\(|replaceTrack\(" in **/*.ts
  │ (packages/webrtc/src/media, packages/webrtc/src)

  │ packages/webrtc/tests/wpt/peerConnectionApiCompatibility.test.ts

  │ packages/webrtc/tests/wpt

  │ "WPT|compatibility|addIceCandidate|rollback|createDataChannel|transceiver|of
  │ ferToReceive|getStats" in **/*.ts (packages/webrtc/tests)

  │ "**/*peerConnection*Compatibility*.ts" in packages/webrtc/tests

  │ packages/webrtc/tests/integrate/getStats.test.ts

  │ packages/webrtc/tests/integrate/trickle.test.ts

  │ packages/webrtc/tests/integrate/peerConnection.test.ts

  │ packages/webrtc/tests/datachannel/send.test.ts

  │ "addIceCandidate|createDataChannel|maxPacketLifeTime|maxRetransmits|rollback
  │ |offerToReceive|setStreams|getParameters|setParameters" in **/*.ts
  │ (packages/webrtc/tests)

● Inspect near-pass WPT failures (shell)
  │ cd /workspace && node -e 'const fs=require("fs");const r=JSON.parse(fs.rea
  │ dFileSync("coverage/webrtc-wpt/results.json","utf8"));const
  │ keys=["webrtc/RTCPeerConnection-removeTrack.https.html","webrtc/RTCPeerCon
  │ nection-addTrack.https.html","webrtc/RTCPeerConnection-constructor.html","
  │ webrtc/RTCPeerConnection-ontrack.https.html","webrtc/RTCPeerConnection-rem…

  │ packages/webrtc/src/errors.ts

  │ "throw new Error\(|createWebRtcDomException\(" in **/*.ts
  │ (packages/webrtc/src)

✗ Memory stored testing practices
    wri…

● Count files and targets with pass (shell)
  │ cd /workspace && node -e 'const fs=require("fs");const r=JSON.parse(fs.rea
  │ dFileSync("coverage/webrtc-wpt/results.json","utf8"));const files=new
  │ Set();const targets=new Set();for(const x of r.results){if(x.status==="PAS
  │ S"){files.add(x.file);targets.add(`${x.file}${x.variant?`
  │ ${x.variant}`:""}`)}}console.log(JSON.stringify({files:[...files].length,t…

# タスク詳細化: WPTで一部成功しているファイルを列挙し、成功数を増やす互換性改善

## 1. タスクの目的と背景

`packages/webrtc` には upstream Web Platform Tests を直接実行する WPT ランナーがあり、`third_party/wpt/webrtc` 配下の **214 ターゲット**を実行できます。現状の実測では **1066 subtest 中 253 PASS / 781 FAIL / 32 TIMEOUT** で、**少なくとも 1 件 PASS している対象は 65 ファイル（68 file+variant）**あります。

このため本タスクの本質は、**完全未対応領域を広く触るより、すでに部分的に互換性があるファイル群を優先して PASS 数を押し上げる**ことです。  
なお現在の `coverage/webrtc-wpt/results.md` は失敗/タイムアウト中心の要約で、**「1件以上成功しているファイル一覧」は標準出力されません**。そのため、まず可視化を足し、その上で実装改善を進めるのが自然です。

### 現状の有望対象（実測）

| 優先度 | 対象 | 現状 | 主な論点 |
| --- | --- | ---: | --- |
| 高 | `RTCPeerConnection-removeTrack.https.html` | 13 PASS / 1 FAIL | stopped transceiver への `removeTrack()` を no-op にできていない |
| 高 | `RTCPeerConnection-addTrack.https.html` | 11 / 1 | `setRemoteDescription` 進行中の transceiver 順序 |
| 高 | `RTCPeerConnection-constructor.html` | 17 / 5 | `TypeError` / `DOMException` ではなく `Error` を投げている |
| 高 | `RTCPeerConnection-ontrack.https.html` | 6 / 3 | `ontrack` 属性の置き場所、rejected media section、stream 付与 |
| 高 | `RTCPeerConnection-remote-track-properties.https.html` | 6 / 2 | remote stream id の引き回し不整合 |
| 中 | `RTCPeerConnection-setLocalDescription-parameterless.https.html` | 5 / 8, `?rest` 4 / 8 | parameterless SLD の state/current/pending description/mid |
| 中 | `RTCPeerConnection-setRemoteDescription-rollback.html` | 8 / 14, `?rest` 8 / 12 | rollback 時の transceiver/track cleanup、例外型、`setStreams` 欠如 |
| 中 | `RTCPeerConnection-addIceCandidate.html` | 12 / 24, `?rest` 12 / 22 | `sdpMid`/`sdpMLineIndex`/`usernameFragment` 検証と candidate 配置 |
| 中 | `RTCDataChannelInit-maxPacketLifeTime-enforce-range.html` | 7 / 10 | omitted/null, 数値変換, 範囲外, `TypeError` |
| 中 | `RTCDataChannelInit-maxRetransmits-enforce-range.html` | 7 / 10 | 同上 |
| 中 | `RTCPeerConnection-transceivers.https.html` | 30 / 14 | `ontrack.streams`, stream id, `sendEncodings` 初期化 |
| 低 | `RTCPeerConnection-mandatory-getStats.https.html` | 4 / 69 | stats フィールドの網羅性不足 |

---

## 2. 実装すべき具体的な機能や変更内容

1. **WPT結果の可視化追加**
   - `coverage/webrtc-wpt/results.json` を集計し、**PASS が 1 件以上ある file(+variant) 一覧**を出せるようにする。
   - 出力項目は最低でも `file`, `variant`, `pass`, `fail`, `timeout`。
   - 既存 `results.md` 拡張か、専用の list/summarize コマンド追加が妥当。

2. **互換性改善の優先実装**
   - **RTCPeerConnection の state machine / rollback / parameterless SLD**
   - **track / transceiver / ontrack / stream id の整合**
   - **addIceCandidate の入力検証と candidate 適用先解決**
   - **createDataChannel オプションの Web IDL 風 coercion・range check**
   - **例外の型と名前のブラウザ互換化**（`Error` ではなく `TypeError` / `DOMException`）
   - **必要なら `RTCRtpSender.setStreams()`、`getParameters()` / `setParameters()` の不足補完**

3. **関連テストの追加・更新**
   - 既存の package test に、WPT failure を再現する小さい回帰テストを足す。
   - 候補: `tests/integrate/peerConnection.test.ts`, `tests/integrate/trickle.test.ts`, `tests/datachannel/send.test.ts`, `tests/integrate/getStats.test.ts`

---

## 3. 技術的な実装アプローチ

- 実行経路は `packages/webrtc/tools/wpt-runner/run.ts` → `runner.ts` で、結果は `coverage/webrtc-wpt/results.json` に保存されます。
- `discoverWptTargets()` は `third_party/wpt/webrtc` 配下の HTML を自動発見するため、**対象選定の source of truth は `allowlist.json` ではなく実行結果 JSON**です。
- まず **results.json を file+variant 単位で group by し、`pass > 0` を抽出する集計処理**を追加します。
- その後、PASS 比率が高い領域から順に実装修正します。

### 主な修正先

- `src/peerConnection.ts`
  - `setLocalDescription`, `setRemoteDescription`, `addIceCandidate`, `createDataChannel`, constructor/config validation
- `src/sdpManager.ts`
  - pending/current description と rollback
- `src/secureTransportManager.ts`
  - ICE candidate の解決・検証
- `src/transceiverManager.ts`
  - transceiver 順序、`ontrack.streams`、remote stream id
- `src/media/rtpSender.ts`
  - `setStreams`, `getParameters`, `setParameters`, sendEncodings 周辺
- `src/sctpManager.ts` / `src/dataChannel.ts`
  - DataChannel 初期化オプションの coercion / null / range / error semantics
- `src/media/stats*`
  - `mandatory-getStats` 対応

### 推奨実施順

1. 可視化追加  
2. `removeTrack` / `addTrack` / constructor error semantics  
3. `ontrack` / remote stream id / transceiver order  
4. `parameterless SLD` / `SRD rollback`  
5. `addIceCandidate`  
6. `DataChannel` option coercion  
7. `getParameters` / `setParameters` / stats 拡張

---

## 4. 考慮すべき制約や注意点

- WPT は `third_party/wpt` submodule 前提。
- **`allowlist.json` の `enabled` / `subtests` は現状 runner で実行制御に使われていません。**
- WPT は **例外クラス・例外名・IDL属性の位置**まで厳密に見ます。`Error` と `TypeError`、`DOMException("InvalidStateError")` の差で落ちます。
- `npm run wpt` の成功は「baseline regression がない」意味であり、**PASS 数が増えたことの証明には `results.json` の比較が必要**です。
- 公開 API や互換性メモを変える場合は `packages/webrtc/README.md` も更新対象です。
- baseline/coverage を意図的に広げる場合だけ `wpt/baseline.json` や `wpt/coverage-baseline.json` を更新します。

---

## 5. 完了条件

- **PASS が 1 件以上ある WPT ファイル一覧**を、実行結果から再現可能な形で出力できる。
- 部分成功している対象について、少なくとも優先領域の **PASS 数が実際に増える**。
- `npm run wpt --workspace packages/webrtc` 実行後も **regression 0** を維持する。
- 追加した互換性挙動を package test で再現・固定化できている。
- baseline/coverage を更新した場合は、関連 JSON と README の整合も取れている。