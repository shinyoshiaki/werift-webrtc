# Issue #667: TURN CreatePermission/ChannelBind 拒否による allocation 汚染の根因修正

## メタ

| 項目 | 内容 |
|------|------|
| 参照ドキュメント | `werift-issue-667-root-cause-and-fix.md` |
| 主対象パッケージ | `packages/ice` |
| 主変更ファイル | `packages/ice/src/turn/protocol.ts`（`TurnProtocol`） |
| 関連仕様 | RFC 8656 (TURN) |
| 既存回帰試験 | `packages/ice/tests/ice/turn.test.ts`（local `NodeTurnServer`） |
| pion TURN コンテナ | `packages/ice/docker/pion-turn/`（UDP）、`packages/ice/docker/pion-turn-tcp/`（TCP） |
| 手動例 | `packages/ice/examples/ice_turn.ts`, `packages/ice/examples/turn_turn.ts` |

---

## 1. タスクの目的と背景

### 目的

`TurnProtocol` において、**同一 TURN allocation 上で 1 つの peer address に対する `CreatePermission` / `ChannelBind` が拒否されても、他の peer address への後続操作が汚染されない**ようにする。これにより ICE connectivity check で拒否候補が先に試されても、後続の正常な `relay <-> relay` 候補対で接続を確立できるようにする。

### 背景（現象）

現行実装（`packages/ice/src/turn/protocol.ts`）では次の allocation 全体共有状態がある。

- `creatingPermission: Promise<void>` — CreatePermission の in-flight
- `channelBinding?: Promise<void>` — ChannelBind の in-flight
- `channelRefreshAt` — channel refresh 期限（allocation 全体）
- `permissionByAddr` / `channelByAddr` — peer ごとの cache（ただし成功前に書かれる）

この設計では peer 固有の操作失敗が allocation 全体に伝播する。

```text
Peer A: CreatePermission / ChannelBind → TURN サーバが拒否 → rejected Promise が残る
Peer B: getPermission / getChannel → 共有 Promise を await → A の rejection を再送出
        → B への CreatePermission / ChannelBind に到達しない
```

ICE では複数 candidate pair を順に試すため、拒否される peer 向け TURN 操作が先に走ると、その後の正常な relay 経路まで全滅しうる。これが Issue #667 の本質。

### 文書化された派生バグ（同根）

| # | 問題 | 影響 |
|---|------|------|
| 1 | rejected Promise のリセット欠如 | 以降の全 peer 操作が失敗 |
| 2 | permission を成功前に `permissionByAddr` へ cache | 失敗後の retry 不可 |
| 3 | permission key が `IP:port`（`addr.join(":")`） | RFC 上 Permission は peer **IP** 単位 |
| 4 | ChannelBind 失敗後も provisional mapping が残る | 失敗後に「既に bind 済み」と誤認しうる |
| 5 | rejection 時に `channelBinding = undefined` が未実行 | 共有 Promise が永久に reject 状態 |
| 6 | `channelRefreshAt` が allocation 全体 | 後から bind した channel が他 channel の refresh 期限を上書き |

---

## 2. 実装すべき具体的な機能や変更内容

### 2.1 CreatePermission の peer 単位 state isolation（必須）

**対象:** `getPermission()` / `createPermission()` / 関連 private フィールド

- `creatingPermission` を廃止し、以下へ置換:
  - `permissionQueue: Promise<void>` — allocation 内の直列化（認証 state 更新 race 回避）
  - `creatingPermissionByAddr: Map<string, Promise<void>>` — peer ごとの in-flight
  - `permissionByAddr` の key を **peer IP のみ**（`addr[0]`）に変更
- queue tail は常に resolve させ、**呼び出し元には本来の rejection を返す**（rejection を次 peer に伝播させない）
- permission cache は **CreatePermission 成功後にのみ** 設定
- 失敗時は in-flight map を `finally` で削除し、同一 peer の再試行を可能にする
- 同一 peer への concurrent `getPermission` は同一 Promise を共有（transaction は 1 回）
- `createPermission()` は現状 `request()` 直呼びだが、`ChannelBind` と同様に **`requestWithRetry()` を使う**（stale nonce 438 等への一貫対応）

### 2.2 ChannelBind の peer 単位 state isolation（必須）

**対象:** `getChannel()` / `channelBind()` / 関連 private フィールド

- `channelBinding` を廃止し、以下へ置換:
  - `channelBindQueue: Promise<void>`
  - `channelBindingByAddr: Map<string, Promise<TurnChannel>>`
- channel key は **IP + port**（Permission とは分離）。`addr.join(":")` より structured key（例: `JSON.stringify(addr)`）を推奨
- 初回 bind 前の provisional mapping（`channelByAddr` / `addrByChannel`）は維持（success より先に ChannelData が来る可能性への対応）
- **初回 ChannelBind 失敗時のみ** provisional mapping を rollback
- 失敗した channel number は **再利用しない**（巻き戻し禁止）。次回 retry は新 number
- 同一 peer concurrent `getChannel` は dedupe

### 2.3 Channel refresh state の peer 化（必須寄り）

- allocation-global の `channelRefreshAt` を削除
- channel ごとの構造体に `refreshAt` を持たせる:

```ts
interface TurnChannel {
  number: number;
  address: Address;
  refreshAt: number;
}
```

- channel A の refresh 失敗が channel B の初回 bind に伝播しないこと

### 2.4 `sendData()` のフォールバック改善（推奨）

現状の「ChannelBind 失敗 → CreatePermission + Send Indication」構造は維持。

- 元の ChannelBind error を握りつぶさずログに残す（403 / 438 / timeout の追跡性）

### 2.5 ユニット / 回帰テスト（必須）

`packages/ice` に TurnProtocol 向けの unit / integration を追加する（モック TURN またはスクリプト可能な transport が望ましい。最低限、既存 `NodeTurnServer` では再現しにくい isolation ケースは **モックで強制 reject** する）。

必須シナリオ（文書 §17 準拠）:

1. CreatePermission failure isolation（A reject → B success）
2. CreatePermission retry（同一 peer 2 回目で success、1 回目は cache されない）
3. concurrent same permission（transaction 1 回）
4. ChannelBind failure isolation
5. failed initial ChannelBind rollback
6. ChannelBind retry（新しい channel number）
7. concurrent same ChannelBind
8. refresh failure isolation
9. independent refresh deadline
10. end-to-end: 拒否 peer 後に valid peer で ChannelData / ICE 成功

### 2.6 既存経路の非回帰（必須）

既存 `packages/ice/tests/ice/turn.test.ts` の local `NodeTurnServer` 経由:

- UDP / TCP / TLS（`turns:`）の `forceTurn: true` relay 同士接続
- 双方向データ送受信

が引き続き green であること。

### 2.7 pion TURN コンテナによる相互接続試験（完了条件・必須）

**werift 同士 + 自前 `NodeTurnServer` だけでは完了としない。**

pion の TURN サーバを Docker で起動し、werift ICE/TURN クライアントがそのサーバ経由で相互接続できることを確認する。

#### 起動方法（既存資産）

```bash
# UDP TURN（packages/ice/docker/pion-turn）
cd packages/ice/docker/pion-turn
docker compose build
# 必要に応じて PUBLIC_IP をホストから見た到達可能 IP に
# PION_TURN_PUBLIC_IP=... PION_TURN_USERS=username=password
docker compose up -d
```

- 既定: realm `localhost`, users `username=password`, port `3478`, `network_mode: host`
- Dockerfile は `github.com/pion/turn` の `examples/turn-server/simple` をビルド
- TCP が必要な場合は `packages/ice/docker/pion-turn-tcp` を同様に利用

#### 確認手順（最低限）

1. **pion TURN 起動**（上記）
2. **Turn クライアント同士（relay データパス）**  
   `packages/ice/examples/turn_turn.ts` 相当（`createTurnClient` ×2、相手の `relayedAddress` に `getChannel` / `sendData`、双方向 ping/pong）
3. **ICE 接続（relay 強制）**  
   `packages/ice/examples/ice_turn.ts` 相当（両端 `forceTurn: true`、候補交換後 `connect()`、`send` 双方向）
4. 期待結果:
   - allocation 成功（relayed address 取得）
   - ChannelBind / データ転送成功
   - ICE state が connected/completed 相当まで到達し、双方向ペイロードが届く
5. 可能なら **拒否 peer を先に試したうえで** 正常 peer で成功するシナリオも pion 上で確認（Issue 再現の本質）

自動化できる場合は opt-in 環境変数付き integration test（例: `PION_TURN_HOST=127.0.0.1`）として `packages/ice/tests` に追加し、CI 既定では skip、ローカル/明示実行で pion を叩く形が望ましい。自動化が難しい場合は手順と実行ログをチケット/PR に残す。

### 2.8 スコープ外（別チケット候補）

- `DEFAULT_CHANNEL_REFRESH_TIME = 500` と Permission lifetime 300s の不整合是正（文書 §16）。Issue 直接原因ではないが、修正時に触れる場合は独立コミットまたは follow-up として明示する。
- TURN control request の全面並列化（認証 state race のため、今回は直列 queue 維持が安全）。

---

## 3. 技術的な実装アプローチ（調査結果サマリ）

### 現状コードの要点

- 実装中枢: `packages/ice/src/turn/protocol.ts` の `TurnProtocol`
  - `getPermission`: 共有 `creatingPermission` を await → 成功前に `permissionByAddr[addr.join(":")] = true`
  - `getChannel`: 共有 `channelBinding` を await → 成功前に mapping 登録 → reject 時に `channelBinding` がクリアされない
  - `sendData`: `getChannel` 失敗時に `getPermission` + Send Indication へフォールバック
  - `channelBind` は `requestWithRetry`、`createPermission` は `request` のみ
- ICE からの利用: `packages/ice/src/ice.ts` が TURN で relay candidate を gather。`forceTurn` 時は host 等を落として relay のみ利用
- サーバ側実装 `packages/ice-server` の permission/channel 管理は **クライアント汚染バグの主因ではない**（今回の修正対象は主にクライアント `TurnProtocol`）

### 推奨設計方針

単純な `finally { creatingPermission = undefined }` だけでは不十分。peer 固有の in-flight / cache と allocation 共通 queue を分離する。

```text
allocation-global:
  permissionQueue / channelBindQueue（直列・rejection 非伝播）
  transport, nonce, realm, integrityKey, channelNumber カウンタ

peer-specific:
  creatingPermissionByAddr / channelBindingByAddr
  permissionByAddr[ip]
  channelByAddr[ip:port] (+ refreshAt)
```

queue パターン（文書 §6.3）:

```ts
const operation = this.permissionQueue.then(() => doWork());
this.permissionQueue = operation.then(() => undefined, () => undefined);
return operation; // 呼び出し元には実 rejection を返す
```

### 試験レイヤ

| レイヤ | 手段 | 目的 |
|--------|------|------|
| Unit | mock request / 強制 reject | isolation・retry・dedupe・rollback |
| Local integration | `createLocalTurnServer` + `Connection` | UDP/TCP/TLS 非回帰 |
| Interop（完了必須） | Docker pion-turn + 例題/ICE | 第三者 TURN での実接続 |
| 既存 E2E（任意） | `e2e/` の NodeTurnServer 経路 | 上位スタック非回帰。pion 代替にはしない |

### 実装順序（レビュー分割の目安）

1. CreatePermission state isolation + unit tests  
2. ChannelBind state isolation + rollback/retry tests  
3. channel `refreshAt` の peer 化  
4. local NodeTurnServer 非回帰（既存 turn.test.ts）  
5. **pion TURN コンテナ相互接続試験**  
6. （任意）refresh interval 見直し

---

## 4. 考慮すべき制約や注意点

1. **認証 state の race**  
   `requestWithRetry` が `nonce` / `realm` / `integrityKey` を更新する。control request を無秩序に並列化しない。直列 queue + rejection 非伝播で十分。

2. **Permission key と Channel key の意味の差（RFC 8656）**  
   - Permission: peer IP 単位  
   - ChannelBind: peer transport address（IP + port）単位  
   key 関数を分離すること。

3. **provisional Channel mapping**  
   success 前の mapping 自体は正当（先着 ChannelData）。失敗時 rollback と number 非再利用をセットで守る。

4. **IPv6 address の key 表現**  
   `addr.join(":")` は IPv6 で曖昧になりうる。structured key を使う。

5. **TURN over TCP / TLS**  
   stream 向け framing（`padTurnFrame` / `splitTurnTcpFrames`）は維持。state 修正が transport 分岐を壊さないこと。

6. **pion コンテナの PUBLIC_IP**  
   `network_mode: host` でも `PUBLIC_IP` がクライアントから見えるアドレスと一致しないと relay が壊れる。ローカル検証では `127.0.0.1` または実際のホスト IP を明示。

7. **完了判定に pion を含める**  
   local `NodeTurnServer` は同一リポジトリ実装のため、クライアントバグの症状がサーバ側寛容さで隠れうる。**第三者実装（pion）での接続成功を完了条件に含める。**

8. **テスト規約（AGENTS.md）**  
   Arrange / Act / Assert。Act/Assert には日本語コメント。Arrange 共通化は `packages/ice/tests/utils.ts` 等に寄せる。

9. **破壊的 API 変更は避ける**  
   公開面は `createTurnClient` / `TurnProtocol` の振る舞い修正が中心。private state の内部再設計で足りる想定。public シグネチャを変える場合は型・doc を同期。

10. **Windows 非サポート**  
    ランタイム対象は Linux/macOS 等 Unix 系。pion Docker 検証もその前提。

---

## 5. 完了条件

### 機能・バグ修正

- [ ] 1 peer の `CreatePermission` rejection が別 peer に伝播しない
- [ ] 1 peer の `ChannelBind` rejection が別 peer に伝播しない
- [ ] rejected Promise が allocation-global state に残らない
- [ ] `CreatePermission` 成功前に permission cache を確定しない
- [ ] failed permission は後続呼び出しで retry できる
- [ ] failed initial ChannelBind の provisional mapping を rollback する
- [ ] failed ChannelBind は後続呼び出しで retry できる（channel number は再利用しない）
- [ ] same-peer concurrent operation を 1 transaction に集約する
- [ ] ChannelBind success より先に到着する ChannelData を処理可能（provisional mapping 維持）
- [ ] channel refresh state を peer/channel 単位で管理する
- [ ] 拒否 candidate の後に正常な relay candidate が試されても ICE が接続成功する

### 検証（コード）

- [ ] isolation / retry / rollback 用の unit または focused integration が追加され green
- [ ] `cd packages/ice && npm run type` が成功
- [ ] `cd packages/ice && npm test` が成功（既存 UDP/TCP/TLS turn 試験含む）
- [ ] 変更が `packages/ice` 外へ波及する場合はルート `npm run type` / `npm run test:small` も実施

### 検証（相互接続）— **必須**

- [ ] **werift 同士 + local `NodeTurnServer`（既存 turn.test.ts）** で relay 接続・双方向データが成功
- [ ] **pion TURN を Docker コンテナで起動**（`packages/ice/docker/pion-turn` 等）
- [ ] その pion TURN 経由で **werift ↔ werift の ICE（`forceTurn`）または TURN client 相互通信** が成功し、双方向データが届くことを確認
- [ ] 上記 pion 試験の手順・環境変数・結果（ログまたは自動テスト）を PR / チケットに記録

### 非目標（完了に不要）

- Permission 300s と channel refresh 500s の完全整合（follow-up 可）
- TURN control plane のフル並列化
- ブラウザ / Chrome E2E の必須化（既存経路の任意確認で可）

---

## 実装時の参照チェックリスト

- 根因・修正方針の全文: `werift-issue-667-root-cause-and-fix.md`
- クライアント実装: `packages/ice/src/turn/protocol.ts`
- 既存 ICE+TURN 試験: `packages/ice/tests/ice/turn.test.ts`, `packages/ice/tests/utils.ts`（`createLocalTurnServer`）
- pion Docker: `packages/ice/docker/pion-turn/`, `packages/ice/docker/pion-turn-tcp/`
- 手動スクリプト: `packages/ice/examples/ice_turn.ts`, `packages/ice/examples/turn_turn.ts`
- RFC: https://www.rfc-editor.org/rfc/rfc8656.html
