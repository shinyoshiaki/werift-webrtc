[**werift**](../README.md)

***

[werift](../globals.md) / kMaxProbeDelayMs

# Variable: kMaxProbeDelayMs

> `const` **kMaxProbeDelayMs**: `10` = `10`

pin BitrateProberConfig `max_probe_delay` default (10ms).
An active cluster whose next_probe_time is this late is discarded.
