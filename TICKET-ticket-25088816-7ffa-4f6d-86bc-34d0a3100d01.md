# GCC 実装の自律的な互換性レビュー・修正・完成

## 1. タスクの目的と背景

### 1.1 目的

対象ブランチ上の **werift GCC send-side BWE** を、次の **2 つの source of truth** だけを基準に網羅レビューし、問題を自律的に探索・修正・回帰テスト化し、完了条件をすべて満たすまで反復する。

1. **親チケット要件**  
   `TICKET-ticket-c724daf9-5f79-4e30-bd50-7b27c8951844.md`  
   （TWCC BWE 抽象化 + legacy / GCC 選択可能化 + イベント契約 + sim 要件）
2. **固定 pin の libwebrtc**  
   - commit: `0fda16159e33adf59c71a7ad1173dcbe5a632102`  
   - ローカル: `third_party/libwebrtc-ref/`（`PIN.json` / `MANIFEST.md`）  
   - 再取得: `python3 third_party/libwebrtc-ref/fetch_ref.py`  
   - Browse: https://webrtc.googlesource.com/src/+/0fda16159e33adf59c71a7ad1173dcbe5a632102/modules/congestion_controller/goog_cc/

**本タスクは「既知レビュー指摘の消化」ではない。**  
既知領域を直したあと、その修正が生んだ意味論差・lifetime 結合・clock 混在・cleanup 過早/漏れを再探索し、最終的に:

- **Blocker: 0**
- **Major: 0**
- GCC 主要 state transition に説明不能な pin 差がない
- Known Differences が重大な意味論差を隠していない
- 親チケット完了条件を回帰させていない

を満たすまで

```text
調査 → 問題特定 → 修正 → regression test 追加 → CI/test → 固定 pin 再比較 → self-review
```

を反復する。Minor も合理的に直せるものは残さない。

### 1.2 背景（親チケットで既に達成済み）

本ブランチ（`ticket/25088816-…`、起点は親実装 `ticket/c724daf9-…`）では次が **既に存在する**。本タスクは再実装ではなく **監査・是正・完成** である。

| 領域 | 状態 | 主なパス |
| --- | --- | --- |
| 共通 interface | 実装済 | `packages/webrtc/src/media/sender/bandwidthEstimator.ts`（I/O + `availableBitrate` + `onAvailableBitrate` のみ。probe は `ProbePacingController`） |
| 変化時のみ通知 | 実装済 | `setAvailableBitrateIfChanged` |
| legacy 推定 | 実装済 | `estimators/legacyCumulativeBwe.ts`（`SenderBandwidthEstimator` alias）+ `onCongestion*` 具象固有 |
| GCC | 実装済 | `estimators/gcc/*`（Trendline / InterArrival / AIMD / LossBasedBweV2 系 / ProbeController / RobustThroughput / BandwidthLimitedCause） |
| 差し替え API | 実装済 | `RTCRtpSender.setBandwidthEstimator` + bridge + probe padding / pacing |
| 単体テスト | 厚め | `tests/media/bandwidthEstimator.test.ts`（約 3770 行、Blocker 回帰多数） |
| peer sim（CI 外） | 実装済 | `packages/webrtc/simulations/` → `npm run test:sim` |
| Chrome sim（CI 外） | 実装済 | `e2e/simulations/` → `cd e2e && npm run test:sim` |
| 既知差分 | 文書化 | `GCC_KNOWN_DIFFERENCES`（`estimators/gcc/constants.ts`） |
| libwebrtc pin | 固定済 | `third_party/libwebrtc-ref/` @ `0fda1615…` |

### 1.3 過去レビューで潰された領域（再発防止の確認対象）

履歴上 Blocker として修正済み。**現状コードでも真に解消されているか**を最初に確認する。消化だけで完了としない。

| 領域 | 期待（pin / 現状方針） | 主な実装 / テスト |
| --- | --- | --- |
| Probe FIFO | send-fill（minBytes ∧ minPackets）で次 cluster。ACK 成功では pacing を進めない | `probeController.ts` / Blocker regressions |
| 80% ACK | 推定成立の下限。pacing 早期終了なし。以後も estimate 再計算 | 同上 |
| 3x→6x FIFO | multi-active 禁止、6x は queue | 同上 |
| initial 全失敗後 recovery | complete 後に recovery 可能 | 同上 |
| pacing 5s / result 1s | 寿命分離 | `kProbePacingTimeoutMs` / `kProbeResultTimeoutMs` |
| controller vs estimator history | result-wait 終了で seq map を消さない。late TWCC 可。sender 10s / receive 1s で有界 | `estimatorHistory` + `kSentInfoMaxAgeMs` |
| zero-packet timeout | history に入れない | 同上 |
| lower probe result | 無視しない + acked×0.85 floor | `gccBwe.ts` |
| mid-cluster abort | partial TWCC loss/overuse で active probe を abort しない | `congestion feedback は active probe を abort しない` |
| probe 適用 | full `reset` ではなく `aimd.setEstimate` / `lossBwe.setBandwidthEstimate` | state-preserving 回帰 |
| acked bitrate | RobustThroughputEstimator defaults（単純 1s 窓ではない） | `acknowledgedBitrateEstimator.ts` |
| probe gating | `GetBandwidthLimitedCause` 相当。単一 `congested` boolean で潰さない | `bandwidthLimitedCause.ts` + 対応表テスト |
| RTT | max_feedback_rtt = arrival − earliest received send。>3s は probe 禁止のみ（pin 外の ×0.8 drop なし） | `gccBwe.ts` / RTT 回帰群 |
| source RTP + padding | reorder 後も sequence 衝突なし、SR octet 非算入 | rtpSender 回帰 |
| TWCC 24-bit ref unwrap | continuous timeline | `TwccReferenceTimeUnwrapper` |
| soft loss | not-received 永久 finalize しない / 二重計上しない | late correction 回帰 |

### 1.4 親チケットとの関係

| 項目 | 値 |
| --- | --- |
| 親チケット | `c724daf9-5f79-4e30-bd50-7b27c8951844` |
| 親の主題 | TWCC BWE 抽象化 + 現状実装 / GCC 選択 + `onAvailableBitrate` 明確化 |
| 本チケット | 親実装後 follow-up の **自律互換レビュー・修正・完成**（過去子タスクと同系統の再監査サイクル） |
| ベース | `develop`（本 worktree 調査時点: develop に対して **behind 0 / ahead 多数**。最終段階で再確認） |
| 非ゴール | 親の interface 再設計、default を GCC に切替、第三アルゴリズム、C++ バインド、REMB 必須化、sim の CI 必須化 |

---

## 2. 実装すべき具体的な機能や変更内容

「新機能追加」が主目的ではない。以下のサイクルを自律実行する。

### 2.1 必須作業フロー

1. **現状把握**  
   親 MD 完了条件、`GCC_KNOWN_DIFFERENCES`、`estimators/gcc/**`、`rtpSender` / `receiverTwcc` / TWCC、`bandwidthEstimator.test.ts`、`develop...HEAD` 差分
2. **固定 pin 突合**  
   `third_party/libwebrtc-ref/` のみ。Web の未ピン main や別 revision を互換判断に使わない
3. **既知問題の再確認**（§2.2）— 直しただけで終わらない
4. **subsystem 横断の自律探索**（§2.3）
5. **状態遷移表**を作り pin と werift を並べる（§2.4）
6. **修正前に落ちる最小 regression**（可能な限り public full-path）
7. **修正**（意味論は原則 pin へ。Known Differences 逃げは厳格）
8. **targeted test → package type → 広い validation → sim**
9. **self-review pass**（実装者前提を捨てる）— 問題があれば 6〜8 を繰り返す
10. **develop 差分評価**（behind なら内容確認・必要なら取り込み後に再検証）
11. **docs / Known Differences / ticket 本文**の実装一致

### 2.2 最初に確認する既知問題（Probe / gating）

#### Probe lifetime

| チェック | pin / 期待 | 確認コード |
| --- | --- | --- |
| mid-cluster abort しない | partial loss/overuse で pacing 中 cluster を捨てない | `gccBwe.ts`（abort 経路が無いこと） |
| pacing vs result-wait | 5s / 1s 独立 | `probeController.process` |
| controller vs estimator history | timeout 後も late TWCC で result 可 | `awaitingResults` → `estimatorHistory` |
| zero-packet | history に残さない | `process` pacing timeout 分岐 |
| no-ACK / partial | sender-side finite lifetime で回収 | `pruneEstimatorHistoryBySendAge` |
| 80% 後 | 残り ACK で refinement | `resultAccepted` + 継続 `onAckedPacket` |
| exact-max | target ≥ max_probe → clamp + `probe_further=false`（`>=`） | `enqueueClusters` / `requestProbe` / `setEstimatedBitrate` |
| lower-result guard | acked×0.85 floor、overuse でも下降は適用可 | `gccBwe` probe apply |

#### Probe state gating（必須比較表）

`const congested = usage === "overuse"` のような **1 bit 潰し**で、libwebrtc の複数 cause を壊していないかを最優先で確認する。

pin 一次ソース:

- `goog_cc_network_control.cc` … `GetBandwidthLimitedCause`
- `probe_controller.cc` … `InitiateProbing` / `SetEstimatedBitrate` / `RequestProbe`
- `delay_based_bwe` / AIMD / LossBasedV2 連携（`GoogCcNetworkController`）

werift:

- `bandwidthLimitedCause.ts` … `getBandwidthLimitedCause` / `isProbeInitiationAllowed` / `maxProbeBitrateBps`
- `gccBwe.ts` … further / recovery / upward vs downward apply
- `probeController.ts` … session / cooldown / further threshold

**必須: Delay × Loss × Controller の比較表を作業中に更新し、差異は正当化できない限り修正する。**

### 2.3 既知以外の subsystem レビュー対象（最低限すべて）

#### Feedback preprocessing

- TWCC sequence unwrap、reference time unwrap（24-bit）、16-bit wrap
- receive-time ordering vs send-time ordering vs transport-seq ordering
- duplicate / overlapping feedback
- late loss correction、soft loss、unknown sequence
- `hasTwccReceiveTiming`（`receivedAtMs === 0` 有効 vs `ReceivedWithoutDelta`）

#### Acknowledged bitrate

- RobustThroughputEstimator default（window 20 / min 1s / max 5s / required 10 / largest-gap replace）
- send/receive rate、reorder、stale
- ALR / prior-unacked-data 省略の影響が major control を変えていないか

#### Delay-based BWE

- InterArrivalDelta、Trendline、overuse/underuse/normal
- stream timeout、reordered packet
- AIMD、RTT、TimeToReduceFurther
- probe result の `setEstimate`（state preservation）

#### Loss-based BWE

- observation window / partial / loss correction / byte loss
- HOLD / decreasing / increasing / delay_based
- delayed increase、inherent loss、candidate bounds
- probe apply 時の history preservation（`setBandwidthEstimate`）

#### ProbeController / ProbeBitrateEstimator

- initial exponential、FIFO、pacing、send-fill、result wait
- estimator history、recovery、further、exact max
- loss/delay/RTT gating、cooldown、timeout、late ACK
- cleanup、sequence ownership、result refinement

#### Sender integration

- media vs padding の RTP sequence / transport-wide sequence
- reorder、retransmission、RTP cache
- padding accounting、SR packet/octet counters
- async padding injection
- estimator reset / reuse、event firing、disposal / cleanup

### 2.4 探索すべきバグパターン（grep だけでなく state を追う）

| パターン | 何を疑うか |
| --- | --- |
| Clock domain | 同一比較に sender wall / TWCC receive timeline / RTP send ts / feedback arrival が混在 |
| Lifetime coupling | sentInfos / ProbeController session / BitrateProber pacing / ProbeBitrateEstimator history が単一 timeout に結合 |
| Premature cleanup | controller 完了で estimator 削除、80% で残り破棄、partial feedback で cluster 破棄 |
| Missing cleanup | no ACK / zero send / never reported / repeated recovery で map 永久保持 → 長時間で有界か |
| State collapsing | congested 1bit / loss-limited 1bit で probe 許可や rate control が pin と違う |
| Known Differences misuse | 「書いてあるから OK」禁止。チケット許容・TS 不可避・主要応答不変の3条件 |

### 2.5 状態遷移テーブル（作業成果物・最低 Probe 周辺）

作業中に作成・更新する（最終報告にも残す）。テンプレ:

| Delay state | Loss state | RTT high? | Controller | libwebrtc Initiate / Request / result apply | werift | Match |
| --- | --- | --- | --- | --- | --- | --- |
| normal | delay_based | no | complete | recovery/further allow (uncapped except max) | （実装確認） | ? |
| underuse | * | * | complete | no new probe (cause=delay_increased) | | ? |
| overuse | * | * | complete | no new probe; upward result ignore; lower+floor ok | | ? |
| normal | decreasing | no | complete | no probe (kLossLimitedBwe) | | ? |
| normal | hold / IncreaseUsingPadding | no | complete | no probe (kLossLimitedBwe) | hold→forbid; padding は increasing 折り畳み注意 | ? |
| normal | increasing | no | complete | allow, max_probe=est×1.5 | | ? |
| normal | * | yes | complete | no probe (kRttBasedBackOffHighRtt) | | ? |

**結果適用（pin 整合の現状方針）:**

| 条件 | upward probe result | downward probe result |
| --- | --- | --- |
| usage=overuse | 適用しない | acked×0.85 floor 付きで適用可 |
| usage=underuse/normal | 条件付き適用（initial は acked×2 soft cap、recovery は target×1.5 等） | 同上 |
| 新規 probe 許可 | `isProbeInitiationAllowed(cause)` のみ | （結果適用とは独立） |

### 2.6 テスト方針

- 修正時は **再現 regression を先または同時** に追加
- private field 直書きだけでは不十分。可能な限り:

```text
RTP sent → pacing → TWCC feedback → estimator → state transition → availableBitrate / next probe
```

#### 最低限 追加・維持するケース

**Probe state**

- underuse 中は新規/further なし
- underuse→normal で recovery 可
- overuse 中は upward 不適用
- lower valid + guard
- loss decreasing/hold で probe なし
- loss increasing は est×1.5 cap（固定 pin 対応）

**Lifetime**

- controller timeout 後 late TWCC result
- timeout で further session が再オープンしない
- receive-time horizon で estimator cluster prune
- no-ACK が sender finite lifetime 後に消える
- zero-packet pacing timeout が leak しない
- repeated recovery でも map/history size bounded

**Boundary**

- `== max` / `== timeout` / `timeout+1`
- 79% / 80% / 100% ACK
- seq `65535→0`
- TWCC reference time wrap
- exact loss / state transition thresholds
- RTT `== 3000` vs `> 3000`（`isRttAboveLimit` は `>`）

規約: Arrange / Act / Assert、共有 Arrange は utility 化、Act/Assert に日本語コメント。

### 2.7 修正後の自律 self-review

テスト通過で終了しない。別 reviewer として diff を読み直す。

問い:

- 別 state に probe を許可してしまったか
- cleanup 遅延で leak / 早期で late feedback 破棄
- sender clock と receiver timeline 混在
- controller と estimator lifetime の再結合
- `>` vs `>=` が pin と一致するか
- Known Differences 文面とコード一致
- docs が古い挙動のまま
- テストが実装追認だけ

**1 fix したら周囲を最低 1 段広く見る**  
例: ProbeController timeout → ProbeBitrateEstimator → GccBandwidthEstimator → RTCRtpSender pacing

### 2.8 ドキュメント同期

コード修正後に一致確認:

- `GCC_KNOWN_DIFFERENCES`（`constants.ts`）
- API / ProbeController JSDoc
- `packages/webrtc/simulations/README.md`、e2e sim README
- 本チケット Markdown（実装と矛盾する記述を放置しない）
- 生成ツール由来のゴミ文が accidentally commit されていないか

### 2.9 develop との差分

最終段階:

```bash
git fetch origin develop  # 可能な場合
git rev-list --left-right --count develop...HEAD
git log --oneline HEAD..develop
git log --oneline develop..HEAD
```

- behind なら develop 変更の競合・回帰・依存を評価
- 取り込みが必要なら取り込み後に CI / relevant tests 再実行
- 本調査時点: **develop に behind 0 / ahead あり**（作業終了時に再計測）

### 2.10 非ゴール（スコープ外）

- 親 interface 契約の大幅変更、default=GCC
- NADA / SCReAM
- C++ バインディング / ネイティブ addon
- REMB 統合の必須化
- Chrome と bit 完全一致
- sim の CI 必須化
- 好みだけの大規模リファクタ

---

## 3. 技術的な実装アプローチ（調査結果の要約）

### 3.1 データフロー（現状）

```text
RTCRtpSender.sendRtp
  ├─ transport-wide seq (RTCDtlsTransport)
  ├─ optional probe padding / token-bucket (ProbePacingController)
  └─ BandwidthEstimator.rtpPacketSent(SentInfo)

Remote ReceiverTWCC → RTCP TransportWideCC
  └─ handleRtcpPacket → estimator.receiveTWCC

GccBandwidthEstimator.receiveTWCC
  ├─ TwccReferenceTimeUnwrapper + sort by wideSeq（loss/soft-loss 経路）
  ├─ receive-time order で acked / probe ACK / inter-arrival
  ├─ Trendline → usage
  ├─ AimdRateControl + LossBasedBwe
  ├─ getBandwidthLimitedCause → further/recovery gate + max_probe
  ├─ ProbeController result apply (setEstimate / setBandwidthEstimate)
  └─ target = min(delay, loss) ± probe → setAvailableBitrateIfChanged
```

### 3.2 モジュール対応（werift ↔ pin）

| werift | pin (`third_party/libwebrtc-ref/…`) | 調査メモ |
| --- | --- | --- |
| `gccBwe.ts` | `goog_cc/goog_cc_network_control.*` | 合成・probe 適用・RTT proxy・cause |
| `bandwidthLimitedCause.ts` | `GetBandwidthLimitedCause` + `InitiateProbing` switch | underuse/overuse 同一 cause；RTT；loss states |
| `interArrivalDelta.ts` | `inter_arrival_delta.*` | group latest send delta |
| `trendlineEstimator.ts` | `trendline_estimator.*` | Detect 内包 |
| `overuseDetector.ts` | （歴史的） | **runtime dead class**；型のみ |
| `aimdRateControl.ts` | `remote_bitrate_estimator/aimd_rate_control.*` | setEstimate / TimeToReduceFurther |
| `lossBasedBwe.ts` | `loss_based_bwe_v2.*` | V1 は pin に無し |
| `acknowledgedBitrateEstimator.ts` | robust + acked interface | default robust path |
| `probeController.ts` | `probe_controller` + `probe_bitrate_estimator` + `pacing/bitrate_prober` | 3 分割を 1 クラス統合 |
| `rtpSender` pacing | PacedSender 軽量代替 | Known Differences |
| TWCC | `transport_feedback_adapter/demuxer` | 配線・soft loss |

### 3.3 pin 上の probe gating（突合の錨）

`GetBandwidthLimitedCause`（pin）:

| Delay usage | RTT high | LossBasedState | Cause | InitiateProbing |
| --- | --- | --- | --- | --- |
| overuse / underuse | * | * | `kDelayBasedLimitedDelayIncreased` | **empty** |
| normal | yes | * | `kRttBasedBackOffHighRtt` | **empty** |
| normal | no | decreasing / IncreaseUsingPadding | `kLossLimitedBwe` | **empty** |
| normal | no | increasing | `kLossLimitedBweIncreasing` | allow, max×`loss_limited_probe_scale`(1.5) |
| normal | no | delay_based | `kDelayBasedLimited` | allow, configured max only |

werift は `hold` を loss state として持ち `kLossLimitedBwe` 相当（forbid）に map。`IncreaseUsingPadding` は rate 上 `increasing` に折り畳みつつ HOLD 中は forbid — **折り畳みが一貫しているか再検証必須**。

`RequestProbe`（pin）は ALR / rapid recovery / large drop 経路が主。werift の recovery は **underuse→normal**（`recovered_from_overuse` 相当）+ cooldown。ALR-only 経路は Known Differences。意味論上「回復直後に探索できる」ことと gating の pin 一致が主眼。

### 3.4 現状実装の健全性（調査時点スナップショット）

**既に揃っている強い点（再確認対象）:**

- RobustThroughputEstimator 経路
- probe apply の state-preserving `setEstimate` / `setBandwidthEstimate`
- pacing / result-wait / estimator history の分離
- GetBandwidthLimitedCause 相当の cause 関数と unit 対応表テスト
- mid-abort しない方針と回帰
- late TWCC / bounded memory 回帰群

**残リスク（自律探索で優先）:**

1. **Cause 折り畳みと loss state 命名** — padding/hold/increasing の境界が pin の `kIncreaseUsingPadding` とずれて probe 許可が変わらないか
2. **further 判定の state 条件** — pin の `SetEstimatedBitrate` は主に `kWaitingForProbingResult`。werift は `complete || waiting_for_result` + planned threshold。余分な further が開かないか
3. **ordering** — loss は wideSeq 順、delay/acked/probe は receive-time 順。経路取り違えがないか
4. **clock** — probe timeout は sender clock、rate は TWCC recv、history prune は両系。比較の混在がないか
5. **OveruseDetector dead code** — 二重実装の混乱源。削除/統合/テスト専用の整理は Minor でも合理的なら実施
6. **Known Differences の長大 1 エントリ** — 実装と 1:1 か。意味論差の隠蔽になっていないか
7. **sim / e2e** — unit だけでは完了にしない。実行不能時は理由と未確認範囲を報告
8. **multi-sender** — transport-wide 共有 vs BWE per-sender（親制約）。二重計上バグのみ修正

### 3.5 推奨作業順

1. 親 MD §完了条件 + `PIN.json` + `GCC_KNOWN_DIFFERENCES` + `bandwidthLimitedCause` + `gccBwe` probe 節 + Blocker describe を通読  
2. pin の `GetBandwidthLimitedCause` / `InitiateProbing` / `SetEstimatedBitrate` / `RequestProbe` をローカルファイルで精読  
3. 状態遷移表を埋める → 不一致をチケット化（内部メモ）して regression → fix  
4. Feedback / acked / delay / loss / sender を subsystem 単位で pin 突合  
5. lifetime / clock / cleanup を長時間有界の観点で追う  
6. `npx vitest run tests/media/bandwidthEstimator.test.ts` + 近傍 + `npm run type`  
7. `npm run test:sim`、可能なら `cd e2e && npm run test:sim`  
8. 範囲に応じ `npm run test:small` または `npm run ci`  
9. develop 差分、docs 同期、self-review、最終報告

### 3.6 検証コマンド

| 目的 | コマンド |
| --- | --- |
| BWE 単体 | `cd packages/webrtc && npx vitest run tests/media/bandwidthEstimator.test.ts` |
| TWCC 近傍 | `cd packages/webrtc && npx vitest run tests/media/receiverTwcc.test.ts tests/media/rtpSender.test.ts` |
| type | `cd packages/webrtc && npm run type` |
| package tests | `cd packages/webrtc && npm test` |
| peer sim（CI 外） | `cd packages/webrtc && npm run test:sim` |
| Chrome sim（CI 外） | `cd e2e && npm run test:sim`（ブラウザ依存。環境失敗とコード失敗を区別） |
| 広い CI | ルート `npm run type` / `npm run test:small` / 必要時 `npm run ci` |

失敗時: 原因調査 → 修正 → **同じコマンド再実行** を成功まで。

---

## 4. 考慮すべき制約や注意点

1. **Source of truth を混ぜない**  
   互換判断は `third_party/libwebrtc-ref` + `PIN.json` の revision のみ。別 revision を読んだら判断に使わない。

2. **親チケット完了条件を縮小しない**  
   interface 薄さ、default=legacy、変化時 bps 通知、setter、delay/loss/probe、sim 要件を回帰させない。

3. **プロトコルとアルゴリズム**  
   TWCC は feedback。Receiver を GCC 専用化しない（dumb receiver + 正しい loss 報告）。

4. **pure TypeScript**  
   ネイティブ依存禁止。`libwebrtc-ref` は参照専用（build/link しない）。

5. **単位と時計**  
   bitrate=bps、時刻=ms。sender wall と TWCC timeline を同一比較に載せない。

6. **Known Differences 規律**  
   残してよいのは (a) チケット許容 (b) TS/制約上不可避 (c) 主要 control response を変えない、の交差。意味論差は原則修正。

7. **テスト規約**  
   失敗握りつぶし禁止。指摘通しの特殊 case 実装を避ける。

8. **sim は CI 対象外だが完了条件の一部**  
   unit 置換だけで完了扱いにしない。実行不能時は理由・未確認範囲を明示。

9. **スコープ制御**  
   チケット内の欠陥はユーザー確認待ちせず修正。好み・大規模リファクタ・チケット外は広げない。

10. **Windows 非対応**  
    Unix 前提を増やさない。

11. **コミット**  
    無関係ファイル改変・force-push を避ける。エージェント由来のチケット汚染文を commit しない。

### 4.1 self-review で必ず探すこと

- 1 fix が別 transition を壊していないか  
- clock / ordering の暗黙化  
- cleanup 過早 / 過遅  
- lifetime 再結合  
- テストの実装追認  
- Known Differences への隠蔽  
- **追加問題を探すだけの pass を最低 1 回**

---

## 5. 完了条件

以下を **すべて**満たすまで完了と宣言しない。

### 5.1 レビュー・品質

- [ ] 親チケット acceptance を回帰させていない（interface / default legacy / 変化時 bps / setter / legacy 固有イベント）
- [ ] ピン済み libwebrtc（`third_party/libwebrtc-ref` / `0fda1615…`）と **体系的に**突合済み（§2.2 / §2.3 / §3.2 最低限）
- [ ] 既知 Blocker 領域が再発していない（§1.3）
- [ ] Probe 周辺の **状態遷移表**を pin と照合し、説明不能差がない
- [ ] state-preserving であるべき更新で不必要な full reset をしていない（または正当化済み）
- [ ] packet reorder / loss / late feedback / wrap / probe 経路のテストが有効
- [ ] lifetime が有界（controller / estimator / sentInfos / seq maps）
- [ ] clock-domain 混在バグがない
- [ ] premature cleanup / lifetime coupling がない
- [ ] 自律発見問題に修正前に落ちる regression がある（ゼロなら調査内容と「差がない」根拠を報告）
- [ ] `GCC_KNOWN_DIFFERENCES` と実装・コメントが一致。意味論隠蔽に使っていない
- [ ] docs / sim README / JSDoc が実装と一致
- [ ] develop 差分を評価済み（必要なら取り込み + 再検証）
- [ ] 最終 self-review で新たな Blocker / Major を自分で発見できない
- [ ] **Blocker: 0 / Major: 0**（1 件でも残る場合は「実装完了」と報告しない）

### 5.2 検証コマンド

- [ ] `cd packages/webrtc && npx vitest run tests/media/bandwidthEstimator.test.ts`（+ 触った近傍）
- [ ] `cd packages/webrtc && npm run type`
- [ ] 変更範囲に応じ `npm run test:small` または `npm run ci`
- [ ] `cd packages/webrtc && npm run test:sim`
- [ ] 可能なら `cd e2e && npm run test:sim`（不可なら理由と未確認範囲を報告）

### 5.3 最終報告フォーマット（実装エージェント出力）

1. **発見した問題一覧**（Blocker / Major / Minor、原因、固定 pin との差）
2. **実施した修正**
3. **追加した regression tests**
4. **固定 pin と比較した主要 state transition の最終結果**（表）
5. **CI / simulation / interoperability の実行結果**
6. **Known Differences として残した差異と許容理由**
7. **最終 self-review**  
   - Blocker: 0  
   - Major: 0  
   - Minor: N
8. **完了条件チェックリスト**（本節の転記）

---

## 参考: 必読ファイル

| # | パス |
| --- | --- |
| 1 | `TICKET-ticket-c724daf9-5f79-4e30-bd50-7b27c8951844.md` |
| 2 | `packages/webrtc/src/media/sender/bandwidthEstimator.ts` |
| 3 | `packages/webrtc/src/media/sender/estimators/gcc/**` |
| 4 | `packages/webrtc/src/media/rtpSender.ts` |
| 5 | `packages/webrtc/src/media/receiver/receiverTwcc.ts` |
| 6 | `packages/rtp/src/rtcp/rtpfb/twcc.ts` |
| 7 | `packages/webrtc/tests/media/bandwidthEstimator.test.ts` |
| 8 | `packages/webrtc/tests/media/receiverTwcc.test.ts` |
| 9 | `packages/webrtc/simulations/**` |
| 10 | `e2e/simulations/**` |
| 11 | `GCC_KNOWN_DIFFERENCES`（`estimators/gcc/constants.ts`） |
| 12 | `examples/mediachannel/simulcast/abr.ts`, `examples/mediachannel/twcc/offer.ts` |
| 13 | `third_party/libwebrtc-ref/`（`PIN.json` / `MANIFEST.md` / `fetch_ref.py`） |

### 外部仕様

- TWCC: https://datatracker.ietf.org/doc/html/draft-holmer-rmcat-transport-wide-cc-extensions-01  
- GCC draft: https://datatracker.ietf.org/doc/html/draft-ietf-rmcat-gcc-02（draft と pin 乖離時は **pin 優先**）  
- libwebrtc pin: https://webrtc.googlesource.com/src/+/0fda16159e33adf59c71a7ad1173dcbe5a632102/

---

## 決定事項（詳細化で固定）

| 項目 | 決定 |
| --- | --- |
| 作業の性質 | 再実装ではなく **自律レビュー + 修正 + 完成** |
| 比較基準 | 親チケット + **固定 pin のみ**（未ピン Web 禁止） |
| 完了バー | Blocker/Major 0、説明不能 state 差なし、Known Differences 非隠蔽、親 acceptance 維持 |
| 既知修正のみ | **完了にならない**。再探索と self-review 必須 |
| テスト | 修正ごとに regression。public full-path 優先。境界値必須 |
| sim | unit 置換不可。peer sim 必須実施、Chrome sim は可能なら実施 |
| スコープ | チケット内欠陥は即修正。好みリファクタはしない |
