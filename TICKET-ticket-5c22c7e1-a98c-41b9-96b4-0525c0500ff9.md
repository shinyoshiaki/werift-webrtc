# Issue #708 修正タスク詳細: configurable SCTP outbound MTU / padding-aware fragmentation

- 対象 issue: [shinyoshiaki/werift-webrtc#708](https://github.com/shinyoshiaki/werift-webrtc/issues/708)
- 参考実装: [pion/sctp#476](https://github.com/pion/sctp/pull/476)
- Pion WebRTC 側 follow-up: [pion/webrtc#3476](https://github.com/pion/webrtc/pull/3476)
- 調査対象リビジョン: `be3dd12f`（`packages/sctp` 0.0.11 / `packages/webrtc` 0.24.4）

---

## 1. タスクの目的と背景

### 1.1 解決する問題

werift は DataChannel/SCTP message を `USERDATA_MAX_LENGTH = 1200` 固定で fragmentation する（`packages/sctp/src/sctp.ts:54`）。この 1200 は **user data payload 長**であり、SCTP packet の MTU ではない。実際に送信される最大 DATA packet は次の 1228 bytes になる。

```text
12 (SCTP common header)
+ 16 (DATA chunk header)
+ 1200 (user data)
= 1228 bytes
```

VPN / Tailscale / WireGuard / overlay / nested virtualization など実効 Path MTU が小さい経路では、この 1228-byte packet が black-hole し、DataChannel の大きなメッセージが stall する。downstream プロジェクトは `1200 -> 1024` の source patch で回避している。

Issue #708 の「`USERDATA_MAX_LENGTH` を設定可能にする」という要求を、**payload 長の公開ではなく SCTP outbound MTU の設定**として実装する。これは Pion SCTP #476 と同じ概念で、header と padding から最大 payload を導出する。

### 1.2 なぜ payload 長ではなく MTU を公開するのか

- 利用者が知りたいのは「1 fragment の payload 長」より「送信 SCTP packet の上限」。
- header/padding 計算を利用者へ押し付けない。
- Pion と同じ概念になり、相互理解しやすい。
- 将来 PMTU discovery / adaptive MTU を実装しやすい。
- DATA と将来の I-DATA で header size が違っても API を変えずに済む。
- `maxMessageSize`（DataChannel 1 message の論理上限、SDP `a=max-message-size` で交渉）とは別概念として整理できる。

### 1.3 Pion #476 の内容

- outbound SCTP MTU の既定値を `1228 -> 1191` に変更。
- `maxPayloadSizeForMTU()` を追加し、SCTP common header と DATA/I-DATA header を差し引く。
- 4-byte chunk padding を考慮して payload 上限を align down する。
- 小さすぎる MTU を validation で拒否する。
- interleaving 有効時は I-DATA header 差を反映する。
- serialized packet が MTU 以下になることをテストする。

Pion WebRTC #3476 でも SCTP の outbound MTU を RTP 用の `1200` から分離し、`1191` を既定にしている。

### 1.4 現在の実装（コードベース調査結果）

`USERDATA_MAX_LENGTH` は `packages/sctp/src/sctp.ts` の 12 箇所で使われている。fragmentation だけでなく congestion control の単位にも流用されている点が重要。

| 行 | 用途 | 現在の式 |
| --- | --- | --- |
| `sctp.ts:54` | 定義 | `const USERDATA_MAX_LENGTH = 1200` |
| `sctp.ts:138` | cwnd 初期値（class field initializer） | `private cwnd = 3 * USERDATA_MAX_LENGTH` |
| `sctp.ts:710` | slow start の増分上限 | `this.cwnd += Math.min(doneBytes, USERDATA_MAX_LENGTH)` |
| `sctp.ts:715` | congestion avoidance の増分 | `this.cwnd += USERDATA_MAX_LENGTH` |
| `sctp.ts:722` | loss 時 `ssthresh` 下限 | `Math.max(..., 4 * USERDATA_MAX_LENGTH)` |
| `sctp.ts:856` | fragment 数 | `Math.ceil(userData.length / USERDATA_MAX_LENGTH)` |
| `sctp.ts:875` | `DataChunk.userData` の slice 長 | `userData.slice(pos, pos + USERDATA_MAX_LENGTH)` |
| `sctp.ts:880` | slice 位置の前進 | `pos += USERDATA_MAX_LENGTH` |
| `sctp.ts:937-938` | burst size（fast recovery / 通常） | `2 * USERDATA_MAX_LENGTH` / `4 * USERDATA_MAX_LENGTH` |
| `sctp.ts:1156` | T3 timeout 時 `ssthresh` 下限 | `Math.max(..., 4 * USERDATA_MAX_LENGTH)` |
| `sctp.ts:1158` | T3 timeout 後の cwnd リセット | `this.cwnd = USERDATA_MAX_LENGTH` |

補足（調査で確定）:

- `ssthresh` の初期値は INIT / INIT_ACK の peer advertisedRwnd（`sctp.ts:328`, `sctp.ts:374`）であり、定数には依存していない。置換不要。
- SCTP は 1 packet = 1 chunk で送信する（`sendChunk` `sctp.ts:1379-1391`）。したがって outbound MTU 制約は「1 DATA chunk の直列化長」に帰着する。
- packet marshal 側の magic number は `packages/sctp/src/chunk.ts` に散在し、`sctp.ts` の fragmentation 計算と共有されていない。
  - `Chunk.bytes`: chunk header 4 bytes（`chunk.ts:48-62`）
  - `DataChunk.bytes`: `length = 16 + this.userData.length`（`chunk.ts:209-228`）
  - `padL()`: 4-byte 境界への padding（`chunk.ts:479-482`、非 export）
  - `parsePacket()`: common header 12 bytes 前提（`chunk.ts:512-547`）
  - `serializePacket()`: 8-byte header + 4-byte checksum + chunk（`chunk.ts:550-572`）

---

## 2. 実装すべき具体的な機能や変更内容

### 2.1 `packages/sctp/src/chunk.ts`: packet-size primitives

packet marshal の magic number を共有定数化し、MTU 計算と乖離しないようにする。

```ts
export const SCTP_PADDING_MULTIPLE = 4;
export const SCTP_COMMON_HEADER_SIZE = 12;
export const SCTP_CHUNK_HEADER_SIZE = 4;
export const SCTP_DATA_FIXED_HEADER_SIZE = 12;
export const SCTP_DATA_CHUNK_HEADER_SIZE =
  SCTP_CHUNK_HEADER_SIZE + SCTP_DATA_FIXED_HEADER_SIZE; // 16
```

- `padL()` を `SCTP_PADDING_MULTIPLE` ベースに変更する。
- `DataChunk.bytes` の `16` を `SCTP_DATA_CHUNK_HEADER_SIZE` に、`Chunk.bytes` の `4` を `SCTP_CHUNK_HEADER_SIZE` に置き換える（フィールド offset は不変）。
- `parsePacket` の `12` を `SCTP_COMMON_HEADER_SIZE` に、`serializePacket` の `Buffer.alloc(8)` を `SCTP_COMMON_HEADER_SIZE - 4` に置き換える。
- 既存の packet round-trip テスト（`packages/sctp/tests/packet.test.ts`）がそのまま通ることが条件。

### 2.2 `packages/sctp/src/sctp.ts`: MTU helper と validation

```ts
import {
  SCTP_COMMON_HEADER_SIZE,
  SCTP_DATA_CHUNK_HEADER_SIZE,
  SCTP_PADDING_MULTIPLE,
} from "./chunk";

export const DEFAULT_SCTP_MTU = 1191;

export interface SCTPOptions {
  mtu?: number;
}

export function maxPayloadSizeForMtu(
  mtu: number,
  dataChunkHeaderSize = SCTP_DATA_CHUNK_HEADER_SIZE,
): number {
  const headerSize = SCTP_COMMON_HEADER_SIZE + dataChunkHeaderSize;
  if (mtu <= headerSize) {
    return 0;
  }
  const payloadBudget = mtu - headerSize;
  return payloadBudget - (payloadBudget % SCTP_PADDING_MULTIPLE);
}

export function validateSctpMtu(mtu: number): void {
  if (!Number.isSafeInteger(mtu) || mtu <= 0) {
    throw new Error("SCTP MTU must be a positive integer");
  }
  if (maxPayloadSizeForMtu(mtu) === 0) {
    throw new Error("SCTP MTU is too small for a DATA chunk");
  }
}
```

重要点:

- 単純な `mtu - 28` ではなく 4-byte alignment を考慮する。最大 payload を 4 の倍数に align down することで、任意長の最終 fragment でも padding 後の packet が MTU 以下になる。
- `dataChunkHeaderSize` 引数は将来 I-DATA を追加した際に header size を差し替えるための拡張点（現時点の呼び出しは 1 引数）。
- `validateSctpMtu` は非数値（`NaN` / `Infinity` / 小数 / 文字列）と、`maxPayloadSizeForMtu(mtu) === 0`（実質 MTU 31 以下）を association 作成前に拒否する。

### 2.3 `SCTP` instance の state 化と 12 箇所の置換

```ts
export class SCTP {
  readonly mtu: number;
  private readonly maxPayloadSize: number;
  private cwnd: number; // field initializer をやめる

  constructor(
    public transport: Transport,
    public port = 5000,
    options: SCTPOptions = {},
  ) {
    this.mtu = options.mtu ?? DEFAULT_SCTP_MTU;
    validateSctpMtu(this.mtu);
    this.maxPayloadSize = maxPayloadSizeForMtu(this.mtu);
    this.cwnd = 3 * this.maxPayloadSize;

    this.localPort = this.port;
    this.transport.onData = (buf) => {
      this.handleData(buf);
    };
  }

  static client(transport: Transport, port = 5000, options: SCTPOptions = {}) {
    const sctp = new SCTP(transport, port, options);
    sctp.isServer = false;
    return sctp;
  }

  static server(transport: Transport, port = 5000, options: SCTPOptions = {}) {
    const sctp = new SCTP(transport, port, options);
    sctp.isServer = true;
    return sctp;
  }
}
```

置換一覧（2 章冒頭の表の各行を `this.maxPayloadSize` に置換）:

| 行 | 置換後 |
| --- | --- |
| `sctp.ts:138` | constructor 内 `this.cwnd = 3 * this.maxPayloadSize` |
| `sctp.ts:710` | `this.cwnd += Math.min(doneBytes, this.maxPayloadSize)` |
| `sctp.ts:715` | `this.cwnd += this.maxPayloadSize` |
| `sctp.ts:722` | `4 * this.maxPayloadSize` |
| `sctp.ts:856` | `Math.ceil(userData.length / this.maxPayloadSize)` |
| `sctp.ts:875` | `chunk.userData = userData.slice(pos, pos + this.maxPayloadSize)` |
| `sctp.ts:880` | `pos += this.maxPayloadSize` |
| `sctp.ts:937-938` | `2 * this.maxPayloadSize` / `4 * this.maxPayloadSize` |
| `sctp.ts:1156` | `4 * this.maxPayloadSize` |
| `sctp.ts:1158` | `this.cwnd = this.maxPayloadSize` |

congestion control algorithm 自体（Pion の `cwnd = min(4*MTU, max(2*MTU, 4380))` 等）は本タスクでは変更しない。既存実装の「1 congestion-control unit = 1 DATA payload」という構造だけを維持して単位を差し替える。

### 2.4 `packages/sctp/src/index.ts`: public export

```ts
export {
  SCTP,
  DEFAULT_SCTP_MTU,
  maxPayloadSizeForMtu,
  validateSctpMtu,
} from "./sctp";
export type { SCTPOptions } from "./sctp";
export {
  SCTP_COMMON_HEADER_SIZE,
  SCTP_CHUNK_HEADER_SIZE,
  SCTP_DATA_FIXED_HEADER_SIZE,
  SCTP_DATA_CHUNK_HEADER_SIZE,
  SCTP_PADDING_MULTIPLE,
} from "./chunk";
```

既存の `SCTP.client(transport)` / `SCTP.client(transport, 5000)` / `new SCTP(transport)` / `new SCTP(transport, 5000)` は source-compatible のまま。

### 2.5 `packages/webrtc/src/transport/sctp.ts`: options を保持し SCTP 再生成でも再利用する

現在の constructor は `(port = 5000, maxMessageSize = DEFAULT_MAX_MESSAGE_SIZE)`（`transport/sctp.ts:44-47`）。3 番目に `SCTPOptions` を追加する。

```ts
import {
  SCTP,
  SCTP_STATE,
  type SCTPOptions,
  type Transport,
} from "../../../sctp/src";

constructor(
  public port = 5000,
  public maxMessageSize = DEFAULT_MAX_MESSAGE_SIZE,
  private readonly sctpOptions: SCTPOptions = {},
) {}

setDtlsTransport(dtlsTransport: RTCDtlsTransport) {
  // ...
  this.sctp = new SCTP(
    new BridgeDtls(this.dtlsTransport),
    this.port,
    this.sctpOptions,
  );
  // ...
}
```

`setDtlsTransport` は bundle の都合で DTLS transport が差し替わると `SCTP` instance を作り直す（`peerConnection.ts:1004-1010` からも呼ばれる）。stored options を再適用しないと MTU 設定が消えるため、必ず `this.sctpOptions` を渡す。テストからの観測点として `sctpTransport.sctp.mtu` を使える。

### 2.6 `packages/webrtc/src/sctpManager.ts`: 生成口の拡張

```ts
createSctpTransport(maxMessageSize?: number, sctpOptions?: SCTPOptions) {
  const sctp = new RTCSctpTransport(5000, maxMessageSize, sctpOptions);
  // ...
}
```

`createDataChannel` 内の fallback `if (!this.sctpTransport) this.createSctpTransport()`（`sctpManager.ts:78-80`）は options なしで既定 MTU になる。`RTCPeerConnection.createDataChannel` は事前に `createSctpTransport()` を通るため実経路では config が反映される（フォールバック動作は現状維持で可）。

### 2.7 `packages/webrtc/src/peerConnection.ts`: config 追加と伝播

1. `PeerConfig` に追加（`peerConnection.ts:1254-1322`）:

```ts
export interface PeerConfig {
  // ...
  /**
   * SCTP outbound packet MTU used for DATA chunk fragmentation.
   * Defaults to DEFAULT_SCTP_MTU (1191), which yields 1160-byte user data.
   */
  sctp: {
    mtu: number;
  };
}
```

2. default config（`generateDefaultPeerConfig` `peerConnection.ts:1444-1484`）:

```ts
sctp: {
  mtu: DEFAULT_SCTP_MTU,
},
```

3. `setConfiguration` に validation と immutable-after-create rule を追加（`peerConnection.ts:365-469`）。`maxMessageSize` の検証（`peerConnection.ts:434-444`）と同じ位置に置く:

```ts
const requestedSctpMtu = normalizedConfig.sctp?.mtu;
if (requestedSctpMtu !== undefined) {
  validateSctpMtu(requestedSctpMtu);
  if (
    this.sctpManager?.sctpTransport &&
    requestedSctpMtu !== this.config.sctp.mtu
  ) {
    throw new Error(
      "sctp.mtu cannot be changed after SCTP transport creation",
    );
  }
}

deepMerge(this.config, normalizedConfig as Partial<PeerConfig>);

// input.sctp === {} のような部分指定でも default に正規化する
this.config.sctp = { mtu: this.config.sctp?.mtu ?? DEFAULT_SCTP_MTU };
```

- constructor は `setConfiguration` を `this.sctpManager` 生成前に呼ぶ（`peerConnection.ts:197-233`）ため、初回設定は immutability 判定に引っかからない。
- SCTP transport 作成後でも **同じ値の再指定は許可**する。
- invalid value は SCTP transport 作成前でも即 reject する（`validateSctpMtu`）。
- `getConfiguration()` は `clonePeerConfiguration`（`peerConnection.ts:1541-1569`）を通るため、`sctp: { ...config.sctp }` を追加して参照共有を防ぐ。

4. 生成経路に config を渡す（`peerConnection.ts:502-509`）:

```ts
private createSctpTransport() {
  const sctp = this.sctpManager.createSctpTransport(
    this.config.maxMessageSize,
    this.config.sctp,
  );
  const dtlsTransport = this.findOrCreateTransport();
  sctp.setDtlsTransport(dtlsTransport);
  return sctp;
}
```

経路全体:

```text
RTCPeerConnection.config.sctp.mtu
  -> RTCPeerConnection.createSctpTransport()
  -> SctpTransportManager.createSctpTransport(maxMessageSize, sctpOptions)
  -> new RTCSctpTransport(5000, maxMessageSize, sctpOptions)
  -> RTCSctpTransport.setDtlsTransport()
  -> new SCTP(new BridgeDtls(...), port, sctpOptions)
```

`maxMessageSize` の意味と SDP `a=max-message-size` の negotiation（`sdp.ts:533` / `sctpManager.ts:117-136`）は変更しない。

### 2.8 docs / changelog

- `packages/sctp/README.md` に `SCTP.client(transport, 5000, { mtu })` の例と default 1191 / derived payload 1160 を記載。
- `changelog.md` の `Unreleased` に behavior change（fragmentation と congestion-control 単位が 1200 -> 1160 に変わる）と新 API を記載。
- public API の TSDoc を追加した場合は `npm run doc` で `doc/` を再生成（repo 追跡対象）。

---

## 3. 技術的な実装アプローチと調査結果

### 3.1 サイズ計算の確定値

| SCTP MTU | payload budget | 4-byte align | 最大 DATA packet | 備考 |
| --- | --- | --- | --- | --- |
| 1191 | 1163 | 1160 | 1188 | 新 default（Pion #476 と同じ） |
| 1228 | 1200 | 1200 | 1228 | 現行 payload 1200 相当 |
| 1052 | 1024 | 1024 | 1052 | downstream の 1024 patch 相当 |
| 32 | 4 | 4 | 32 | 最小 usable MTU |
| 31 以下 | 3 以下 | 0 | - | validation で reject |

計算式: `maxPayloadSize = alignDown4(mtu - 12 - 16)`、packet 長 `= 12 + 16 + payload <= mtu`。

### 3.2 behavior change（既定値変更の影響）

default `mtu = 1191` により `maxPayloadSize = 1160` となり、congestion-control の単位が次のように変わる。release note に記載する。

| 項目 | 変更前 | 変更後 |
| --- | --- | --- |
| cwnd 初期値 | 3600 | 3480 |
| slow start 増分上限 | 1200 | 1160 |
| congestion avoidance 増分 | 1200 | 1160 |
| loss 時 ssthresh 下限 | 4800 | 4640 |
| normal burst | 4800 | 4640 |
| fast recovery burst | 2400 | 2320 |
| T3 timeout 後 cwnd | 1200 | 1160 |

### 3.3 テスト実装方針

既存テストの Arrange を再利用する。Act / Assert には日本語コメントを付ける（repo 規約）。

#### A. helper / validation（新規 `packages/sctp/tests/mtu.test.ts`）

- `maxPayloadSizeForMtu`: `1191 -> 1160`, `1228 -> 1200`, `1052 -> 1024`, `32 -> 4`。
- `validateSctpMtu`: `0` / 負値 / `NaN` / `Infinity` / `1.5` / `28` / `29` / `31` を reject、`32` を accept。
- public export は `packages/sctp/tests/api.test.ts` の既存スタイルで確認してもよい。

#### B. serialized packet が MTU を超えない（`mtu.test.ts` または `packet.test.ts`）

```ts
const chunk = new DataChunk(0x03, undefined);
chunk.userData = Buffer.alloc(maxPayloadSizeForMtu(mtu));
const packet = serializePacket(5000, 5001, 0, chunk);
expect(packet.length).toBeLessThanOrEqual(mtu);
```

- `mtu = 1191` で `packet.length === 1188` も確認。
- padding 境界: final payload `1157` / `1158` / `1159` / `1160` のいずれも padding 後 packet `<= 1191`。

#### C-F. fragmentation / custom MTU / ordered / partially reliable（`packages/sctp/tests/sctp.test.ts` に追加）

`createMockSctp`（`sctp.test.ts:65-73`）と同じ fake `Transport` で送信 packet を capture する。`parsePacket` は checksum を検証するため、そのまま reassemble に使える。

- Arrange: `const packets: Buffer[] = []; const transport = { send: vi.fn(async (b) => packets.push(b)), close: vi.fn() }; const sctp = SCTP.client(transport, 5000, { mtu }); sctp.setRemotePort(5000);`
- default `mtu = 1191`: `1160 -> 1 chunk`, `1161 -> 2`, `2320 -> 2`, `2321 -> 3`。
- 各 packet が `<= 1191`、reassemble 結果が元 message と一致。
- custom `mtu = 1052`: `1025 -> 2 chunks`（先頭 1024、末尾 1）。
- ordered / unordered で chunk 数と size 上限が同一。unordered は全 fragment の flag に `SCTP_DATA_UNORDERED (0x04)` が立つのみの差。
- reliable / `maxRetransmits` / `expiry` で fragmentation 上限が変わらない。
- 注意: 既定 cwnd 内（例: 最大 3 fragments = 3480 bytes）に収まる payload を使う。`peerRwnd` は既定 1 MiB で問題なし。`setRemotePort` 未設定だと `sendChunk` が `invalid remote port` で失敗する。

#### G-H. WebRTC config propagation / setConfiguration（新規 `packages/webrtc/tests/issue/708.test.ts` + `packages/webrtc/tests/transport/sctp.test.ts`）

- `new RTCSctpTransport(5000, undefined, { mtu: 1052 })` に `dtlsTransportPair()`（`tests/fixture.ts:129-150`）で transport を設定し、`sctp.mtu === 1052`。
- `new RTCPeerConnection({ sctp: { mtu: 1052 } })` + `createDataChannel("dc")` で `pc.sctpTransport!.sctp.mtu === 1052`。default は `DEFAULT_SCTP_MTU === 1191`。
- `setConfiguration`: transport 作成前の変更 success / 作成後の同値 success / 作成後の異値 reject / invalid MTU reject。`getConfiguration().sctp.mtu` の反映も確認。
- 既存の `maxMessageSize` テスト（`tests/integrate/peerConnection.test.ts:657-685`）と `createDataChannelPair`（`tests/utils.ts:9-67`）の Arrange を再利用する。

#### I. browser interoperability（`e2e/tests/datachannel/datachannel.test.ts`）

- `e2e/server/index.ts` は `../../packages/webrtc/src` を export しているため、ローカル変更が E2E server にそのまま乗る。
- server handler（`e2e/server/handler/datachannel/datachannel.ts`）に binary echo 用 handler を追加し、`e2e/server/main.ts` の `tests` map（`main.ts:148` 付近）へ登録する。
- browser test で `Uint8Array` payload を送受信し byte 一致を確認する。少なくとも:
  - `> 1160` bytes（fragmentation を強制）
  - `60000` bytes 程度（`maxMessageSize` default 65536 の範囲内）
  - ordered DataChannel。unordered は E2E が安定している場合のみ追加。
- DTLS 暗号化後の packet size 検査は不要。packet sizing は low-level unit test で担保する。
- 実行: `npm run e2e:verbose`（初回は `npm run e2e` が Chromium を導入）。

### 3.4 実装順序

1. Step 1: `chunk.ts` の size constants + `maxPayloadSizeForMtu` / `validateSctpMtu` + helper/serialization tests。
2. Step 2: `SCTPOptions` / `DEFAULT_SCTP_MTU` / constructor・factory / instance state / 12 箇所置換 / fragmentation・congestion の unit tests。
3. Step 3: `PeerConfig.sctp.mtu` / default / validation / propagation / `setConfiguration` / `getConfiguration` clone / propagation tests。
4. Step 4: E2E、`packages/sctp/README.md`、changelog、必要なら typedoc 再生成。

---

## 4. 考慮すべき制約や注意点

1. **`deepMerge` は nested object を deep merge しない**（`packages/webrtc/src/utils.ts:322-348` はトップレベルキーを代入するだけ）。`{ sctp: {} }` のような入力で `config.sctp` が空オブジェクトになるため、merge 後に `{ mtu: this.config.sctp?.mtu ?? DEFAULT_SCTP_MTU }` へ正規化する。`normalizedConfig.sctp` も `{ ...input.sctp }` でコピーし、利用者オブジェクトとの参照共有を避ける。
2. **class field initialization の順序**: `packages/sctp/tsconfig.json` は `useDefineForClassFields: true`。`cwnd` は `maxPayloadSize` に依存するため field initializer をやめ constructor で代入する。`mtu` / `maxPayloadSize` は `readonly` で constructor 代入。
3. **1191 を payload 長として使わない**。1191 は SCTP packet MTU、derived payload は 1160。
4. **単純な `mtu - 28` にしない**。4-byte padding を考慮して align down する。
5. **DTLS overhead を SCTP layer で二重に引かない**。`sctp.mtu` は Pion #476 と同様、DTLS へ渡す SCTP packet 自体の上限。IP/UDP/DTLS overhead は `maxPayloadSizeForMtu()` では扱わない。
6. **`maxMessageSize` と混同しない**。`maxMessageSize` は DataChannel 論理 message 上限で SDP 交渉対象、`sctp.mtu` は outbound packet 上限、derived payload は 1 DATA fragment 長。
7. **validation は silent clamp しない**。`Number.isSafeInteger` で finite / integer / positive を担保し、`maxPayloadSizeForMtu === 0` を別エラーで reject する（実質 MTU 31 以下）。エラーメッセージはテストから参照するため固定する。
8. **live association 中の MTU 変更を禁止**。`maxPayloadSize` / `cwnd` / outbound queue / sent queue が MTU 前提で初期化済みのため、transport 作成後の異値変更は reject。同値再指定は許可。
9. **transport 再生成時に config を失わない**。`setDtlsTransport()` は bundle 差し替え（`peerConnection.ts:1004-1010`）で `SCTP` を作り直すため、`RTCSctpTransport` に stored options を必ず再適用する。
10. **既存呼び出しの source compatibility**。`SCTP.client/server` と constructor の第 3 引数は optional にする。既存の `new RTCSctpTransport()` / `new RTCSctpTransport(5000, maxMessageSize)` も壊さない。
11. **`getConfiguration()` の参照漏れ**。`clonePeerConfiguration` に `sctp: { ...config.sctp }` を追加しないと、返り値経由で live config を書き換えられてしまう。
12. **空 payload の既存挙動**。`userData.length === 0` は `Math.ceil(0 / maxPayloadSize) === 0` で chunk が生成されない（現行と同じ）。本タスクでは挙動を変えない。
13. **WPT strict shim を `src` に漏らさない**。`tools/wpt-runner` 側の都合で validation を厳しくする必要はない。
14. **unit test の flow control**。`SCTP.send` は cwnd / peerRwnd に従って送信するため、mock transport の capture テストは cwnd 内の payload を使い、`setRemotePort` を設定する。
15. **release note / version**。既定 fragmentation サイズが 1228 -> 1188 bytes（payload 1200 -> 1160）に変わる behavior change を `changelog.md` に明記する。

---

## 5. 完了条件

### 機能・互換性

- [ ] module-level `USERDATA_MAX_LENGTH = 1200` への依存を完全に除去する（fragmentation / cwnd / ssthresh / burst / T3 timeout の全 12 箇所）。
- [ ] `maxPayloadSizeForMtu()` が SCTP common header + DATA chunk header + 4-byte padding を考慮して payload を導出する。
- [ ] public low-level SCTP API（constructor / `SCTP.client` / `SCTP.server` の第 3 引数）から outbound MTU を設定できる。
- [ ] `RTCPeerConnection({ sctp: { mtu } })` から low-level SCTP まで MTU が伝播する。
- [ ] default SCTP MTU は 1191、default DATA max payload は 1160。
- [ ] custom `mtu = 1052` で max payload 1024 相当を実現できる。
- [ ] full-size DATA packet が configured MTU を越えない（padding 境界含む）。
- [ ] invalid / too-small MTU を association 作成前に reject する。
- [ ] ordered / unordered、reliable / partially reliable の全 send path で同じ MTU 制約が守られる。
- [ ] `SCTP.client/server` / constructor の既存呼び出しが source-compatible。
- [ ] `maxMessageSize` の意味と SDP `a=max-message-size` negotiation を変更しない。
- [ ] live SCTP association 作成後の異なる MTU への変更を rejectし、同値再指定は許可する。
- [ ] `getConfiguration().sctp.mtu` が現在値を返し、返り値の変更が内部 config に影響しない。
- [ ] README / API docs に `sctp.mtu` と default 1191 を記載する。
- [ ] release note（`changelog.md`）に fragmentation behavior change を記載する。

### テスト・検証

- [ ] helper / validation test（A）が通る。
- [ ] serialized packet `<= mtu` test（B、padding 境界含む）が通る。
- [ ] fragmentation / custom MTU / ordered / partially reliable test（C-F）が通る。
- [ ] RTCPeerConnection config propagation / `setConfiguration` test（G-H）が通る。
- [ ] browser interop で大きな DataChannel message が回帰しない（I、可能なら `npm run e2e:verbose`）。
- [ ] `cd packages/sctp && npm test` が通る（type + vitest）。
- [ ] `cd packages/webrtc && npm test` / `npm run type` が通る。
- [ ] 横断変更のため `npm run type` と `npm run test:small` が通る。

---

## Non-goals / follow-up

- **Path MTU Discovery**: Pion #476 でも別 issue。werift はまず configurable / safe MTU を導入し、PMTU discovery / black-hole detection は別タスク。
- **SCTP message interleaving / I-DATA**: 現行実装に I-DATA がないため DATA header のみを対象とする。将来は `maxPayloadSizeForMtu(mtu, useInterleaving)` へ拡張し I-DATA header size を反映する。
- **congestion control algorithm の Pion / RFC 追従**: `cwnd = min(4 * MTU, max(2 * MTU, 4380))` 等への変更は分離する。MTU fix と behavior change を混ぜると回帰の切り分けが難しくなるため。

---

## 参照ファイル

| ファイル | 役割 |
| --- | --- |
| `packages/sctp/src/sctp.ts` | `USERDATA_MAX_LENGTH` 12 箇所、`SCTP` class、fragmentation、congestion control |
| `packages/sctp/src/chunk.ts` | packet/chunk marshal、`padL`、`serializePacket` / `parsePacket` |
| `packages/sctp/src/index.ts` | public export |
| `packages/sctp/tests/sctp.test.ts` | mock transport / fragmentation test 追加先 |
| `packages/sctp/tests/packet.test.ts` | packet round-trip / serialization test 追加先 |
| `packages/sctp/tests/api.test.ts` | public export test |
| `packages/sctp/README.md` | low-level API docs |
| `packages/webrtc/src/transport/sctp.ts` | `RTCSctpTransport`、`new SCTP`、`setDtlsTransport` |
| `packages/webrtc/src/sctpManager.ts` | `createSctpTransport` |
| `packages/webrtc/src/peerConnection.ts` | `PeerConfig`、default config、`setConfiguration` / `getConfiguration`、propagation |
| `packages/webrtc/src/utils.ts` | `deepMerge`（shallow） |
| `packages/webrtc/tests/transport/sctp.test.ts` | `RTCSctpTransport` unit test 追加先 |
| `packages/webrtc/tests/integrate/peerConnection.test.ts` | config test の既存パターン |
| `packages/webrtc/tests/utils.ts` | `createDataChannelPair` Arrange 共有 |
| `e2e/tests/datachannel/datachannel.test.ts` | browser interop test 追加先 |
| `e2e/server/handler/datachannel/datachannel.ts` | E2E server handler |
| `e2e/server/main.ts` | handler 登録 |
| `changelog.md` | release note |
