# 指示: DTLS Epic 1 の自律監査・根本修正・完成

対象リポジトリ:
- shinyoshiaki/werift-webrtc

対象ブランチ:
- ticket/5fc64332-b0d1-4ab0-bc1e-4b0f3a26c135

基準:
- リポジトリ内の `TICKET-ticket-5fc64332-b0d1-4ab0-bc1e-4b0f3a26c135.md`
- 関連する子チケット / docs / AGENTS.md
- RFC 6347 / RFC 9147 / RFC 8446 / RFC 5764
- 現在の develop との差分

# 目的

Epic 1 を「指摘されたバグだけ直した状態」ではなく、
DTLS 1.2 / DTLS 1.3 / dual fallback / carrier / lifecycle / WebRTC default 1.2 が
一貫した association state machine として完成した状態まで持っていく。

既知の指摘だけを修正して終了してはいけない。

既知の問題と同じ根本原因を持つ未発見バグを、
コード・テスト・状態遷移・失敗経路・race・loss/retransmit の観点から
自律的に探索し、P0/P1および関連するP2をすべて修正すること。


# 1. 最初に行うこと

以下を先に読み、仕様と現在実装の対応表を作る。

1. 親ticket
2. 関連子ticket
3. `packages/dtls/src/**`
4. `packages/dtls/tests/**`
5. `packages/webrtc/src/transport/dtls.ts`
6. `.github/workflows/nodejs.yml`
7. DTLS 1.2 / 1.3 のRFC上の lifecycle / retransmission / cookie / alert / downgrade要件

その後、コードを変更する前に、少なくとも以下の状態遷移を書き出すこと。

    association
      initial
      pre-cookie / probing
      committed12
      committed13
      closing
      closed

各状態について、

- 有効なRX source
- TX peer
- active engine
- crypto state ownership
- retransmit timer
- pending async task
- carrier
- onConnect
- onError
- onClose
- send/exporter/keyUpdate
- version decision

を一覧化する。

「どのオブジェクトが何を所有しているか」が曖昧な箇所を重点監査する。


# 2. 既知の問題は修正するが、そこから横展開する

現在少なくとも以下の問題が疑われている。


## A. DTLS 1.2 ClientHello commit のtransactionality

cookie検証後の

`commitClientHelloToAssociation()`

が途中でassociation stateを変更してからthrowできる。

以下のようなstateがpartialに残らないことを保証する。

- namedCurve
- SRTP profile
- remoteExtendedMasterSecret
- localRandom
- remoteRandom
- cipherSuite
- localKeyPair
- その他ClientHello由来のmutable state

### 要求

validation / negotiation と state commit を分離する。

例:

    const negotiated = validateClientHello(...);

    // ここまでは association state を一切変更しない

    commitNegotiatedClientHello(negotiated, ...);

またはsnapshot/rollbackでもよいが、
正常系・失敗系の所有権が明確な設計を優先する。

### 必須テスト

Aのvalid-cookie CH2がcommit途中で失敗
→ association stateが完全に変更前と同じ
→ Bの正常なCH1/CH2
→ handshake成功


## B. peer authentication と transport address の混同

現在 `pinnedPeer` が

- UDP peer pin
- return-routability
- association authentication

の複数意味を兼ねていないか調査する。

特にWebRTCのICE transportはDTLSへraw source addressを渡さない。

その場合でも、

- protected fatal alert
- protected close_notify
- authenticated handshake failure
- application data
- lifecycle teardown

が正常動作する必要がある。

「IP:portが見えること」と
「下位transportが既にpeerを認証/固定していること」を
別概念として設計する。

必要ならTransport capabilityとして明示する。

例:

    peerIdentityMode:
      | "datagram-address"
      | "authenticated-single-peer"

または同等の型安全な仕組みにする。

単に特殊caseを足すだけではなく、
UDP / ICE / custom carrier で一貫する抽象化にする。

### 必須テスト

addressをDTLSへ渡さないauthenticated transport上で:

- DTLS 1.2 handshake
- protected app data
- peer fatal alert → terminal + onError
- peer close_notify → terminal + onClose
- local close
- version mismatch
- late epoch-0 spoof相当はterminalにしない


## C. stale async work / stale retransmit generation

re-HVR、version fallback、close、fatal、commit12、commit13などで、
以前のflight/timer/promiseが後からwakeして作用しないか全面監査する。

対象:

- Flight1
- Flight2/HVR
- Flight3
- Flight4
- Flight5
- Flight6
- waitForReady
- DTLS 1.3 RTO
- ACK
- epoch prune
- carrier timer
- async send
- close_notify
- dual probing

特に同一flightの「新しい世代」が始まったとき、
古いgenerationの処理が再送・state変更・error callbackを行わないこと。

必要ならassociation generationとは別に、

    flightGeneration
    hvrGeneration
    candidateGeneration

等を導入する。

### 必須テスト例

    HVR1
    → CH2(cookie1)
    → HVR2
    → CH2(cookie2)をdrop
    → RTO時間経過

この時、

- cookie1のCH2が再送されない
- pending Flight3 loopは1つ
- cookie2だけがactive
- delayed onErrorなし


# 3. 同種問題を自律的に探索すること

上記3件を直した後、以下を体系的に検索する。


## 3.1 mutate-before-validate

以下のパターンをgrep / code searchする。

    state変更
    ↓
    validation
    ↓
    throw可能

特に:

- cipher
- dtls context
- srtp
- transcript
- epoch
- peer pin
- selected version
- carrier
- handshake cache

validationが失敗した時にpartial stateが残る箇所は
すべてレビュー対象。


## 3.2 callback-before-terminal / terminal-before-cleanup

以下の順序を全terminal pathで確認。

fatal:

    disable public API
    cancel pending work
    mark terminal
    close candidates/carrier
    onError
    onClose

graceful close:

    disable public API
    cancel non-close work
    send bounded close_notify
    terminal cleanup
    onClose exactly once

callback内でユーザーが再度 `close()` / `send()` / `connect()` を呼ぶ
re-entrant caseもテストする。


## 3.3 timer ownership

すべての

- setTimeout
- Promise timer
- RTO
- retransmit
- epoch cleanup
- delayed callback

について所有associationを明確化する。

以下の後にtimerが残らないこと。

- fatal
- local close
- peer close_notify
- commit12
- commit13
- version mismatch
- RTO exhaustion
- handshake complete

単にcallback側で

    if (closed) return;

とするだけでは不十分。

可能なものはtimerそのものをcancelする。


## 3.4 duplicate / reorder / late packet

全flightについて最低限、

- duplicate current flight
- duplicate previous flight
- late packet after next flight
- late packet after connected
- late packet after version commit
- late packet after close
- packet from wrong peer

を確認する。

duplicate受信で、

- random再生成
- key再生成
- sequence巻き戻し
- handshake cache破壊
- callback再発火

がないこと。


## 3.5 address / peer pin

すべてのTXについて、

    last received rinfo

を暗黙に信用していないか確認する。

peer pin後は、

- Flight retransmit
- alert
- close_notify
- application data

が最後に届いたspoof datagramのsourceへリダイレクトされないこと。

一方、addressless authenticated transportでは
address pinを必須条件にしないこと。


## 3.6 version transition

dual:

    initial
    → probing
    → committed12

および

    initial
    → probing
    → committed13

について、

- loser candidate停止
- loser timer停止
- loser async callback停止
- carrier ownership継続
- Public APIがwinnerだけを見る
- exporterがwinnerだけを見る
- sendがwinnerだけを見る
- closeが全candidateを閉じる

ことを確認する。

HVRだけでversionをcommitしない。

late 1.3 SH/HRR / late 1.2 ServerHelloで
commitが逆転しないこと。


# 4. Security boundary

generic `werift-dtls` として以下を再確認する。


## DTLS 1.2

HelloVerify cookie:

    HMAC(
      secret,
      peer identity,
      canonical ClientHello parameters
    )

- legacy_cookie field自体はbinding対象から除外
- cookie取得peerと使用peerが異なればreject
- cookie前はcrypto association stateをcommitしない
- cookie後だけamplifying Flight4へ進む


## DTLS 1.3

- RFC 9147 cookie
- address validation
- anti-amplification <= 3x
- fragment/reassembly bounds
- replay
- malformed records
- CID unsupported handling
- epoch correctness


# 5. テストを「通る」ではなく「壊したら落ちる」ようにする

テスト追加時は、
修正前実装で本当にFAILすることを論理的に確認する。

悪い例:

    sleep 100ms
    expect(close < 80ms)

のように旧実装でも偶然通るテストは禁止。

race/retransmitテストでは、

- generation counter
- controlled packet drop
- controlled send interception
- explicit state observation

を使って決定論的にする。

過度な wall-clock threshold は避ける。


# 6. 禁止事項

以下は禁止。

- timeoutを伸ばすだけ
- flaky testをskip
- assertionを弱める
- catchして無視する
- errorをlogだけして継続
- `as any`追加だけで型問題を回避
- lifecycle bugをclosed flagの追加だけで隠す
- duplicate packetを無条件dropしてRFC retransmissionを壊す
- CIが無いのに「PASS」と記録する
- 実行していないinteropを成功扱いする


# 7. 実装後の必須検証

最低限:

    cd packages/dtls
    npm run type
    npm test

common / WebRTC transportを変更した場合:

    npm run type
    npm run test:small

さらに可能なら:

- OpenSSL DTLS 1.2
- BoringSSL DTLS 1.3 client/server両role
- werift self 1.2
- werift self 1.3
- dual → 1.2 fallback
- dual → 1.3
- WebRTC default 1.2

を実行する。

実行できなかったものは必ず

`未確認`

と記載する。


# 8. 完了前に自分でレビューする

実装が終わったと思った時点で、
もう一度「レビュー担当者」として自分の変更を監査する。

特に以下の問いに全て答える。

1. 失敗途中にmutable stateが残る箇所はないか
2. close後にwakeするPromise/timerはないか
3. old generationがcurrent associationに作用できないか
4. duplicate packetでkey/random/stateを再生成しないか
5. authenticated transportとaddress pinを混同していないか
6. UDP/carrier/ICEでRX lifecycleが一致しているか
7. onConnect/onError/onCloseは各associationで正しい回数か
8. Public APIはterminal後必ず拒否するか
9. version loser candidateは完全に停止するか
10. current testsが修正前コードで実際にFAILするか

ここで新しいP0/P1/P2を発見した場合は、
ユーザーに確認せずそのまま修正してよい。


# 9. 追加の横断監査

修正後は、今回変更した関数だけでなく、
その関数が共有するstate・timer・transport・callbackを読み書きする
全call siteを逆方向/順方向に追跡すること。

同じinvariantを破る経路が1つも残っていないことを確認する。

特に以下について call graph を追う。

- `DtlsContext`
- `CipherContext`
- `SrtpContext`
- `TransportContext`
- `pinnedPeer`
- `engine13`
- `parkedEngine13`
- `associationCarrier`
- `associationTornDown`
- `connected`
- `dualPhase`
- `flight`
- `fatalError`
- retransmit timer
- AbortController / AbortSignal
- `lastFlight`
- `lastMessage`
- `handshakeCache`

単一のfieldが複数の意味を兼ねている場合は、
条件分岐を増やしてごまかすのではなく、
意味ごとにstateを分離することを検討する。


# 10. 完了条件

以下をすべて満たすまでタスク完了としない。

- 既知P1/P2を修正
- 同じroot causeから派生するP0/P1を自律探索して修正
- 関連するP2も修正
- loss/reorder/duplicate/raceの回帰テスト追加
- WebRTC default DTLS 1.2回帰確認
- dual association lifecycle整合
- carrier lifecycle整合
- timer/promise leakなし
- cookie/address-validation security boundary整合
- 実行したtest結果を正確に記録
- 未実行項目をPASS扱いしない

P0/P1が残っている場合は「完成」と宣言しない。


# 11. 最終報告形式

最後に以下の形式で報告する。

## 修正結果

### 発見した問題

- P0 ...
- P1 ...
- P2 ...

既知の指摘だけでなく、自律監査で新たに発見したものもすべて記載する。


### 根本原因

各問題を個別バグとしてではなく、

- ownership
- transactionality
- lifecycle
- stale async work
- peer authentication
- version state machine
- retransmission semantics

等のroot cause単位で整理する。


### 実装変更

- 変更ファイル
- 変更したstate machine
- ownershipの変更
- APIへの影響
- compatibilityへの影響

を記載する。


### 追加した回帰テスト

各テストについて、

- 何を再現するか
- 修正前実装ではなぜFAILするか
- 修正後に何を保証するか

を記載する。


### 実行結果

以下の形式で、実際に実行したものだけを記録する。

- command: `cd packages/dtls && npm run type`
  result: PASS / FAIL / 未確認

- command: `cd packages/dtls && npm test`
  result: PASS / FAIL / 未確認

- command: `npm run type`
  result: PASS / FAIL / 未確認

- command: `npm run test:small`
  result: PASS / FAIL / 未確認

- OpenSSL DTLS 1.2:
  result: PASS / FAIL / 未確認

- BoringSSL DTLS 1.3:
  result: PASS / FAIL / 未確認

実行していないものは絶対にPASSと書かない。


### 残存リスク

- 未検証interop
- platform dependency
- timing依存
- RFC上の未対応
- Epic 2以降へ意図的に残したもの

を明記する。


### Epic 1 完了条件

| Gate | Result | Evidence |
| --- | --- | --- |
| werift self DTLS 1.3 両role | PASS/FAIL/未確認 | command/test |
| protected app data bidirectional | PASS/FAIL/未確認 | test |
| BoringSSL interop 両role | PASS/FAIL/未確認 | command/test |
| OpenSSL DTLS 1.2 regression | PASS/FAIL/未確認 | command/test |
| dual → 1.2 fallback | PASS/FAIL/未確認 | test |
| dual → 1.3 | PASS/FAIL/未確認 | test |
| version mismatch actionable error | PASS/FAIL/未確認 | test |
| KeyUpdate | PASS/FAIL/未確認 | test |
| exporter | PASS/FAIL/未確認 | test |
| WebRTC default DTLS 1.2 | PASS/FAIL/未確認 | test |
| cookie/address validation | PASS/FAIL/未確認 | test |
| anti-amplification | PASS/FAIL/未確認 | test |
| lifecycle terminal invariants | PASS/FAIL/未確認 | test |
| timer/task cleanup | PASS/FAIL/未確認 | test |
| duplicate/loss/reorder | PASS/FAIL/未確認 | test |
| Public API compatibility | PASS/FAIL/未確認 | evidence |


# 最重要原則

既知3件を直しただけで終了してはいけない。

今回の問題のroot causeである、

- mutate-before-validate
- partial commit
- stale async work
- stale retransmit generation
- ownership ambiguity
- transport authentication と address pin の混同
- terminal transition と callback ordering
- duplicate/reorder時のstate再生成
- version candidate間のownership競合

を `packages/dtls` 全体と `packages/webrtc` のDTLS利用経路から探索すること。

修正を行うたびに、
「この修正と同じinvariantを共有している別経路はないか」
をcall graphの上下方向に追跡する。

P0/P1/P2を新たに発見した場合、
ユーザーへの確認を待たずに原因分析・修正・回帰テスト追加まで進めること。

最終的にP0/P1が残っている、
または必須完了条件を満たしていない場合は、
「Epic 1 完成」と宣言してはいけない。