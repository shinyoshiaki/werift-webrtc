`shinyoshiaki/werift-webrtc` の
`ticket/c724daf9-5f79-4e30-bd50-7b27c8951844`
ブランチについて、GCC/TWCC 実装を完成させてください。

単に既知のレビュー指摘を修正するだけではなく、
**同じ種類の問題をコードベース全体から自律的に探索し、
新たに発見した Blocker / Major もすべて修正してください。**

# 最重要方針

このタスクでは「レビュー指摘を通すための局所修正」ではなく、

**固定 pin の libwebrtc と werift の GCC の意味論を体系的に比較し、
主要な state transition / timing / ordering / lifetime / bitrate update の
未説明差をなくして実装を完成させること**

を目的とします。

参照元の優先順位:

1. `third_party/libwebrtc-ref/`
2. `third_party/libwebrtc-ref/PIN.json` の固定 commit
   `0fda16159e33adf59c71a7ad1173dcbe5a632102`
3. `TICKET-ticket-c724daf9-5f79-4e30-bd50-7b27c8951844.md`
4. RFC / GCC draft
5. その他

**unpinned の current libwebrtc を混ぜないでください。**

固定 pin に不足している依存 source/header がある場合は、
まず curated snapshot を補完してから比較してください。

---

# 1. まず既知の残件を修正する

少なくとも以下を確認・修正してください。

## A. high RTT と LossBasedBwe の更新順序

現在の high RTT path が、

- LossBasedBwe の低い結果を `currentTarget` に先に反映
- その後さらに RTT backoff `×0.8`

となっていないか確認してください。

固定 pin の
`SendSideBandwidthEstimation::UpdateEstimate()`
では `IsRttAboveLimit()` branch が LossBasedBweV2 の result 適用より先です。

以下のケースを regression test にしてください。

```text
current target = 1 Mbps
delay estimate = 1 Mbps
loss result    = 300 kbps
RTT            = above limit
RTT drop interval = due

期待:
pin と同じ target

禁止:
300kbps を先に採用してからさらに ×0.8
```

high RTT 時の `ApplyTargetLimits()` / `GetUpperLimit()` が
実際に何を clamp するかも固定 pin から確認してください。

---

## B. `OnProcessInterval()` 相当の periodic update

固定 pin では
`GoogCcNetworkController::OnProcessInterval()`
から継続的に、

```cpp
bandwidth_estimation_.UpdateEstimate(msg.at_time);
```

が呼ばれます。

werift が現在 `rtpPacketSent()` / `receiveTWCC()` のイベント時だけ
RTT backoff を進めているなら不十分です。

以下を満たす構造にしてください。

- RTT-limit に入った後
- RTP/TWCC event が無い時間でも
- pin 相当の process interval で
- `drop_interval` ごとに RTT target backoff が進む
- packet を送っていないだけで `CorrectedRtt` 自体が増えることはない

タイマーを追加する場合は、

- lifecycle
- dispose
- reset
- estimator swap
- timer cleanup

まで確認してください。

専用 `process(nowMs)` API を使う場合は、
production path から実際に周期実行されることをテストしてください。

---

## C. RTT clock-domain heuristic を production から排除

`feedbackTimeMsForRtt()` のような、

```text
wall clock と synthetic timeline が離れすぎたら
receive timeline に fallback
```

というテスト都合の heuristic が production algorithm に入っていないか
再確認してください。

固定 pin と同じく、

- send time
- feedback time

は同一 sender/local clock domain としてください。

TWCC receiver timestamp は pending-time 差分のためだけに使用してください。

synthetic test を成立させるために production algorithm を曲げないこと。

必要なら、

- test clock injection
- fake clock
- `Date.now()` 基準の synthetic data

にテストを直してください。

特に 60 秒超 late feedback 等で clock domain が途中切替されないことを
regression test にしてください。

---

# 2. 既知指摘の修正後、必ず自律探索を行う

ここがこのタスクの本体です。

**既知3件を直した時点で終了してはいけません。**

`develop...HEAD` と固定 pin の関連 source を、
reviewer として最低1回最初から読み直してください。

最低限、以下の観点を体系的に探索してください。

## 2.1 State machine

以下を確認してください。

- state transition の順番が pin と同じか
- 1 feedback 内の transition を取りこぼしていないか
- previous/current state の保存位置が正しいか
- `complete`
- `waiting`
- `hold`
- `increasing`
- `decreasing`
- `delay_based`
- `underuse`
- `overuse`
- `normal`

等の遷移条件が正しいか

また、

- update 前の state と update 後の state を混同していないか
- 一度 complete した session が意図せず reopen しないか
- state-preserving update なのに full reset していないか

を確認してください。

---

## 2.2 Update ordering

特に以下を pin と1ステップずつ対応付けてください。

```text
TWCC feedback
↓
acked throughput
↓
probe estimator
↓
delay-based estimate
↓
probe SetEstimate
↓
loss-based update
↓
send-side bandwidth estimate
↓
bandwidth limited cause
↓
ProbeController
↓
published target
```

一つでも前後している場合、
その差が実際に意味論差を生まないか確認してください。

意味論差がある場合は修正してください。

「Known Differences」に書くだけで済ませないでください。

---

## 2.3 Clock domains

以下を混同していないか探してください。

- sender local clock
- wall clock
- TWCC receive-time timeline
- RTCP RTT
- propagation RTT
- feedback RTT
- pacer clock
- probe receive timeline
- probe sender timeout
- ProcessInterval clock

特に、

- unit test の synthetic clock を production に持ち込んでいないか
- clock mismatch を heuristic で隠していないか
- Date.now と TWCC relative time を直接比較していないか

を確認してください。

---

## 2.4 Lifetime / cleanup

以下を確認してください。

- ProbeController lifetime
- ProbeBitrateEstimator history
- sent packet history
- estimatorHistory
- seqToCluster
- seqToSendInfo
- soft loss
- finalized seq
- late correction
- reorder history
- sequence wrap
- pacing timeout
- feedback timeout
- ProcessInterval timer
- estimator swap 時の cleanup

以下を満たしてください。

- cleanup が早すぎない
- 無期限に残らない
- controller と estimator の lifetime を不必要に結合しない
- zero-packet がリークしない
- no-ACK がリークしない
- late-ACK を過早に捨てない
- wrap 後に古い state が新 packet に作用しない

---

## 2.5 Loss / delay / RTT の優先順位

以下の組み合わせを重点的に確認してください。

```text
overuse + probe
underuse + probe
normal + probe
high RTT + loss
high RTT + probe
high RTT + no feedback
high RTT + no new RTP event
high RTT + later recovery
loss decreasing + recovery probe
loss hold + recovery probe
loss increasing + further probe
lower probe + loss
upward probe + loss
late correction + loss observation
stream timeout + existing trendline state
RTT recovery + previous RTT drop state
```

pin と違う順序や state transition がないか確認してください。

---

## 2.6 AIMD

固定 pin の、

- `ChangeState`
- `ChangeBitrate`
- `MultiplicativeRateIncrease`
- `AdditiveRateIncrease`
- `TimeToReduceFurther`
- `GetNearMaxIncreaseRate`
- `LinkCapacityEstimator`
- `SetEstimate`
- `SetRtt`
- `Reset`

を、定数だけではなく **state と履歴の保持条件まで**比較してください。

特に、

- RTT source
- default RTT
- RTT clamp
- beta
- `-5kbps`
- `1.5 × throughput + 10kbps`
- minimum multiplicative increase
- minimum additive increase
- link capacity deviation
- link capacity reset semantics
- decrease timestamp
- previous estimate history

を確認してください。

---

## 2.7 Probe

以下を再確認してください。

- pacing completion
- ACK と pacing の責務分離
- min packets
- min bytes
- 80% estimate
- remaining ACK refinement
- 3x / 6x
- further threshold
- planned target
- exact max clamp
- timeout
- result ordering
- lower probe result
- upward probe result
- initial total failure
- recovery
- active cluster
- queued cluster
- probe padding
- probe padding sequence
- probe result と congestion state の順序
- ProbeController lifetime
- ProbeBitrateEstimator lifetime
- loss / underuse / high RTT 時の new probe gating

---

## 2.8 TWCC / RTP

以下を再確認してください。

- transport-wide sequence wrap
- transport-wide sequence uniqueness
- transport-wide sequence ownership
- RTP sequence uniqueness
- media reorder + probe padding
- RunLength chunk
- StatusVector chunk
- packetStatusCount
- late `not-received → received`
- duplicate feedback
- overlapping feedback
- `ReceivedWithoutDelta`
- `receivedAtMs === 0`
- reference_time unwrap
- 24-bit wrap
- reorder retention
- sequence mapping cleanup
- retransmission / RTX との相互作用

---

# 3. Known Differences で重大差を隠さない

`GCC_KNOWN_DIFFERENCES` の各項目について1件ずつ監査してください。

各項目について、

1. 本当に意図的差分か
2. ticket の non-goal に該当するか
3. bitrate/state transition を大きく変えないか
4. 固定 pin に寄せられるのに未実装なだけではないか

を確認してください。

以下に該当するものは Known Difference に逃がさず修正してください。

- congestion response を大きく変える
- recovery timing を変える
- probe exploration を変える
- state transition を変える
- RTT/loss/delay の優先順位を変える
- timer/lifetime の意味論を変える
- current target の更新値が大きく変わる
- packet reorder/loss 解釈を変える
- feedback stall 時の安全動作を変える

Known Difference を削除できる実装が可能なら、
原則として実装を pin に寄せて削除してください。

---

# 4. 固定 pin snapshot 自体も監査する

`third_party/libwebrtc-ref/` に、
werift 側で使っている定数や helper の定義元が不足していないか確認してください。

例えば、

```text
kCongestionControllerMinBitrate
```

等を参照しているのに定義元 header が snapshot に無い場合、
curated list に追加してください。

必要に応じて、

- `fetch_ref.py`
- `PIN.json`
- `MANIFEST.md`

を更新し、常に整合させてください。

固定 commit は変更しないでください。

また、

- `__pycache__`
- `*.pyc`
- browser binaries
- generated temporary files
- worktree artifacts

等をコミットしないでください。

---

# 5. Regression-first

新しい問題を自律発見した場合は、

```text
問題発見
↓
修正前に失敗する regression test を追加
↓
失敗を確認
↓
修正
↓
同じ regression が green
```

の順にしてください。

単に修正後のコードに合わせてテストを書くのは禁止です。

少なくとも以下は決定的テストを用意してください。

- high RTT + low LossBased result
- high RTT + no new RTP/TWCC event + periodic process
- high RTT → normal RTT recovery
- no feedback from first packet
- feedback clock domain
- very late feedback
- probe + overuse
- probe + underuse
- probe + loss-limited state
- underuse→normal transition within one feedback
- lower probe
- upward probe
- sequence wrap
- media reorder + padding
- late soft-loss correction
- reset/state preservation
- estimator swap cleanup

テストで private field を直接書き換えるのは、
他に方法がない controller 単体テストに限定してください。

可能なものは public production path で検証してください。

---

# 6. 実装後の self-review

修正後、テストが通った時点で終了してはいけません。

**追加問題を探すためだけの review pass を最低1回実施してください。**

その review pass では、
「今直した箇所」だけを見るのではなく、

- 固定 pin
- `develop...HEAD`
- `GCC_KNOWN_DIFFERENCES`
- regression tests
- production wiring

を横断して、

> 今 reviewer としてこのブランチを見た場合、
> Request changes を出す Blocker / Major は残っていないか

という観点で確認してください。

Blocker / Major を1件でも発見したら、

```text
regression追加
↓
修正
↓
テスト
↓
再self-review
```

を繰り返してください。

**最終 self-review で新規 Blocker / Major が 0 件になるまで終了禁止です。**

---

# 7. 検証

最低限、以下を実行してください。

```bash
cd packages/webrtc
npx vitest run tests/media/bandwidthEstimator.test.ts
npx vitest run tests/media/receiverTwcc.test.ts
npm run type
```

変更範囲に応じて、

```bash
npm run test:small
```

も実行してください。

チケット指定の CI は、

```bash
node .sak-context/link/cli/src/index.ts ticket run-ci \
  --ticket-id c724daf9-5f79-4e30-bd50-7b27c8951844
```

を使用してください。

CI pre-run merge が失敗した場合も放置しないでください。

```text
CI pre-run merge failure
↓
git status / worktree / base branch を確認
↓
原因修正
↓
同じ run-ci command を再実行
↓
green になるまで継続
```

「CI が開始できなかった」を成功扱いにしないでください。

可能なら両 simulation も実行してください。

```bash
cd packages/webrtc
npm run test:sim
```

および、

```bash
cd e2e
npm run test:sim
```

ブラウザ/OS/外部依存による環境失敗と、
実装コードによる失敗は明確に区別してください。

---

# 8. develop を取り込む

最終検証前に、

```bash
git log
git status
git diff develop...HEAD
```

等で最新 `develop` との差を確認してください。

branch が behind の場合は最新 develop を取り込んでください。

競合解消後、必ずもう一度、

- targeted vitest
- receiver TWCC test
- type
- broader test
- CI
- simulations
- fixed-pin self-review

をやり直してください。

「GCC と直接競合しない commit だから取り込まない」という判断で
完了扱いにしないでください。

---

# 9. 完了条件

以下を**すべて**満たすまで終了しないでください。

- [ ] 既知レビュー指摘がすべて解消
- [ ] 自律探索で発見した Blocker / Major がすべて解消
- [ ] 最終 self-review で新しい Blocker = 0
- [ ] 最終 self-review で新しい Major = 0
- [ ] GCC 主要 state transition に未説明差なし
- [ ] timing / ordering / clock domain に未説明差なし
- [ ] lifecycle / cleanup に unbounded state なし
- [ ] state-preserving update で不要な full reset なし
- [ ] `GCC_KNOWN_DIFFERENCES` と実装が一致
- [ ] Known Differences で重大な意味論差を隠していない
- [ ] tests が実装追認だけになっていない
- [ ] 自律発見した問題に regression test がある
- [ ] pin snapshot に監査に必要な source/header が揃っている
- [ ] generated files / cache / binaries の誤 commit なし
- [ ] 最新 develop を取り込み済み
- [ ] targeted tests green
- [ ] receiver TWCC tests green
- [ ] type green
- [ ] broader tests green
- [ ] CI green
- [ ] 実行可能な werift↔werift simulation green
- [ ] 実行可能な Chrome↔werift simulation green

---

# 10. 最終報告

最後に以下を表形式で報告してください。

| 項目 | 内容 |
|---|---|
| 最終 HEAD | SHA |
| 修正した既知問題 | 一覧 |
| 自律発見した問題 | 一覧 |
| 各問題の root cause | 一覧 |
| fixed pin の対応箇所 | file / function |
| 追加 regression tests | test名 |
| Known Differences | 残った項目と理由 |
| pin snapshot 追加ファイル | 一覧 |
| develop 取り込み | SHA / 結果 |
| targeted GCC tests | PASS / FAIL |
| ReceiverTWCC tests | PASS / FAIL |
| type | PASS / FAIL |
| broader tests | PASS / FAIL |
| CI | PASS / FAIL |
| werift↔werift simulation | PASS / FAIL / 環境制約 |
| Chrome↔werift simulation | PASS / FAIL / 環境制約 |
| 最終 self-review | Blocker 0 / Major 0 / Minor N |
| 残る懸念 | Minor 以下のみ |

最後に必ず、

```text
Final review result:
Blocker: N
Major: N
Minor: N
```

を記載してください。

**Blocker または Major が1件でも残っている状態で
「実装完了」「レビュー完了」「マージ可能」と報告してはいけません。**

既知問題を修正しただけで終了せず、
自律探索 → regression-first 修正 → 検証 → self-review を繰り返し、
**Blocker 0 / Major 0 まで実装を完成させてください。**