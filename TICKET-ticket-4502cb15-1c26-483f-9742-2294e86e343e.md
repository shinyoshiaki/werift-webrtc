# RTP m-line の拒否と再利用

## 目的

この文書は、受信した offer にローカルで扱えない RTP codec が含まれる場合の応答と、停止した RTP m-line の再利用動作を定義する。

`RTCPeerConnection.setRemoteDescription()` は、構文的に有効な offer に共通 codec がないという理由だけで失敗してはならない。offer を適用し、受け入れられない media section は Answer で拒否する。media section の順序、MID、format token を保ち、後続の ICE candidate 解決と再交渉を可能にする。

## 範囲

この仕様が定める内容:

- 共通 RTP codec がない remote m-line の受理と Answer での拒否
- BUNDLE group と identification-tag の維持・再選択
- remote/local ICE candidate と m-line の対応
- `removeTrack()`、`transceiver.stop()`、停止済み m-line の再利用
- 上記の交渉で必要となる commit / rollback の状態保護

ここで定めるのは影響を受ける交渉経路の correctness 条件である。PeerConnection 全体の状態機械の再実装、全 subsystem に共通する transaction coordinator の導入、全遷移を横断する atomic transaction 化、ICE/DTLS restart orchestration 全体の再設計は対象外とする。

## RTP m-line の拒否

### Remote offer の適用

`TransceiverManager.setRemoteRTP()` は codec 交渉結果が空でも例外を投げない。remote SDP、MID、m-line index を保持して signaling state を進める。

codec 不一致による拒否は `direction === "inactive"` や `stopped` と別の状態として扱う。remote 側が既に port 0 で拒否している場合も offer を適用し、media pipeline を準備しない。

受け入れられない section では次を行わない:

- `sender.prepareSend()` を含む送信準備
- `receiver.prepareReceive()`、router 登録、RID / SSRC 登録
- `onTrack` の通知
- TWCC、RTCP、NACK などの media pipeline の登録・更新

交渉中の既存 media pipeline は、保留中の offer を適用しただけでは停止しない。拒否が Answer として確定した時点で、その section に属する pipeline と不要になった transport を停止する。共有 transport は他の有効な section が使用している限り維持する。

### Answer の media section

拒否する Answer m-line は、offer と同じ位置、MID、media type、protocol を保ち、port を `0` にする。m-line 自体を削除したり、順番を変えたりしない。

Answer の `fmt` には offer の format token を少なくとも一つ残す。ローカル codec 一覧が空でも、空の `m=` 行は生成しない。受け入れる m-line は従来どおり有効な非ゼロ port を使う。

非ゼロの Answer / pranswer は、保留中の offer と交渉可能な codec を持つことを確定前に検証する。検証に失敗した場合は `InvalidAccessError` とし、signaling state と既存 description を変更しない。remote port 0 の Answer は受理する。

## BUNDLE と ICE candidate

Answer の BUNDLE group は、remote offer の membership と identification-tag の順序を基準に作る。拒否した MID は group から除く。offer に無い section を追加で group に含めない。

- 非 tag section だけを拒否する場合、既存 tag を維持する。
- tag section を拒否する場合、受け入れる section を新しい tag として選び、group の先頭に置く。
- group 内の section をすべて拒否する場合、Answer から BUNDLE group を省く。
- group 外の section は独立した transport と ICE credentials / candidate を維持する。

remote offer の全 media section と MID / index は description 内に保持する。拒否した m-line の `sdpMid` または `sdpMLineIndex` を使う `addIceCandidate()` も解決できること。

local candidate は m-line 0 を固定の tag とみなさず、交渉済み BUNDLE tag と実際の transport 所有者から MID / index を決める。拒否済み MID や別 transport の section を candidate の宛先として使わない。

## m-line の停止と再利用

### 設定

`PeerConfig.mLineReuse` は `"compatible" | "aggressive"` を受け取り、既定値は `"compatible"` とする。値は生成時に選択し、無効値と `setConfiguration()` による途中変更は拒否する。

- `compatible`: `removeTrack()` による inactive section は非ゼロ port のまま保持する。停止の Answer が確定した port-zero slot は、後続の新規 transceiver が同じ index で再利用できる。
- `aggressive`: inactive section を port 0 にする従来の wire 動作を明示的に選べる。停止の拒否交渉が確定した slot は再利用できるが、停止済み transceiver の再開は保証しない。

両モードで codec 不一致の拒否、format、MID、順序、BUNDLE、ICE candidate の条件を守る。`addTrack()` が自動で再利用できる sender は未停止・未拒否のものに限る。

### `removeTrack()` と `stop()`

compatible mode の `removeTrack()` は sender を停止せず track を切り離す。必要な交渉後、同じ sender に track を戻して direction を復帰すれば同じ MID / index で再開できる。

`transceiver.stop()` は冪等とする。sender / receiver、router 登録、RTCP / NACK / TWCC、保留中 RTP を停止・解除し、交渉を要求する。共有 ICE / DTLS transport は transceiver の停止だけでは破棄しない。

`stopping` transceiver は次の local offer で port 0 を提示し、対応する拒否 Answer の適用後に `stopped` を確定する。compatible mode では answerer が `stop()` を呼んだだけで Answer を強制的に port 0 にしない。answerer 自身が次の offer を作る段階で停止を提示する。

SDP にまだ関連付けられていない transceiver を停止した場合、停止だけを理由に新しい m-line を作らない。停止済み m-line を再利用するときは同じ index に新しい MID を割り当て、停止済み transceiver object を復活させない。停止交渉が確定する前に追加された transceiver はその slot を先取りしない。

再利用は BUNDLE の tag、unbundled transport、RTP より前にある SCTP section、繰り返しの停止・再利用でも成立すること。remote offer が同じ index に新 MID を提示した場合、新しい receiver / router として扱う。

## 交渉中の状態保護

以下はこの仕様で扱う交渉経路に適用する。異なる remote ICE generation、SCTP association、現在の media pipeline を保留中の更新で破壊しない。

### Commit と rollback

- 保留中の codec reject は `rejected` として保持し、確定前に terminal な `stopped` にしない。既存 pipeline は commit まで利用可能にする。
- `transceiver.stop()` による protocol-driven な停止確定が `negotiationneeded` を再発火させない。アプリケーションが明示した `restartIce()` 等の要求は通常どおり通知する。
- rollback では、この交渉で追加した transceiver / SCTP transport や sender / receiver / router の変更を取り除くか復元する。既存 association と共有 transport は保つ。
- pending offer の ICE / DTLS credentials、role、fingerprint、candidate、end-of-candidates は transport 単位で stage し、commit 時に適用する。rollback では破棄または直前の状態へ戻す。
- 新しい ICE generation の candidate / end-of-candidates は該当 generation に束ねる。replacement offer は最新 generation を採用し、rollback 後に新世代の candidate を current generation へ混ぜない。
- restart に対する Answer が staged local credentials を広告する場合、commit 後の live transport credentials と一致させる。Answer に旧 generation の candidate / end-of-candidates を混在させない。
- 確立済み SCTP association と異なる remote `sctp-port` は、association に触れる前に `InvalidModificationError` で拒否する。`max-message-size` は既定値を含めて正規化し、既存 association では commit / rollback の対象にする。

ICE checklist 内部の pair 再構築や、ICE restart 後の end-to-end RTP 再開を含む汎用 restart orchestration はこの仕様の保証範囲に含めない。

### Transport と診断出力

ICE gather / restart / connect の対象は有効な media section が所有する live transport に限る。拒否が確定し、他に live な所有者がいない transport は停止・整理する。close、stats、role など全 transport を対象にする操作は既存の API 動作を保つ。

無条件のログ出力を追加しない。ICE username、password、candidate、DTLS fingerprint などの credential 情報を stdout や debug 出力へ記録しない。

## 実装箇所

主な変更対象は `packages/webrtc` の SDP、transceiver、peer connection、secure transport、SCTP 管理層。

- `transceiverManager.ts`: codec の交差、拒否状態、sender / receiver の準備、再利用条件
- `sdpManager.ts`: Answer m-line、format fallback、BUNDLE membership / tag、transport description
- `peerConnection.ts`: remote media の割り当て、commit / rollback、停止の確定、ICE / DTLS / SCTP の交渉
- `secureTransportManager.ts`: candidate の世代・transport・MID / index の対応
- `rtpTransceiver.ts`: rejected / stopping / stopped の区別

ICE、DTLS、RTP の下位 protocol に変更が必要な場合は、上記の交渉要件に直接必要なものに限定する。

## 回帰テストと完了条件

テストは `packages/webrtc/tests/issue/705.test.ts` と、再利用用の関連テストに置く。共通 Arrange setup は一箇所の helper にまとめ、テストを Arrange / Act / Assert に分ける。Act / Assert には各操作と検証意図が分かる日本語コメントを付ける。

以下を確認する:

1. unbundled の非対応 video と対応 audio で remote offer を適用でき、Answer は video のみを port 0 にする。位置と MID は維持する。
2. BUNDLE の非 tag m-line を拒否したとき、その MID だけ group から除く。
3. BUNDLE tag を拒否したとき、受け入れ section を新 tag に選び、次の交渉でも membership と transport ownership が一致する。
4. remote が port 0 にした section とローカル codec 不一致が重なっても適用でき、track / media pipeline を作らない。
5. 拒否 section の MID / index を使った remote candidate と、再 tag 後の local candidate が正しい section に紐づく。
6. `removeTrack()`、`stop()`、再交渉、再利用後に MID / index と RTP 受信が定義どおりになる。BUNDLE / unbundled、SCTP 先行、未関連付け transceiver、繰り返し再利用を含める。
7. answer codec 検証、pending pipeline、ICE / DTLS generation、SCTP 更新で commit と rollback が current state を保護する。
8. 既存の port-zero track、通常の codec negotiation、関連する bundled offer の動作を維持する。

コード変更時の検証は `cd packages/webrtc && npm run type` と関連テストを含む `cd packages/webrtc && npm test` を基本とする。公開プロトコル動作を変える場合は `changelog.md` の Unreleased に記録する。WPT、Firefox / Safari、全体 CI は、この仕様単独の完了条件には含めない。

## 参照仕様

- RFC 8829 (JSEP): Answer 生成時の media section 拒否
- RFC 3264 §6.1: 共通 media format がない場合の port-zero rejection と offer format の保持
- RFC 8843 §7.3 / §7.3.4: BUNDLE の tag 再選択と拒否 section の扱い
- RFC 9429 §5.2.2、および W3C WebRTC: transceiver の停止と再利用に関する動作
