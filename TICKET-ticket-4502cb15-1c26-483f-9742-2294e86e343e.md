# Issue #705: 非対応 RTP m-line を例外ではなく Answer 側で reject する

対象 Issue: https://github.com/shinyoshiaki/werift-webrtc/issues/705
下流再現: https://github.com/campey/workadventurer/pull/46

## 1. タスクの目的と背景

`RTCPeerConnection.setRemoteDescription()` が、remote offer の audio/video m-line とローカル `PeerConfig.codecs` に共通 codec が無い瞬間に `Error("negotiate codecs failed.")` を投げる。これは JSEP / SDP offer-answer として早すぎる。構文的に妥当な offer がローカルで受け取れない media section を含むことは正規のケースであり、offer は適用し、非対応 m-line は generated answer で `m=` port `0` として reject するのが正しい。

現状の失敗点は `TransceiverManager.setRemoteRTP()`（`packages/webrtc/src/transceiverManager.ts`）。codec 交渉後に:

```ts
if (transceiver.codecs.length === 0) {
  throw new Error("negotiate codecs failed.");
}
```

のため、1 本の非対応 m-line が `setRemoteDescription()` 全体を中断する。後続の対応可能な m-line は処理されず、`createAnswer()` が reject する機会も無い。

実害は WorkAdventurer のブラウザ interop。ブラウザ offer は bundled（`a=group:BUNDLE 0 1 2`）で tagged m-line が video。音声専用アプリでも `video: [useVP8()]` をダミー追加しないと werift が throw する、という回避になっている。

仕様根拠:

- RFC 8829 (JSEP) answer generation: 対応かつ許可された media format が無い offered m-line は、対応する answer m-line の port を 0 にして reject し、以後その m-line の処理を skip してよい
- RFC 3264 §6.1: 共通 media format が無い stream は port 0 で reject。offer に列挙された format を少なくとも 1 つ残す
- RFC 8843 §7.3: BUNDLE の tagged m-section を reject する場合の tag 再選択。tagged を port 0 にして group をそのまま残してはいけない

## 2. 実装すべき具体的な機能や変更内容

当初要件では公開 API のシグネチャ変更は不要だった。追加要件（§6）として `PeerConfig.mLineReuse` を導入する。`setRemoteDescription` / `createAnswer` のプロトコル動作を直す。

### 2.1 `setRemoteRTP()` で空 codec を throw しない

対象: `packages/webrtc/src/transceiverManager.ts` の `setRemoteRTP()`。

- codec フィルタ結果が空でも throw しない
- `mid` / `mLineIndex` は従来どおり設定する（後段の ICE 解決と answer の m-line 対応に必要）
- 受け入れ不可を transceiver 上の状態として残す（後述の rejected 表現）。`direction === "inactive"` と同一視しない
- 空 codec のときは以降の sender/receiver 準備を skip する
  - `getLocalRtpParams()` → `sender.prepareSend()`
  - `receiver.prepareReceive()` / `router.registerRtpReceiverByRid` / `registerRtpReceiverBySsrc`
  - `onTrack`（既存の `remoteMedia.port !== 0` ガードに加え、codec reject でも発火しない）
  - `setupTWCC()`
- すでに remote 側で reject 済み（`port === 0`）かつローカル codec も一致しない場合も、SRD は成功しパイプラインを組まない

`peerConnection.ts` の SRD ループは `sdpManager.setRemoteDescription()` で pending remote を先に保存してから `setRemoteRTP()` する。現状 throw すると remote SDP だけ部分適用された中途半端な状態になる。throw を止めることでループが最後まで回り、`signalingState` が `have-remote-offer` になる。

### 2.2 Answer SDP で reject 済み m-line を生成する

対象: `packages/webrtc/src/sdpManager.ts` の `buildAnswerSdp()` / `createMediaDescriptionForTransceiver()` / `addTransportDescription()`。

現在 `addTransportDescription()` は `media.direction === "inactive"` のときだけ `port = 0` にする。codec 不一致による reject は direction とは別フラグで port 0 にする。

reject した answer m-line が満たすべきこと:

- offer と同じ位置・同じ MID・同じ media type / proto を維持する
- port は `0`（通常受け入れ時は `DISCARD_PORT` = 9）
- SDP として有効な format token を少なくとも 1 つ残す。`transceiver.codecs` が空だと `createMediaDescriptionForTransceiver()` の `codecs.map(c => c.payloadType)` が空 `fmt` になり `m=video 0 UDP/TLS/RTP/SAVPF ` という不正 SDP になる。offer 側 `remoteMedia.fmt`（少なくとも先頭 PT）をコピーする
- offer / answer の m-line 数と順序を崩さない

### 2.3 BUNDLE ルールを answer 経路に適用する

対象: `sdpManager.ts` の `buildAnswerSdp()` の `a=group:BUNDLE` 構築、および必要なら `peerConnection.ts` の bundle transport 割り当て / `secureTransportManager.ts` の candidate の `sdpMid` / `sdpMLineIndex`。

現状 `buildAnswerSdp()` は `bundlePolicy !== "disable"` なら **全** media の MID を BUNDLE group に入れる。reject した m-section は group から外す（RFC 8843 §7.3.4）。

- 非 tag m-line だけ reject: group からその MID を除き、offerer-tagged を tag のまま残す
- offerer-tagged を reject: 残った受け入れ m-section を新しい answerer-tagged にする。`a=group:BUNDLE` の先頭 identification-tag がその tag。既存の `handleNewIceCandidate()` は bundled 時に `sdpMLineIndex = 0` と `_localDescription.media[0]` の MID を使うため、m-line 0 が reject だと trickle が reject 済み MID に紐づく。tag 再選択後は **受け入れ側の tagged m-section** を見る
- BUNDLE group の m-section を全部 reject したとき: answer から `a=group:BUNDLE` を省略する
- unbundled（`bundlePolicy: "disable"`）: 非対応 m-line だけ port 0、対応 m-line は通常の非ゼロ/discard port。BUNDLE 再選択は不要

BUNDLE 付き remote offer の SRD では、先頭 audio/video の DTLS を `bundleTransport` にして後続が共有する。tagged video を reject しても、offer の BUNDLE ICE パラメータは tagged 側にあることが多いので、remote offer 適用時にその ICE/DTLS を共有 transport へ入れる現状は維持してよい。answer 生成時に reject 済み m-line を group から外し、tag を受け入れ m-line へ移す。

### 2.4 ICE candidate の m-line / MID 対応を維持する

trickle の `addIceCandidate({ sdpMid, sdpMLineIndex })` は remote description の media 配列で解決する（`SecureTransportManager.resolveCandidateMediaIndices()`）。非対応 m-line でも remote offer の media / MID / index は残す。ローカル answer が reject しても、remote が送るその MID の candidate が「Media section for sdpMid was not found」にならないこと。

### 2.5 回帰テスト

配置: 既存の `packages/webrtc/tests/issue/141.test.ts` / `142.test.ts` に合わせ、`packages/webrtc/tests/issue/705.test.ts`。複数ケースの Arrange はテストファイル先頭か sibling の 1 ファイルにまとめ、Act / Assert には日本語コメントを付ける。

必須ケース（Issue 記載）:

1. **unbundled**: 非対応 video + 対応 audio → SRD 成功。answer は video のみ port 0、audio は受け入れ。MID / 順序維持
2. **BUNDLE の非 tag m-line が非対応**: spec 通り reject し、group から外す。SRD は throw しない
3. **BUNDLE の offerer-tagged が非対応**: spec 通り group/tag 再選択。SRD は throw しない
4. **remote が既に `port 0` で、ローカル codec も無い**: SRD 成功。track / media pipeline を組まない（既存の `peerConnection.test.ts`「rejected sections do not dispatch track」は opus 対応あり + port 0。codec 不一致との組み合わせは別ケース）
5. **m-line 順 / MID 維持**: 後段の `addIceCandidate({ sdpMid / sdpMLineIndex })` が失敗しない

再現の注意:

- unbundled は必ず `bundlePolicy: "disable"`。デフォルトは `"max-compat"`
- `new RTCPeerConnection({ codecs: { audio: [useOPUS()] } })` は `deepMerge` が `codecs` オブジェクトを丸ごと置換するため `video` は消える。明示的に video を空にしたい場合は `video: []` でも可
- 受け入れ側に video codec を載せない（WorkAdventurer のダミー `useVP8()` をテストで再現しない）

## 3. 技術的な実装アプローチ（調査結果）

プロトコル経路は ICE → DTLS → SCTP/RTP。本修正は `packages/webrtc` の SDP / transceiver 層のみ。`ice` / `dtls` / `rtp` には手を入れない。

### 現在の呼び出し順

1. `RTCPeerConnection.setRemoteDescription()`（`peerConnection.ts`）
2. `SDPManager.setRemoteDescription()` が SDP を parse し pending remote に保存
3. 各 `remoteMedia` について transceiver を照合 or `addTransceiver(..., { direction: "recvonly" })`
4. bundled なら先頭の DTLS を共有
5. `TransceiverManager.setRemoteRTP()` が codec を交差し、空なら throw
6. 成功時のみ ICE/DTLS パラメータ適用、最後に `signalingState = "have-remote-offer"`
7. `createAnswer()` → `SDPManager.buildAnswerSdp()` が remote m-line 順に transceiver を引き、`createMediaDescriptionForTransceiver()` で answer media を作る

### 推奨する状態の持ち方

Issue の示唆どおり、codec reject は `direction === "inactive"` と混ぜない。`RTCRtpTransceiver`（`packages/webrtc/src/media/rtpTransceiver.ts`）に rejected を表すフラグ（または同等の内部状態）を足し、次で参照する。

- `setRemoteRTP()`: 空 codec、または必要なら remote `port === 0` を拒否として記録
- `buildAnswerSdp()` / `addTransportDescription()`: そのフラグで port 0、BUNDLE 除外、fmt フォールバック
- `setRemoteRTP()` のパイプライン skip 条件

`stopped` / `stopping` は `transceiver.stop()` 用なので流用しない。

### BUNDLE / ICE で触りやすい箇所

- `sdpManager.ts`: answer の `GroupDescription("BUNDLE", ...)`。全 MID 追加をやめ、受け入れ m-line のみ。tag は group 先頭
- `peerConnection.ts` SRD: `remoteIsBundled` 時の `bundleTransport`。reject しても offer 適用自体は共有 transport でよい。answer 側 tag と local candidate の MID を一致させる
- `secureTransportManager.ts` `handleNewIceCandidate()`: bundled でも常に m-line 0 を tag 扱いしない。reject 後は answerer-tagged の MID / index を使う
- remote candidate 解決は現行の remote media 配列のままでよい。SRD が remote m-line を落とさなければ足りる

### 既存テストとの関係

- `tests/integrate/peerConnection.test.ts` の port 0 + opus で ontrack しないケースは維持
- `tests/issue/141.test.ts` / `142.test.ts` は bundled 実 SDP の SRD。video codec デフォルト `useVP8()` があるので現状通り通る。デフォルト codec を変えない
- 本修正向けの WPT allowlist 項目は無い。WPT runner の厳しい shim を `packages/webrtc/src` に持ち込まない

### 変更しないもの

- `PeerConfig.codecs` のマージ仕様（`deepMerge` はネストを再帰せず `codecs` を置換する）
- 公開メソッド名、例外型を「空 codec の offer を reject するため」に増やすこと
- 非対応 m-line を SDP から削除すること（位置対応が壊れる）

## 4. 考慮すべき制約や注意点

- **例外を消すだけでは不十分。** SRD 成功のあと `createAnswer()` が不正 SDP（空 fmt、reject なのに port 9、BUNDLE に reject 済み MID が残る、tagged が port 0 のまま）を出さないこと
- **WorkAdventurer のトポロジを「音声だけ必ず成立」と決め打ちしない。** Issue の本丸は「codec 不一致を SRD 例外にしない」こと。tagged video を reject した BUNDLE は RFC 8843 の tag 再選択に従う。その結果として音声専用アプリがダミー VP8 無しで SRD できるのが望ましい
- **互換モードでは reject と inactive を混同しない。** inactive は direction 交渉。追加要件の積極モードのみ、明示 opt-in で inactive を port 0 にする従来挙動を許容する。port 0 を inactive だけに結び付けると、recvonly のまま reject したいケースや、逆に inactive だが受け入れ可能なケースと衝突する
- **fmt トークン必須。** RFC 3264 は reject 時も offer の format を少なくとも 1 つ残す。空 `m=` 行は不可
- **部分適用。** 先頭 m-line で throw すると、作られた transceiver と pending remote が残り signalingState が更新されない。throw 廃止後も、reject した section に対して `prepareSend`（`params.codecs[0]` が undefined）や空 encodings の `prepareReceive` を呼ばない
- **local ICE の MID。** BUNDLE で m-line 0 を reject すると、現行の `media[0]` 前提の candidate 付与が壊れる。SRD だけでなく `createAnswer` / trickle の local 側も見る
- **テストの bundlePolicy / codecs。** デフォルト `max-compat` とデフォルト `video: [useVP8()]` のままでは Issue の最小再現にならない
- **検証範囲。** 単一パッケージのプロトコル修正。先に `packages/webrtc` の test / type。WPT や examples e2e は必須にしない。公開プロトコル動作が変わるので `changelog.md` の Unreleased に bug fix を追記する
- **Windows 非サポート。** 実行・検証は Unix 系のみ
- **ルート原因を直す。** テストで例外を握りつぶしたり、下流アプリに video codec を足させる回避を本家に入れたりしない

## 5. 完了条件

- [ ] `setRemoteRTP()` は offered m-line の交渉 codec が空でも `Error("negotiate codecs failed.")` を投げない
- [ ] その m-line は answer で元の位置・MID・type/proto を保ったまま port `0` で reject され、有効な format token がある
- [ ] 非対応 section では sender/receiver 準備・router 登録・`onTrack`・TWCC セットアップを行わない
- [ ] unbundled: 非対応 video + 対応 audio で SRD 成功、answer は video のみ reject、audio は受け入れ
- [ ] BUNDLE 非 tag の非対応 m-line: group から除外。SRD は例外にしない
- [ ] BUNDLE offerer-tagged が非対応: RFC 8843 に沿った tag 再選択。SRD は例外にしない
- [ ] remote `port 0` かつローカル codec 無し: SRD 成功、media pipeline / track を組まない
- [ ] reject した m-line の MID / `sdpMLineIndex` に対する `addIceCandidate` が remote offer 適用後も解決できる
- [ ] 回帰テストを `packages/webrtc/tests/issue/705.test.ts`（または同等）に追加。Arrange / Act / Assert、Act/Assert に日本語コメント、Arrange ヘルパーは使い回せる 1 箇所
- [ ] `cd packages/webrtc && npm run type` と、新規・関連テストを含む `cd packages/webrtc && npm test` が通る
- [ ] `changelog.md` Unreleased に本修正（#705）を追記する
- [ ] 既存の port 0 ontrack テスト、issue 141/142、デフォルト codec の通常 negotiation を壊していない


## 6. 追加要件: m-line 再利用モードと transceiver.stop

### 6.1 質問への回答と対象手順

`removeTrack → 交渉 → stop → 交渉 → 新規 addTransceiver → 交渉` を
`removeTrack と stop → 交渉 → 新規 addTransceiver → 交渉` に短縮しても、元が2本なら m-line は2本を維持する。
条件は、stop による port 0 の offer/answer を完了してから新規 transceiver を追加すること。
停止交渉前に追加した場合は停止予定の位置を先取りせず追加 m-line を使う。
再利用時には同じ index に新しい MID を割り当て、停止した transceiver オブジェクトを復活させない。

### 6.2 モード選択

- `PeerConfig.mLineReuse: "compatible" | "aggressive"` を追加する。既定は `"compatible"`。
- `compatible`: inactive は非ゼロ port のまま保持。停止の拒否交渉が完了した位置を次の新規 transceiver に再利用する。
- `aggressive`: inactive を port 0 にする従来の wire 挙動を明示 opt-in として残す。拒否交渉完了後に位置を再利用する。これは一時停止ではなく拒否なので、旧 transceiver の再開は保証しない。
- 生成時に選択し、`setConfiguration()` による途中変更と無効な値は拒否する。
- 両モードで #705 の codec reject、format/MID/順序、BUNDLE 除外、ICE candidate 対応を維持する。
- 互換モードでローカル `addTransceiver()` が inactive 枠を横取りしない。`addTrack()` の自動再使用は未送信・非停止・非拒否の sender に限定する。

### 6.3 stop と再利用の追加仕様

- `stop()` は冪等。sender/receiver、router 登録、RTCP/NACK/TWCC/pending RTP を停止・解除し、交渉を要求する。共有 ICE/DTLS transport は破棄しない。
- `stopping` の transceiver は次の local offer で port 0。拒否 answer の適用後に `stopped` が確定する。
- 互換モードで answerer が stop を呼んだだけでは answer を強制的に port 0 にしない。自分からの次の offer で停止を交渉する。
- SDP に未関連付けの transceiver を stop した場合、停止済みの新規 m-line を生成しない。
- 新規 transceiver は交渉済み port 0 の位置を優先して使い、新 MID を割り当てる。旧 MID/index は旧 transceiver から外す。
- remote offer に同じ位置・新 MID が現れた場合も、旧停止済み transceiver と区別し、新しい receiver/router で受信する。
- BUNDLE の先頭停止、unbundled、SCTP が RTP より前にある SDP、繰り返し再利用を検証する。
- 当初要件の codec 不一致を表す `rejected` と永久停止を区別する方針は保持する。全 reject ケースの JSEP 状態機械や bundle-only 例外を網羅する変更ではない。

根拠: RFC 9429 §5.2.2、W3C WebRTC の transceiver.stop / addTrack。
詳細とモードの使用例: `docs/design/705-media-rejection-and-removetrack.md`、`packages/webrtc/README.md`。

### 6.4 追加完了条件と実行結果

- [x] 短縮手順でも m-line 2本を維持する。Chromium 151.0.7922.34 同士、および werift の回帰テストで確認。
- [x] compatible / aggressive を設定で選択でき、既定値・無効値・途中変更を検証。
- [x] 互換モードの stop、拒否交渉後の同じ index / 新 MID 再利用、交渉前に再利用しない条件を実装。
- [x] 停止予定の answerer が自ら offerer になって停止するケースを検証。
- [x] BUNDLE / unbundled、SCTP 先行、未関連付け stop、繰り返し再利用後の RTP 受信を検証。
- [x] `packages/webrtc/tests/issue/705-reuse.test.ts` に11件追加。Arrange helper は `705.helpers.ts` に集約し、Act/Assert に日本語コメントを記載。
- [x] Chromium ↔ werift の removeTrack E2E 7件成功（リトライなし）。両モード、ブラウザ stop、werift stop を含み、新規 MID/index での実 RTP 受信を確認。
- [x] webrtc パッケージ型チェック・全テスト成功（318 passed / 3 skipped）。
- [x] workspace `npm run type` と `npm run test:small` 成功（import-test を含む）。E2E の型チェック・ビルド成功。
- [x] changelog とモード選択・仕様比較ドキュメントを更新。

WPT、Firefox/Safari、E2E 全件、workspace 全体 CI は今回未実施。既存の承認は追加要件への承認を意味しない。追加差分のレビュー結果は別途記録する。

## 7. PR #711 レビュー P1 指摘への対応

レビュー対象 head `275ac369` に対する P1 5 件を以下の方針で修正した。`rejected`（codec 不一致の pending 状態）と `stopped`（terminal）の分離を基本とし、確定は answer の commit 時点で行う。

- **answer/pranswer の codec 検証は commit 前に** (`transceiverManager.validateAnswerCodecs` + `peerConnection.setRemoteDescription` の退避・復元): non-zero の answer/pranswer が pending local offer と共通 codec を持たない場合は `InvalidAccessError` で SRD を失敗させ、signaling・各 description を不変に保つ。remote port 0 は受理する。`setRemoteRTP` 側にも防御的な throw を残す。
- **pending rejection では current pipeline を維持** (`transceiverManager.setRemoteRTP`): 再交渉 offer の codec 不一致で既存 pipeline（`sender.codec` あり）がある場合は `rejected=true` のまま準備 skip のみで return し、旧 codec RTP の受信・既存 track を local rejected answer の `setLocalDescription()` まで維持する。新規 m-line は従来どおり skip する。
- **answer BUNDLE は offered 基準で組み立て** (`sdpManager.appendAnswerBundleGroup`): offer 用 `appendBundleGroup` と分離し、remote offer の BUNDLE membership と identification-tag 順を基準に、reject 済み (port 0) MID だけを除外する。offer に無い m-section は束ねず、subsequent negotiation も offered group から継承する。
- **rejected answer の確定で terminal stopped** (`peerConnection.finishMediaStops` + `TransceiverManager.runWithoutNegotiationNeeded`): local/remote の port-zero answer 適用時に `mLineReuse` に関係なく `rejected` transceiver を `forceStop()` し、交渉済み port-zero slot を新 MID で再利用可能にする。確定処理の `onStopping` による `negotiationneeded` 再発火は抑える。pending 段階では `rejected` と `stopped` を分離したままにする。
- **addTrack の再利用条件に非停止 sender を要求** (`transceiverManager.addTrack`): 両 predicate に `!t.sender.stopped` を追加し、`removeTrack()` 後の停止済み sender を再利用しない（fresh transceiver を作成する）。

回帰テスト: `packages/webrtc/tests/issue/705-review.test.ts` に 8 件追加（answer BUNDLE の順序・部分束ね・tag 再選択と次回交渉、answer/pranswer 検証と状態不変および port-zero 対照、remote port-zero answer の lifecycle、negotiationneeded 非再発火、交渉前 removeTrack→addTrack の実 RTP 受信）。`705.test.ts` の 3 件を新仕様に更新（pipeline は確定まで維持、reject 枠は新 MID で再利用、`currentDirection` は `stopped`）。

## 8. PR #711 レビュー P1 Round2 指摘への対応

`7aa3b76f` 承認後に追加された P1 6 件を以下の方針で修正した。

- **BUNDLE transport は membership/tag 基準で共有** (`peerConnection.setRemoteDescription` の事前フェーズ + `createIndependentTransport`): offered group の member のみ tagged MID の transport を共有し、group 外は live かつ受け入れ時だけ独立 transport を立てる。停止済み・remote port 0 の section は共有に残して garbage を増やさない。新規 m-line は live transport のみ再利用し、停止済みだけが掴む dead transport を拾わない（`findOrCreateTransport`）。
- **ICE 集約・gather・restart・connect は live transport のみ** (`SecureTransportManager.liveDtlsTransports/liveIceTransports`): 確定済み reject 枠の transport に failed/checking を引きずられず、tag 切替後も接続・RTP が継続する。close/stats/role は従来どおり全 transport が対象。
- **local candidate の tag は negotiated tag 優先** (`sdpManager.getNegotiatedBundleTag` + 所有者判定): remote answer/pranswer の group 先頭が local 受け入れ m-line に対応すればそれを使い、group 外 transport 由来は持ち主自身の MID/index で送る。共有 transport の持ち主は live を優先し、reject 済み MID でラベルしない。
- **answer 検証は commit 前の exact pending offer 基準** (`peerConnection` が退避済み `prevPendingLocalDescription` を渡す): config フォールバックでは検出できない「offer で絞った codec 以外だけの final answer」を reject し、`have-local-offer` と pending を保つ。
- **removeTrack は reusable detach** (`transceiverManager.removeTrack`): `sender.stop()` をやめ `replaceTrack(null)` 相当の detach にし、同じ sender への `replaceTrack` + direction 復帰で同一 MID/index 再開を可能にする。terminal `sender.stop()` は `transceiver.stop()` 経路に限定。
- **protocol-driven stop の negotiationneeded を抑制** (`runWithoutNegotiationNeeded` の generic 化 + SRD 適用部全体のラップ): remote SDP 由来の stop/finalize では発火させず、application の明示的 `stop()` は従来どおり発火する。
- **rollback で transceiver media 状態を復元** (`snapshotTransceiverMedia/restoreTransceiverMedia` + `pendingTransceiverSnapshot`): offer/pranswer 適用前に退避し、remote rollback で復元する。port 0 offer/pranswer の terminal stop・pipeline 破棄も commit まで遅延させた。remote pranswer からの rollback も許可し、その場合は pending local offer を残して `have-local-offer` へ戻す。
- **SSRC-less placeholder track の重複解消** (`router.registerRtpReceiverBySsrc` + `setRemoteRTP` の ontrack 条件): transceiver ごとに1本だけ作って再利用し（codec は更新）、receiver tracks が増えたときだけ ontrack する。再交渉後の未知 SSRC RTP は同じ track に届く。

回帰テスト: `705-review.test.ts` に Round2 として 11 件追加（partial BUNDLE の transport 分離と両 m-line 実 RTP および trickle MID、tag 切替後の接続と restart candidate の negotiated tag、offer-narrowed final answer/pranswer の失敗と pending 保持、同一 sender 再開、remote answer/re-offer の event 沈黙と明示 stop の対照、unsupported/port 0 re-offer と port 0 pranswer の rollback 復元、SSRC-less 再交渉の track 単一性と未知 SSRC 配送）。`705-review.test.ts` の交渉前 removeTrack→addTrack テストは detach 仕様に更新（live sender 再利用）。

検証: `packages/webrtc` 全体 338 passed / 3 skipped、`npm run type`（webrtc・workspace）成功、`npm run format` 適用済み。E2E ブラウザテスト（removeTrack・oneway 含む）は当環境の headless Chromium で接続確立自体がタイムアウトするため未確認（変更無関係の oneway も同様に失敗する環境起因）。WPT・Firefox/Safari・全体 CI は未実施。
