werift DTLS 実装の自律的な完成

対象リポジトリ:

"shinyoshiaki/werift-webrtc"

対象ブランチ:

"ticket/277e0dc5-a06d-409b-9ec1-63d67af71837"

このブランチの作業を継続し、リポジトリ内の対応する ticket ファイルに記載された要件・完了条件を満たすところまで実装を完成させてください。

最重要方針

既知のレビュー指摘だけを機械的に修正して終了してはいけません。

既知の問題を修正したあと、その問題と同じ root cause、同じ state machine、同じ lifecycle、同じ ownership boundary、同じ async/race pattern を持つ箇所を自律的に探索してください。

特に以下を繰り返してください。

1. 問題を発見する
2. root cause を特定する
3. 同じ root cause が存在する箇所を横断検索する
4. 再現テストを書く
5. 実装を修正する
6. 関連テスト・回帰テストを実行する
7. 修正によって新しい状態遷移・race・resource leak が発生していないか再レビューする
8. 新しい P0 / P1 / P2 問題が見つかった場合は再び 1 に戻る

P0 / P1 / P2 相当の問題が見つからなくなるまで、このループを自律的に継続してください。

単に「テストが通る」ことを完成条件にしないでください。

---

1. 最初に読むもの

最初に必ず以下を確認してください。

- 対象ブランチと "develop" の全差分
- リポジトリ内のこのticketに対応するticketファイル
- DTLS 1.3 / DTLS 1.2 fallbackに関係する既存research/plan
- "packages/dtls"
- DTLSを利用するWebRTC側コード
- GitHub Actions
- interopテスト
- README / public API documentation

ticketファイルに記載された仕様・完了条件を source of truth としてください。

コードだけを見て「これで十分」と判断しないでください。

---

2. 現時点で特に再確認すべき問題

これまでのレビューで多数のlifecycle/state-machine問題が見つかっています。

現在修正済みに見えるものも含め、再発していないか確認してください。

Version negotiation / HVR

- "[DTLS1.3, DTLS1.2]" dual negotiation
- HVRを受けてもunauthenticated downgradeしないこと
- original DTLS 1.3 ClientHello stateを必要な期間保持すること
- original ClientHelloに対するHRR/ServerHelloとHVRがraceしても正常動作すること
- HRR/ServerHello packet loss後もoriginal ClientHelloを再送できること
- DTLS 1.2へのcommit前にServerHelloを十分検証すること
- malformed ServerHelloでversion commitしないこと
- "[1.2,1.3]"入力のpublic semantics
- downgrade sentinel検証を弱めないこと

Association ownership

UDPとcustom "DtlsHandshakeCarrier.inject()"の両方で、

- 同じassociation dispatcherを通ること
- candidate engineが直接RX ownershipを奪わないこと
- probing
- committed12
- committed13
- closed

の各phaseで受信先が一意であること。

losing candidateから遅れて届いたpacket/eventでwinnerが変化しないこと。

Peer pinning

受信packetをdropするだけでは不十分です。

spoof packetによって、

- mutable "rinfo"
- retransmission destination
- application-data destination
- carrier destination

が攻撃者へ変更されないことを確認してください。

RXだけでなく TXのpeer ownership もassociationが持つべきです。

Candidate lifecycle

以下をassociation単位で管理してください。

- active DTLS 1.3 engine
- parked DTLS 1.3 engine
- DTLS 1.2 legacy flight
- carrier
- retransmission timer
- async waiter
- event subscription
- UDP RX callback
- generation/token
- candidate pending task

version commit / close / fatal / graceful close後にlosing candidateが復活してはいけません。

Timer / async task ownership

以下すべてについて、単に「次回wake時に終了」ではなく、可能な限り 即座にpending timer/taskをcancel してください。

- local close
- peer close
- fatal alert
- handshake completion
- version commit
- losing candidate disposal
- association failure

特に以下を横断検索してください。

- "setTimeout"
- "timers/promises"
- Promise-based sleep
- retry loops
- retransmit loops
- polling loops
- unresolved resolver
- Event subscriptions
- async callback
- scheduled carrier task

"flightTimers"だけを直して満足しないでください。

---

3. terminal-state invariant を明確化する

associationがterminalになった後は、versionやroleに関係なく同じ基本invariantを持たせてください。

少なくとも以下を検討してください。

terminal association
  connected = false

  new handshake start = prohibited
  application send = prohibited
  exporter = prohibited
  peer certificate access requiring active association = prohibited
  reconnect on same association = prohibited
  incoming late packets = ignored
  losing candidate events = ignored

  retransmission timers = 0
  pending waiters = 0
  carrier scheduled tasks = 0

  transport/carrier ownership = released appropriately

特に以下を比較してください。

- pure DTLS 1.2 client
- pure DTLS 1.2 server
- pure DTLS 1.3 client
- pure DTLS 1.3 server
- dual → committed12
- dual → committed13
- probing中
- close中
- fatal処理中

同じ意味の操作がversion/roleによって異なる結果になっていないか確認してください。

---

4. Public API lifecycle を自律探索する

以下のAPIをterminal state前後で検証してください。

- "connect()"
- "close()"
- "send()"
- exporter
- peer certificate取得
- "onConnect"
- "onError"
- "onClose"
- "carrier.inject()"

特に、

connect
close
connect again

connected
fatal
connect again

connected
close_notify
connect again

connected
fatal
send

connected
close_notify
send

のような 「終了後に再利用できてしまわないか」 を調べてください。

local close / remote close / fatal closeで挙動が不必要に異ならないことも確認してください。

---

5. Alert semantics

DTLS 1.2のalert処理をRFCに照らして再確認してください。

最低限、

fatal alert
  → association failure
  → terminal

close_notify
  → graceful close
  → terminal

other warning
  → 原則connection維持

を区別してください。

単に"onError"や"onClose"イベントを発火するだけではなく、内部stateも整合させてください。

また、

- "onError"
- "onClose"

の発火回数と順序を明示的にテストしてください。

terminal transitionはidempotentであるべきです。

---

6. Race conditionを積極的に作る

happy pathだけでなく、意図的にraceを発生させてください。

例:

close vs carrier.inject
close vs UDP RX
fatal vs retransmission timeout
version commit vs legacy Flight await
commit13 vs Flight5 resume
commit12 vs late DTLS 1.3 ServerHello
commit13 vs late DTLS 1.2 ServerHello
HVR vs original 1.3 HRR
HVR vs original 1.3 ServerHello
close vs waitForReady sleep
fatal vs waitForReady sleep
close_notify vs application send

テストでは必要に応じて、

- deferred Promise
- fake timer
- controlled packet forwarding
- intentionally dropped datagram
- manually held packet
- injected carrier
- separate spoof UDP peer

を利用してください。

単純な"setTimeout()"ベースのflaky testは極力避けてください。

---

7. Tests-first

新しい問題を発見したら、可能な限り先に再現テストを追加してください。

テストは「内部変数が期待値」というだけではなく、できる限り観測可能な挙動を検証してください。

例:

peer Aと接続
↓
peer Bからspoof
↓
client.send()
↓
Aだけが受信
Bは受信しない

fatal alert
↓
onError
↓
onClose
↓
send/connect/exporter reject
↓
late packetをinject
↓
state変化なし

close
↓
全timer/resolver/taskが即0
↓
RTOを進める
↓
追加sendなし

---

8. RFC確認

実装判断に疑問がある場合は、推測ではなく一次仕様を確認してください。

主に:

- RFC 9147 — DTLS 1.3
- RFC 8446 — TLS 1.3
- RFC 6347 — DTLS 1.2
- RFC 5246 — TLS 1.2
- DTLS-SRTP関連RFC
- ticketが直接参照しているRFC/draft

特に以下はRFCと照合してください。

- version negotiation
- supported_versions
- downgrade protection
- HRR
- legacy_cookie / HVR
- retransmission
- alert
- close_notify
- KeyUpdate
- ACK
- anti-replay
- epochs
- CID unsupported behavior
- MTU / fragmentation
- amplification bounds

---

9. Security-oriented review

security boundaryとして以下を自律探索してください。

- unauthenticated packetによるstate commit
- version downgrade
- spoofed peerによるTX destination変更
- replay
- old epoch acceptance
- malformed recordによるresource exhaustion
- unbounded reassembly
- unbounded early data
- stale candidate resurrection
- unauthenticated alertによるDoS
- timer/resource leak
- association close後のpacket acceptance
- certificate/signature validation不足
- key_share validation不足

「クラッシュしない」だけではなく、攻撃者が接続状態や送信先を操作できないことを確認してください。

---

10. Interop

最低限以下を最後まで確認してください。

werift ↔ werift

- DTLS 1.2
- DTLS 1.3
- dual ↔ dual
- dual client ↔ 1.2-only server
- 1.3-only ↔ 1.2-only failure
- DTLS-SRTP exporter

external

ticketの完了条件に従い、

- BoringSSL DTLS 1.3
  - werift client → BoringSSL server
  - BoringSSL client → werift server
- OpenSSL DTLS 1.2

を実行してください。

外部テストが環境依存で実行できない場合は、実行できなかった理由を正確に記録してください。

---

11. 必須検証

少なくとも以下を実行してください。

cd packages/dtls
npm run type
npm test

さらにリポジトリの現在のscript構成を確認して、

npm run type:packages
npm run ci

等、ticketの完了条件に必要なものを実行してください。

BoringSSL / OpenSSL interopも実行してください。

一部testだけ通して「完了」としないでください。

---

12. 自律レビューを最低2周行う

全テストがgreenになった時点で、すぐ終了してはいけません。

その状態からもう一度、実装者ではなく 敵対的code reviewerの視点 でレビューしてください。

最低2周:

Review pass 1

- correctness
- RFC compliance
- race
- async lifecycle
- security
- resource ownership

Review pass 2

pass 1で修正した変更だけを対象にせず、branch全体を再び確認してください。

「この修正によって別の場所に新しい非対称性が発生していないか」を重点確認してください。

---

13. 重要度

問題は以下で分類してください。

P0

- security compromise
- key/identity validation破壊
- severe interoperability regression
- major data corruption

P1

- 接続が成立しない主要経路
- incorrect version commit
- spoof / downgrade可能
- terminal stateからassociationが復活
- incorrect destinationへの送信
- fatal後のassociation利用
- major state-machine race

P2

- timer/task/resource leak
- role/version lifecycle inconsistency
- error handling inconsistency
- missing regression coverage
- bounded but incorrect edge behavior

P0/P1/P2が残っている状態で「完成」と宣言してはいけません。

---

14. コード品質

個別の症状ごとに特殊なbooleanやifを追加し続けるのではなく、root causeを解消してください。

優先する構造:

association
 ├─ phase
 ├─ pinned peer
 ├─ candidate ownership
 ├─ RX dispatcher
 ├─ timers/tasks
 ├─ transport/carrier
 └─ terminal transition

以下のような責務を可能なら一箇所へ集約してください。

- commit12
- commit13
- fail
- graceful close
- local close
- teardown

同じ処理をclient/server/1.2/1.3へコピーしないでください。

---

15. コメント・ドキュメント

コメントには「何をしているか」ではなく、

- なぜ必要か
- どのRFC要件か
- どのraceを防いでいるか
- なぜassociation layerがownershipを持つか

を記載してください。

public API semanticsが変化した場合はREADME/API docsも更新してください。

生成ドキュメントがある場合は同期してください。

---

16. Git

作業は現在の

"ticket/277e0dc5-a06d-409b-9ec1-63d67af71837"

ブランチ上で継続してください。

修正を論理的な単位でcommitしてください。

最終的にすべての変更をpushしてください。

既存のユーザー変更を勝手にrevertしないでください。

不要なforce pushはしないでください。

---

17. 完了条件

以下をすべて満たすまで作業を終了しないでください。

- ticketの全要件を満たす
- ticketの全completion gateを満たす
- P0なし
- P1なし
- P2なし
- DTLS 1.2 regressionなし
- DTLS 1.3 regressionなし
- dual fallback正常
- BoringSSL interop正常
- OpenSSL interop正常
- tests green
- typecheck green
- resource leakなし
- close/fatal/graceful-close後のcandidate復活なし
- role/versionごとのterminal semanticsに説明不能な非対称性なし
- public documentation同期済み
- 最終自律レビューを2周実施済み
- commit済み
- push済み

---

18. 最終報告

最後に以下を簡潔に報告してください。

発見した問題

P0 / P1 / P2ごとに列挙。

Root cause

症状ではなく原因を書く。

修正内容

どのownership/state-machine/lifecycleをどう変更したか。

追加テスト

再現条件と、修正後に何を保証するテストか。

RFC確認

参照したRFC sectionと実装判断。

実行結果

実際に実行したcommandと結果。

成功していないものを成功したと書かないこと。

残存リスク

P0/P1/P2が本当に0か明示する。

Git

- 最終commit SHA
- push先branch

---

行動原則

途中でユーザーへ確認を求めず、合理的に判断できる内容は自律的に判断してください。

一つ問題を修正するたびに「完了」と判断せず、

«この問題を生んだ設計上の原因は他の経路にも存在しないか？»

を必ず問い直してください。

特に、

«clientで直したならserverも確認する
1.2で直したなら1.3も確認する
UDPで直したならcarrierも確認する
RXで直したならTXも確認する
closeで直したならfatal/close_notify/handshake-completeも確認する
success pathで直したならloss/race/spoof pathも確認する
timerを直したならwaiter/subscription/callbackも確認する»

という横展開を必須としてください。

目標は「現在知られているレビュー指摘を0件にすること」ではなく、このticketの実装を、追加レビューで同系統のP0〜P2が見つからない水準まで完成させることです。